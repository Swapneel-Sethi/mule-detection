# Dependencies / build output
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# Environment files — never commit secrets. .env.example is the only exception.
.env*
!.env.example

# Deploy / local tool state
.vercel/
.netlify
.netlify_token
.hermes/

# Graphify tool state — cache and dated snapshot dirs regenerate every run
graphify-out/cache/
graphify-out/20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]/

# Logs
*.log

# Python
__pycache__/
*.pkl
scripts/_learned_weights.json

# Large data files
*.csv
public/synthetic_dataset.json

# Debug/preview artifacts and agent-session transcripts
/constellation_verify.png
session-*.md

# Secrets — service-account keys and other key files
*.json.key
*serviceAccountKey*.json
*service-account*.json
