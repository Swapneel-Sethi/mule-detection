# Domain: UI/UX & FRONTEND

Files: all src/components/** (DashboardContent 191, AccountsContent 171, TransactionsContent 216, AlertsContent 222, AnalyticsContent 544, SankeyChart 297, PlotlySankey 19, MuleGalaxy 1070, Sidebar 117, SidebarOverlay 83, ui/* ~666 total) + pages under src/app/{page,accounts,transactions,alerts,analytics,graph} + globals.css (147) + APP/design-tokens.md.
Prior-run: MASTER DOMAIN 1 items 1-31, DOMAIN 3 items 1-27, DOMAIN 5 items 1-21.

## [ui:dash] DashboardContent.tsx + page.tsx
R1 items D5#1 (safeStat preserving zeros), #2 (highRiskCount fallback disjoint def 6,783), #12 (refresh banner parity), #14 (Top Risk empty state), #21 residuals (hardcoded isMule badge rows? fabricated 105,501 fallback?). R2 D5#21 Turnover label ambiguity (Dashboard sums FLAGGED-only but labeled generic) — relabel now?. R3 stat math, category bars divide-by-zero/clamp, top-risk sort parity with API.

## [ui:list] AccountsContent + TransactionsContent + pages
R1 D5#3 (  subtitle literal bug), #6 (Date-per-comparison allocations), #7 (container width convention), #13 (footer Showing X of Y truth), #15 (whitespace search scan, DISPLAY_CAP, risk-bar clamp, name search, casts, memoization), #16 utils fixes visible here. R2 D5#20 silent truncation at fetched-limit vs true 8,578 flagged — add hint now?. R3 virtualization absence over big lists, formatters, row keys.

## [ui:alerts-analytics] AlertsContent + AnalyticsContent + SankeyChart + PlotlySankey
R1 D6#10 (pageIndex reset on filter change; severity colors via tokens; Invalid Date guard; empty-state split no-data vs no-match; table caption), D6#1 (Y-axis /1e7 mislabeled M — verify BOTH axis + tooltip + cast guard), D3#2 (plotly_click pointNumber fix), D3#10-12 (sankey rounding/sub-₹1K/guard), D1#15 (ErrorState live-region). R2 D3#19 residuals (FPS meter re-render loop, legend swatch color mismatch, BFS hidden corridors), register A33. R3 recharts ResponsiveContainer heights, Plotly SSR guards/dynamic import/bundle weight, aria-pressed toggles.

## [ui:galaxy] graph/page.tsx + api/graph/mule-galaxy/route.ts + MuleGalaxy.tsx ENTIRE (1070 — chunk reads)
R1 D3#1 (viewMode ALL FLAGGED == MULE ACCOUNTS collapse — verify bands render 6,783 watchlist), #4 (hover resurrecting hidden corridors w/ time-scrubber), #5 (₹250k threshold operators + recalibration vs max corridor ₹196,720.91), #6 (GalaxyLink lastDay typing), #14 (maxDistance vs camera far), #15 (trace-mode dimming). R2 D3#19/20 residuals (radiusCache staleness A22, zoomToFit overlay margins, pattern chips slice(12), O(all-links) hover scans, cold-start stampede ~113MB parse, meta.highRisk semantic conflation). Pan/zoom math, RAF/listener cleanup, devicePixelRatio fresh hunt.

## [ui:kit] ui/* + Sidebar + SidebarOverlay
R1 D1 items 5-25: skip-link focus, scroll lock + focus trap, closeDrawer/openDrawer stability, Column.width contract, StatCard trend sign, RiskBadge unknown-level + case-insensitivity + aria-label, PageHeader token sizes, FilterBar hasActiveFilters 'all' semantics + duplicate ids + arrow affordance, LoadingState import, Button lg-size inversion, reduced-motion block, Card/Skeleton cleanups, DataTable skeleton align + key collision. R2 D1#26-31 residuals (v2.4 version chaos theme I — propose canonical single source now?; twitter card image; drawer inert; sidebar 200px magic number ×2; page.tsx metadata overclaim). R3 a11y sweep (roles, keyboard nav DataTable, touch targets).

## [ui:css] globals.css + design-tokens.md + hardcoded-value sweep
R1 D1#1 (clamp() degenerate fixes — verify fluidity actually works: min < preferred < max), #2/#4 (font alias unification w/ next/font vars), #3 (.edge-* removal), #16 (design-tokens.md Charcoal/Ash/spacing corrected), #19 (.risk-dot removed + doc), #24 (focus-offset token). R2 #31 residuals (box-sizing dupe, scrollbar/::selection hardcode #333, chart-* utilities zero consumers — adjudicate delete). THEN Grep components for hex colors outside tokens. prefers-reduced-motion coverage complete? THEN ui_ux_flaws.md row-by-row: still-present => ui-ux finding; fixed-but-doc-says-open => stale-doc.
