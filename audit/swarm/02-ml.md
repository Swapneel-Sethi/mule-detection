# Domain: ML MODEL & INFERENCE

Files: xgboostPredictor.ts (355), transactionXgboost.ts (302), mlModel.ts (330), markovModel.ts (272), transactionScorer.ts (336), reportGenerator.ts (326); model JSONs in APP/public (model_weights.json 64KB, ml_params.json, transaction_feature_importances.json, transaction_model.json ~4MB); training/export scripts APP/scripts (train_transaction_model.py, train_meta_learner.py, recompute_ml_scores.py, recompute_transaction_scores.py, combine_ml_params.py, export_xgboost.py, convert_model.py).
Prior-run: MASTER DOMAIN 2 items 4-38 + reported-only 39-47. Themes C/E/F/G.

## [ml:pred] xgboostPredictor.ts ENTIRE
R1 items 7 (async path dead -> loadModel/computeMLScore zero callers), 8 (base_score log-odds alignment), 21 (bracketed base_score parse), 38 (public/ path). R2 #43 (relative fetch("/model_weights.json") unresolvable server-side; TTL stale-on-failed-refetch). R3 full hunt incl. duplicate MLFeatures interface via Grep.

## [ml:txn] transactionXgboost.ts + transactionScorer.ts ENTIRE
R1 items 4 (timezone skew getHours on UTC stamps), 5 (amount_ratio serving vs training formula), 9 (dead-model path), 16 (is_night off-by-one), 17 (missing-field defaults), 18 (FLAG_THRESHOLD derivation). R2 #46 (dead exports initTransactionModel/computeTransactionRisk). Verify FLAG_THRESHOLD scale consistency 0-1 vs 0-100 end-to-end.

## [ml:fallbacks] mlModel.ts + markovModel.ts + reportGenerator.ts ENTIRE
R1 items 13 (markov comment vs leaky matrix), 31-32 (STATE_THRESHOLDS unused entries; analyzeTemporalEvolution requiring is_mule; days_to_suspicious NaN typing; reportGenerator reading never-emitted automated_timing — feature was added, confirm now emitted or report still-broken). R2 #40 (markov single-observation dormancy), #41 (magic /25 /6 denominators; sentinel 999 leaking to UI). R3 hunt.

## [ml:jsons] model JSONs vs TS consumers
python-load model_weights.json + ml_params.json + transaction_feature_importances.json: field names/order/spelling vs what the TS parsers read; thresholds vs engine constants; R2 #44 ml_params platt STALE-vs-runtime (open item) + write-only status; #45 importances unconsumed + GAIN-vs-frequency mismatch. Confirm which artifacts are live contracts vs dead.

## [ml:exporters] export_xgboost.py + convert_model.py + public/transaction_model.json spot-check
R1 items 19-22 (xgboost 3.x crashes fixed? base_score None? nested JSON format? learning_rate config path?) and P0 DATA item (shipped transaction_model.json base_score=0.5 vs true intercept ~0.0801 => -2.44-logit bias until re-export — verify whether artifact was regenerated or still biased; flag-only if needs regeneration). H1 traversal <= vs strict < semantics. R3.

## [ml:training] train_*.py + recompute_*.py + combine_ml_params.py
R1 items 23-29 (CV account grouping, holdout-on-train, leakage handling 7944/7959, meta hardcoded false claims, min-max fit before CV split, platt fitted against wrong target, CWD paths -> __file__). R2 #36 (Nelder-Mead finite check). Verify feature lists in trainers EQUAL runtime xgboostPredictor FEATURES exactly (C6 regression risk after any fix). R3.
