# D2-3 [ML-XGB-TS] Files: mule-detection/src/lib/xgboostPredictor.ts, transactionXgboost.ts. Focus: leaf aggregation vs XGBoost dump convention (shrinkage/eta applied twice?), base_score handling in log-odds space, sigmoid base (0.5?), feature vector ORDER/names vs exporter order, async-load dead paths (cachedModel never populated), cache TTL/invalidate logic, NaN/missing-feature defaults vs training imputation.
(agent_id=agent-66)

 outcome="completed">Audit complete. Baseline cross-checked against `07-SWARM36-MASTER.md` (D2 items #6–#10 fixed, #42–#46 reported-only) and `02-ml-model.md`; every line of both slice files read; contracts verified against `public/model_weights.json`, `public/transaction_model.json`, `scripts/recompute_ml_scores.py`, `scripts/train_transaction_model.py`, consumers `detectionEngine.ts` / `transactionScorer.ts`, plus dataset sampling and `npx tsc --noEmit` (exit 0). Disproven along the way: `age_days` mapping and `from/to` remapping are handled upstream (`detectionEngine.ts:1429,1443-1444`) — no break there.

[P0|UNFIXED] mule-detection/public/transaction_model.json:6 — shipped `base_score: 0.5` still carries the pre-fix exporter's value, so serving omits the true intercept (≈0.0801 ⇒ logit −2.441) and every transaction score is biased +2.44 logits vs `predict_proba` — evidence: sampled artifact prints `'base_score': 0.5` while the fixed exporter (`scripts/train_transaction_model.py:234-261`) documents the real config value format `[8.0066666E-2]` and resolves it correctly; TS side (`transactionXgboost.ts:94-99,167`) now adds `logit(base_score)=0` correctly, confirming the defect is purely the unregenerated artifact (master P0, unchanged at HEAD) — fix: re-run `train_transaction_model.py` to regenerate, **and** re-derive `FLAG_THRESHOLD` (`transactionScorer.ts:67`, =0.3), which per `transactionXgboost.ts:157-161` was calibrated against this exact biased distribution.

[P2|NEW] mule-detection/scripts/recompute_ml_scores.py:85 — calibration reference adds `base_score` RAW into log-odds (`log_odds = model["base_score"]`), unlike both TS consumers which add `logit(base_score)` (`xgboostPredictor.ts:95-100,181`; `transactionXgboost.ts:94-99,167`) — same C2 bug class the swarm fixed in TS but missed here; benign only while the account artifact keeps `base_score: 0.0` (verified), but any re-export carrying a real probability prior makes this script diverge from serving by logit(p)−p logits, silently recomputing `ml_score`/`calibrated_score`/`risk_level`/`is_mule` for all 105,501 accounts and poisoning the `ML_SCORE_MIN/MAX` refit (its own documented purpose, :20-22) — fix: mirror `baseScoreLogOdds`: `log_odds = math.log(b/(1-b)) if 0 < b < 1 else b` with the same guards.

[P3|NEW] mule-detection/src/lib/xgboostPredictor.ts:36,39,276 + src/lib/transactionXgboost.ts:31,34,242 — `MODEL_CACHE_TTL_MS` can never expire: after the initial population via the sync kick (`if (!cachedModel) void loadModel()`), no path ever calls the loader again — the async wrappers `computeMLScore`/`computeTransactionRisk`/`initTransactionModel` have zero callers (repo-wide grep) and the sync guard is skipped once `cachedModel` is truthy — models are cached until process restart; inverse face: while `cachedModel` stays null after a failed fetch, every scoring call re-fires a full multi-MB JSON fetch (deduped only while in-flight) — superseded mechanism of known item #43 ("failed refetch") which is now unreachable — fix: fire-and-forget `void loadModel()` unconditionally, or delete the TTL constant and document cache-forever.

[P3|NEW] mule-detection/scripts/recompute_ml_scores.py:78-84 — NOTE claims leaf×learning_rate "intentionally DIVERGES from xgboostPredictor.ts (which sums post-eta dump leaves as-is)" — false since swarm fix D2-#6 aligned them (`xgboostPredictor.ts:171-178` now multiplies each leaf by `lr`) — a future reader following this note would "restore" the divergence and break the account-side calibration stack — evidence: quote of py :79-80 vs TS :162-170 comment stating deliberate parity — fix: rewrite the NOTE to state the two implementations are intentionally identical.

[P3|NEW] mule-detection/src/lib/xgboostPredictor.ts:347-354 + src/lib/transactionXgboost.ts:294-301 — `countSplitFeatures` recurses into `missing`, but the shipped artifacts serialize duplicated sibling subtrees under `missing` (value-identical copies at 446 nodes in `model_weights.json` and 29,963 in `transaction_model.json`, verified by dict equality) → split counts double-counted and `getFeatureImportances`/`getTransactionFeatureImportances` proportions skewed vs the "true split-based importance" docstring (`xgboostPredictor.ts:310-312`) — fix: recurse `left`/`right` only (matching the new exporter's deduplicated shape, `train_transaction_model.py:289`).

[P3|NEW] mule-detection/src/lib/xgboostPredictor.ts:135-136 + src/lib/transactionXgboost.ts:129-130 (+ recompute_ml_scores.py:68-69) — NaN routes to `node.missing ?? null`, terminating traversal with a silent 0 contribution; the fixed exporter sets `missing: None` whenever the default direction duplicates the yes branch (`train_transaction_model.py:289`), so post-regeneration a NaN feature would exit mid-tree instead of following the default child (real XGBoost semantics) — currently masked because the sync wrappers pre-filter non-finite inputs to the fallback and the shipped artifacts duplicate the branch — fix: `node = node.missing ?? node.left ?? node.right ?? null`.

[P3|NEW] mule-detection/src/lib/transactionScorer.ts:128-131 vs scripts/train_transaction_model.py:107-114 — train/serve coercion mismatch on falsy values: trainer coerces `0`/null→defaults via `or` (calibrated_score→0.3, risk_score→10), while serving `safeNum(v, fallback)` returns **0** for `0`/`null`/`false`/`""` (`Number(null)===0` is finite) and its fallback fires only on `NaN`/`undefined` — contradicts the "byte-for-byte equivalence" contract claim (`transactionScorer.ts:14-17`); verified harmless on current data (accounts_dataset.json: zero=null/0 count = 0 for both fields) but any future zero/null row flips sender/receiver risk features between train and serve — fix: align both sides on explicit null/empty handling (`safeNum` should return `fallback` for null/`""`, keeping numeric 0).

[P3|NEW] mule-detection/src/lib/xgboostPredictor.ts:210-214 — `buildFeatureVector` docstring still asserts "Unavailable features (kyc_status, account_type, authority_score) get conservative defaults — 1 for kyc_status (assumed verified)" — the function applies no defaults, and `detectionEngine.ts:1541-1555` now reads `kyc_status`/`account_type` from dataset records (swarm fix D2-#10 landed there, not here) — stale comment contradicts current wiring and documents behavior that would be a defect if reimplemented — fix: rewrite as pure ordering documentation.

[P3|NEW] mule-detection/src/lib/xgboostPredictor.ts:313 + src/lib/transactionXgboost.ts:275 — `getFeatureImportances` / `getTransactionFeatureImportances` are orphaned exports: zero callers under `src/` (repo-wide grep shows only definitions), so the split-count logic (incl. the double-count above) computes output nothing consumes — fix: delete or wire into a UI surface.

[P3|UNFIXED] mule-detection/src/lib/transactionScorer.ts:334 + src/lib/transactionXgboost.ts:267 — dead exports `initTransactionModel` / `computeTransactionRisk` (async wrapper) still present with zero callers — master D2 reported-only #46, unchanged at HEAD — fix: remove or call `initTransactionModel()` at an API-route startup boundary.

[P3|UNFIXED] mule-detection/src/lib/xgboostPredictor.ts:47 + src/lib/transactionXgboost.ts:41 — relative `fetch("/model_weights.json")` / `fetch("/transaction_model.json")` cannot resolve server-side (API/RSC contexts) → permanent silent fallback there — known reported-only #43, still present — fix: absolute URL from env base or `fs.readFile` of `public/` when `typeof window === "undefined"`.

[P3|UNFIXED] mule-detection/src/lib/xgboostPredictor.ts:137 + src/lib/transactionXgboost.ts:131 — traversal uses `val <= thresh` vs XGBoost's strict `<` — known reported-only #42 (matches Python refs incl. recompute_ml_scores.py:70; benign, boundary-rows only) — still present — fix optional: `<` with missing-default on equality, applied consistently across all three implementations.

[P3|UNFIXED] mule-detection/src/lib/xgboostPredictor.ts:82-85 + src/lib/transactionXgboost.ts:75-78 — `sigmoid` NaN policy implicit: `!Number.isFinite(x)` → `return x > 0 ? 1 : 0` sends NaN→0 by accident of comparison — known LOW from 02-ml-model.md, still present — fix: explicit `Number.isNaN(x) ? 0 : …` branch or comment.

[P3|UNFIXED] mule-detection/src/lib/xgboostPredictor.ts:315-324,336-344 — identical 7-element default-importances array duplicated verbatim twice in-file (transactionXgboost already extracted `DEFAULT_IMPORTANCES`) — known #43 partial, still present — fix: hoist one module const like the counterpart file.

[P3|UNFIXED] mule-detection/src/lib/xgboostPredictor.ts:278-289 + src/lib/transactionXgboost.ts:245-252 — silent fallback indistinguishable from model output: load failure OR any single non-finite feature wholesale swaps in `weightedFallback*` with only a console.warn, no source flag reaches downstream scores/UI (account-side fallback scale ≠ model+lr scale feeding `ML_SCORE_MIN/MAX`) — known MEDIUM reported-only from 02-ml-model.md, still present — fix: return `{score, source}` or expose a module-level `lastScoreSource`.

Verified-correct in-slice (no finding): eta/shrinkage parity is deliberately split and internally consistent (account TS ×lr ↔ recompute script ×lr ↔ ±0.1 leaf dumps; txn TS raw leaves ↔ trainer `predict_proba`, `train_transaction_model.py:300` trains with lr=0.1 and dumps post-shrinkage); `baseScoreLogOdds` conversion correct in both TS files; `buildFeatureVector` order matches `feature_names` exactly in both artifacts (16/16, string-keyed splits); `num_trees=200`/`num_features=16` headers accurate; loader dedup/validation sound; root `model_weights.json` stub copy no longer exists (02-report correction #1 resolved at HEAD); `tsc --noEmit` exit 0.

TOTALS sev=P0:1,P1:0,P2:1,P3:13 status=NEW:8,UNFIXED:7,REGRESSION:0