# D3-5 [VIZ-DATA-INTEGRITY] Machine-verify graph pipeline consistency (python sampling, no full prints): mule-galaxy API output vs sources public/accounts_dataset.json (92MB) + transactions_synthetic.json (17MB). Check: node count == account count?, edge endpoints all reference existing account ids, risk_score ranges within 0-100 everywhere, mule-flag counts consistent between artifact and API meta, generatedAt freshness, any score &gt;100 (×100 bug survivors), ID format consistency (ACC- prefixes etc.). Report measured numbers.
(agent_id=agent-74)

 outcome="completed">Audit complete. All measurements below were produced by replicating `mule-galaxy/route.ts`'s exact build logic in Python over the shipped artifacts (`accounts_dataset.json` 92.48 MB / 105,501 records; `transactions_synthetic.json` 17.97 MB / 99,952 records), plus targeted reads of consumers. `npx tsc --noEmit --incremental false` → exit 0.

**Simulated payload at current HEAD data:** `nodes=8,578, links=7,952, mules=1,962, highRisk=6,616, totalVolume=₹25,83,59,658.00, flaggedVolume=₹25,83,44,425.33`; score branches `{cal×100: 8578, cal>1: 0, fallback: 0}`; degree-0 nodes = 53.

---

[P2] [REGRESSION] src/app/api/data-local/route.ts:174-178 + src/components/DashboardContent.tsx:58-63 — master-report D5-fixed#2 ("Dashboard highRisk aligned to API's disjoint definition") is broken again: the two definitions are inverted at HEAD — data-local serves `highRisk = is_mule !== true && (critical||high)` which is **0 forever** on this artifact, while the Dashboard comment/fallback encode `isMule && !(critical||high)` claiming it is "Same disjoint tier rule as the API" — evidence: measured `(is_mule=False, risk_level∈{critical,high})` rows = **0** (all 1,962 critical/high accounts are mules; histogram `[(F,'low'):96923, (T,'medium'):6616, (T,'critical'):1943, (T,'high'):19]`); `useLocalData.ts:127` (`json.stats || …`) means the served 0 wins, so Dashboard's "High Risk" bar renders 0 while MuleGalaxy's "Watchlist" stat shows **6,616** for the identical population — fix: make route.ts:176 `a.is_mule === true && !(a.risk_level==="critical"||a.risk_level==="high")` (yielding 6,616, matching galaxy `meta.highRisk` and the Dashboard comment), or correct the Dashboard comment to the non-mule-severe rule.

[P1] [UNFIXED] src/app/api/graph/mule-galaxy/route.ts:49,77,162-163 — known A4/S10/#20 staleness class still present: module-level `cachedPayload` lives for the process lifetime, so `generatedAt` is frozen at first cold build and regenerated data is invisible until restart; cold start synchronously parses ~110 MB twice with no stampede guard — evidence: `let cachedPayload: GalaxyPayload | null = null` with no TTL/invalidate anywhere between lines 77-178; additionally `generatedAt` is a dead field (declared at `MuleGalaxy.tsx:44`, rendered nowhere) so clients cannot even see the staleness — fix: add TTL + stale-while-recompute (pattern already exists in analytics route), or precompute the payload at build time; delete or render `generatedAt`.

[P2] [UNFIXED] src/components/MuleGalaxy.tsx:87-96 — known A22: `radiusCache` is module-scoped and survives snapshot reloads, so radii computed from an older payload persist after refetch (only bounded by the size>20000 clear) — evidence: unchanged code block `const radiusCache = new Map<string,number>()` outside the component — fix: key cache by `snapshot.generatedAt` or reset it in the fetch effect.

[P3] [UNFIXED] src/app/api/graph/mule-galaxy/route.ts:136-138,158-160,169 — known #20 remainder verified still true on current data: `calibrated > 1` branch dead (0/8,578 accounts hit it; calibrated_score max 0.856, every node takes ×100), `validLinks` filter drops 0 links (all 7,952 endpoints resolve by construction), and `meta.totalVolume` has zero consumers (grep: declared at route.ts:42/MuleGalaxy.tsx:50, read nowhere) — evidence: simulation branch counts `{cal>1:0, cal*100:8578}` and `validLinks==links:True` — fix: collapse the score heuristic to the live branch, drop the redundant filter, drop or consume `totalVolume`.

[P3] [UNFIXED] src/app/api/graph/mule-galaxy/route.ts:127-156 — known #27: 53 flagged accounts have degree 0 and render as isolated dots — evidence: re-measured at HEAD: exactly **53** nodes with degree 0 out of 8,578 (corridor predicate keeps only both-endpoints-flagged pairs; 92,000 of 99,952 txns touch ≥1 unflagged endpoint) — fix: document or filter degree-0 nodes from the default view.

[P3] [UNFIXED] public/transactions_synthetic.json — known D4#29 mixed riskScore scale survives: 7,469 rows ≤1.0 (min 0.10) vs 92,483 rows on 0–100 (max exactly 100.0); no value >100 anywhere (zero ×100 survivors) — evidence: measured `txn.riskScore n=99952 min=0.10 max=100.00 >100:0 (0,1]:7469`; no viz impact today because the galaxy route never reads txn riskScore — fix: regenerate or normalize at the two consumer sites that do read it.

[P3] [NEW] src/app/api/graph/mule-galaxy/route.ts:31-32,103-105 — link field named `lastDay` actually stores the **earliest** activity day of the corridor (docstring itself says "Earliest activity day"); consumer (`MuleGalaxy.tsx:217-231`) correctly treats it as corridor start for the scrubber, but the identifier invites a wrong-consumer bug — evidence: aggregation keeps `min(day)` (`if (day && (!existing.lastDay || day < existing.lastDay))`), measured range 2026-01-01..2026-06-29 — fix: rename to `firstDay` in payload + `GalaxyApiLink` (only consumer is MuleGalaxy.tsx:35).

[P3] [NEW] src/app/api/graph/mule-galaxy/route.ts:98 — corridor days bucketed by the **UTC** date part of `.000Z` stamps while the app's sibling time charts bucket IST (known A12 policy split), so a corridor lights up on a different calendar day than an IST business day — evidence: sample stamp `2026-04-11T20:18:05.000Z` = 2026-04-12 01:48 IST; suffix census 99,952/99,952 `.000Z`; impact confined to scrubber granularity — fix: derive day via `Asia/Kolkata` offset if IST convention is canonical, or document UTC as the scrubber's timeline.

[P3] [NEW] public/accounts_dataset.json — HITS scores contain negative epsilon rows violating non-negativity: `hub_score = -1.02e-26` (ACC000175DA), `authority_score = -1.51e-22` (ACC00000761) — float residue of normalization; harmless to galaxy (fields unread there) but breaks any future `>= 0` invariant — evidence: full-scan found exactly 1 hub row and ≥1 authority row `< 0` — fix: clamp negatives to 0 in `generate_dataset_json.py` export.

TOTALS sev=P0:0,P1:1,P2:2,P3:6 status=NEW:3,UNFIXED:5,REGRESSION:1

CLEAN (verified-clean aspects of the slice, with measurements): `src/app/api/graph/mule-galaxy/route.ts` pipeline math, `public/accounts_dataset.json`, `public/transactions_synthetic.json` — node universe = flagged predicate exactly (nodes 8,578 = is_mule∨critical/high; ≠ 105,501 total accounts **by design**, and page copy is honest: "ML-flagged accounts", MuleGalaxy.tsx:57-59,831); edge integrity 100% (99,952/99,952 txn endpoints resolve against all 105,501 account ids; 7,952 galaxy-link endpoints ⊆ node ids; 0 self-loops; 0 blank/duplicate account ids; MAX_LINKS cap not approached, 7,952 < 40,000); score ranges all within bounds and zero >100 survivors (risk_score 5.3–85.6, calibrated 0.053–0.856, ml_score 0.262–0.466, graph_score 0–5, txn riskScore ≤ 100.0, clamped node.score 56.0–85.6); mule-flag counts internally consistent (artifact is_mule 8,578 = nodes; partition Active-Mules 1,962 + Watchlist 6,616 = 8,578; flagged-corridors 7,941 vs unflagged 11, ₹15,232.71 gap explains Flagged-Volume ≈ total volume); timestamps 99,952/99,952 ISO-valid calendar dates spanning 180 distinct days 2026-01-01..2026-06-29 (no month-13, no inversions); ID formats consistent per scheme (accounts `ACC*` 98,500 + legacy `ACM*` 7,001 — the ACM batch is exactly the 7,001 rows missing `account_age_days`, tying into known D4#23; txns `TXN*` 99,952; no remaining `startsWith("ACC"/"ACM")` heuristics anywhere in src); stale graph artifacts (`network_graph`/`bipartite*`/`hypergraph*.json`) absent from `public/`; 18 of 7,959 flagged txns fall outside both-flagged corridors per the documented predicate (by design, noted for completeness).