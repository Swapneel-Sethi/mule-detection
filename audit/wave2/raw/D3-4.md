# D3-4 [VIZ-SANKEY] Files: mule-detection/src/components/SankeyChart.tsx, plotly/PlotlySankey.tsx, types/plotly-partial.d.ts, scripts/visualize_sankey.py. Focus: CSS var() strings passed as SVG paint, node/link value units (Cr vs raw ₹), circular-flow amount semantics (sum of ALL edges vs cycling amount), dedup key destroying orientation, Plotly import weight/lazy-loading, resize observers cleanup.
(agent_id=agent-73)

 outcome="completed">All evidence gathered — typecheck green, contract chain traced end-to-end. Final report:

---

## LINE-BY-LINE DEFECT AUDIT — D3-4 [VIZ-SANKEY]

Baseline checked: master §Domain 3 items 2/10/11/12/17 (fixed), H1, M3, M4, reported-only #21/#26, register A11/A13. All previously-fixed Sankey fixes verified intact at HEAD (`pointNumber` click at `SankeyChart.tsx:203`, hex colors, unrounded `amount/1e5` at :176, sub-₹1K `formatINR` at :43, `aria-pressed` at :232, modebar off at :287, factory bundle in `PlotlySankey.tsx`). No REGRESSIONs found.

**[P2] NEW** mule-detection/src/app/api/analytics/route.ts:323-326 — Sankey-only heuristic rebrands pattern-less flows as PASSTHROUGH when source is critical/high — branch exists nowhere else; contradicts own comments claiming shared attribution: route.ts:8-9 *"so both views always agree on how a transaction is attributed"* and :317 *"Same canonical mapping as txnByPattern"*, while `txnByPattern` (route.ts:124-140) has no such branch — clicking the Sankey's PASSTHROUGH chip filters page-wide charts (AnalyticsContent.tsx:175-177, 201) whose PASSTHROUGH totals were computed WITHOUT promotion ⇒ same-named buckets show different money on one screen — delete the promotion branch (or apply identically in `txnByPattern` and fix the comments).

**[P2] UNFIXED** mule-detection/src/app/api/analytics/route.ts:292,296-300 (consumer AnalyticsContent.tsx:522) — circular-path dedup key `[from,via,mid].sort().join("->")` destroys orientation (A→B→C and C→B→A collapse, first-encountered wins) AND displayed amount = sum of LIFETIME volumes of all three edges (`amountByEdge` accumulates every historical txn, route.ts:282), not the cycling amount — register A13 recorded this pre-swarm; code + justifying comment *"Sum all three legs…"* unchanged at HEAD — anchor the key directionally (`${from}->${via}->${mid}`) and sum only the specific cycling transactions' amounts.

**[P2] UNFIXED** mule-detection/src/app/api/analytics/route.ts:328-330,341 (consumers SankeyChart.tsx:71-72,104-127) — sankey flow endpoints keyed on `account_id.slice(-6)` merge distinct accounts sharing a 6-char suffix (register A11: multiple colliding groups) — SankeyChart then renders merged ghosts as single nodes/hover totals with no way to tell — use full ids in the API key/labels (truncate only at render).

**[P3] NEW** mule-detection/src/app/api/analytics/route.ts:347 (consumers SankeyChart.tsx:84-87,111,126; AnalyticsContent.tsx:200-214) — flows silently truncated to top-20 per pattern before the UI, yet downstream presents sample sums as authoritative: filter chip reads "*N flows · N accounts · ₹total*", "Other · N accts" nodes count only sampled suffixes, and node hovers show partial totals with no "top flows" qualifier — label the chip/nodes as top-20 samples or ship unsampled aggregates.

**[P3] NEW** mule-detection/src/components/SankeyChart.tsx:116,260-261 — pattern-node hover duplicates the amount twice: label already embeds `FANIN · ₹X L` and `hovertemplate` appends `<b>%{customdata}</b>` carrying the identical `formatINR(patternTotals)` string — drop the total from the label (or render plain customdata for pattern nodes).

**[P3] NEW** mule-detection/src/components/SankeyChart.tsx:74-75,140,145 — link-aggregation keys `${from}|${pattern}` / `${pattern}|${to}` parsed back with naive `split("|")`; an id containing `|` silently swaps fields (same separator-fragility family flagged for galaxy/analytics keys, different file) — encode with a control separator (`\u0000`) or store tuple keys in a Map-of-Maps.

**[P3] UNFIXED** mule-detection/src/components/SankeyChart.tsx:228-245 — legend chips remain clickable with no disabled/guarded state when `onPatternSelect` is undefined (optional-chain no-op); component is reusable and the prop is optional — disable buttons when the callback is absent (reported-only item #21, still present).

**[P3] UNFIXED** mule-detection/src/components/plotly/PlotlySankey.tsx:6-9 — docstring cites "~3.5 MB min / 4.65 MB chunk measured … cutting the client payload by ~4 MB"; figures unverifiable in-repo (no measurement artifact/build output) — measure or soften to qualitative claim (reported-only item #21, still present).

**[P3] NEW** mule-detection/scripts/visualize_sankey.py:31-36,43,74 — "CIRCULAR" demo builds linear chains `LOOP_A→LOOP_B→LOOP_C→LOOP_EXIT` that never loop back (production CIRCULAR requires a real closing edge, route.ts:290-291), `LOOP_EXIT` gets mule-red via `"LOOP" in n` despite being the cash-out beneficiary, and the title duplicates the production chart title verbatim while plotting fabricated `MULE_*`/`VICTIM_*` data — misrepresents the pattern it illustrates; rename nodes to reflect a pass-through-to-exit or close the cycle (`loop[0]` as `mid→from` target).

Focus areas with no defects found: CSS-var-as-paint (all resolved hex/rgba), value units (internal lakhs, all user-visible strings via `formatINR`), lazy-loading (`react-plotly.js@4.1.0` exports-map `./factory` ships `dist/factory.d.mts` — valid; `plotly.js/lib/{core,sankey}` exist; `npx tsc --noEmit` exit 0), ResizeObserver cleanup (none manual; `useResizeHandler` + factory purge cover lifecycle), hydration (`ssr:false` dynamic).

TOTALS sev=P0:0,P1:0,P2:3,P3:6 status=NEW:5,UNFIXED:4,REGRESSION:0
CLEAN: mule-detection/src/types/plotly-partial.d.ts