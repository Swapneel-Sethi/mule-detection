# D2-4 [ML-SUPPORT] Files: mule-detection/src/lib/mlModel.ts, transactionScorer.ts, markovModel.ts, reportGenerator.ts. Focus: hand-authored fallback tree agreement with real model outputs, 16-feature vector parity with training script, getHours() local-vs-UTC skew, is_night boundary, amount_ratio formula vs training, report numbers formatting/units, Markov transition matrix row sums.
(agent_id=agent-67)

 outcome="completed">Audit complete. All four slice files read line-by-line, cross-checked against the trainer (`scripts/train_transaction_model.py`), the shipped artifacts (`public/transaction_model.json`, `accounts_dataset.json`, `transactions_synthetic.json` sampled programmatically), and counterpart consumers (`detectionEngine.ts`, `transactionXgboost.ts`). Baseline known-items from `07-SWARM36-MASTER.md` + digest verified at HEAD.

---

**[P0] [UNFIXED]** `mule-detection/public/transaction_model.json` (consumed via `transactionScorer.ts:67`, `transactionXgboost.ts:94-99`) — shipped artifact still carries the wrong intercept: `base_score: 0.5` vs true training intercept ≈0.0801, so after the C2 logit fix serving adds `logit(0.5)=0` where the real model adds ≈−2.44 nats to every margin — evidence: my inspect of the JSON → `base_score: 0.5, num_trees: 200, num_features: 16`; trainer `resolve_base_score()` docstring cites authoritative config value `[8.0066666E-2]`; master P0-DATA item, deliberately not regenerated per digest:245 because `FLAG_THRESHOLD=0.3` was calibrated against this broken output distribution — re-export with fixed exporter AND re-derive threshold in the same release (`audit/mltest/txn_threshold_probe.ts`).

**[P1] [UNFIXED]** `mule-detection/src/lib/detectionEngine.ts:1720` (+ `transactionScorer.ts:305-320`) — `runDetection` passes **un-normalized** `rawAccounts` into `scoreAllTransactions`, whose map keys on `acc.id`: 0/105,501 dataset rows have an `id` key (verified), so all entries collapse onto key `undefined`, every sender/receiver lookup misses, and **every transaction is silently scored against the unknown-account stub** (calibrated 0.3, risk 10→0.1, hub 0, velocity 0, total_in 0) instead of its real accounts — evidence: `const transactionScores = scoreAllTransactions(validTransactions, rawAccounts);` vs the normalization pass at :1422-1431 that built `normalizedAccounts` for exactly this alias reason; dataset sample `with id key: 0` — pass `normalizedAccounts` (this is the surviving half of known items master-#11 / digest:205).

**[P1] [UNFIXED]** `mule-detection/src/lib/detectionEngine.ts:665,897,912,918,924` — account-side temporal features still use host-local `getHours()`/`getDay()` on UTC `Z` timestamps (`hour_distribution_entropy`, `weekend_ratio`, `night_txn_ratio`, `business_hours_ratio`, night-owl pattern window) — the exact skew class fixed as P1 in `transactionScorer.ts:155-156` (master #4) but left behind in the feature extractor feeding BOOSTING_TREES Tree 3 (`night_txn_ratio>0.3`, mlModel.ts:89), `interactionScore` (mlModel.ts:310), red flags and reasons — evidence: `const hour = new Date(t.timestamp).getHours(); return hour >= 0 && hour < 5;`; previously reported-only (master #41 P3-bucket) but it is the identical twin of a fixed P1 — switch to `getUTCHours()`/`getUTCDay()`.

**[P2] [NEW]** `mule-detection/src/lib/detectionEngine.ts:666,919` vs `mule-detection/src/lib/transactionScorer.ts:158` + `reportGenerator.ts:149` — two irreconcilable "night" definitions ship together: trained-model convention `is_night = hour < 6` (trainer :136, scorer copy "00:00–06:00" :229) vs engine behavioral features `< 5` with copy "(00:00-05:00)" (engine :1355, reportGenerator :149) — hour-5 activity is "day" for account scoring but "night" for transaction scoring; the swarm's boundary fix propagated to only one side — evidence: quoted lines above — align engine windows to `hour < 6` (UTC) and update both copy strings.

**[P2] [NEW]** `mule-detection/src/lib/reportGenerator.ts:289-296` — the "CRITICAL FIX" severity propagation is dead code: comment claims "Each pattern carries its own severity from the detection engine", but all 9 `red_flags.push` sites in `generateExplanation` (detectionEngine.ts:1332-1393) emit no `severity`, so `"severity" in rf` is always false and every red flag inherits the account-level `riskLevel` (a medium account's minor fan-in flag reads "medium"; everything on a critical account reads "critical"); the fallback chain also double-casts unvalidated strings into the severity union — evidence: grep shows zero `severity` inside any `red_flags.push` object — either attach per-pattern severity in `generateExplanation` (patterns already carry it, :263-767) or drop the dead branch and fix the comment.

**[P2] [UNFIXED]** `mule-detection/src/lib/mlModel.ts:174,177,186-196` — hand-authored fallback still near-constant [0.073, 0.111] (bias −2.5 + max Σleaf×lr +0.42 nats), which after detectionEngine's [0.262, 0.466] normalization window (:1563-1567) clamps to 0 — the entire fallback ML component contributes a constant zero — evidence: constants unchanged at HEAD; max leaf sum 4.2×0.1; known reported-only (digest:123) — needs hand-trees recalibrated against real score distributions or explicit dead-labeling.

**[P3] [NEW]** `mule-detection/src/lib/reportGenerator.ts:173-176` — community summary prints `${(commScore*100).toFixed(0)}% internal density`, but `community_score` is the composite `min(1, density*2 + fastFlowRatio)` (detectionEngine.ts:565): density is pre-doubled and fast-flow share is not density at all — e.g. density 0.30/fast 0.40 renders "100% internal density" — evidence: formula cited — relabel copy ("community risk index") or decompose the components.

**[P3] [NEW]** `mule-detection/src/lib/reportGenerator.ts:188-194` — freeze recommendation reason hardcodes "Critical risk score (…)" whenever `riskLevel === "critical" || isMule`, but `is_mule` fires at calibrated ≥0.551 which includes the *medium* band (0.551–0.66, detectionEngine.ts:1594,:1603) — analyst report says Critical while its own `risk_level` field says "medium" — evidence: band constants cited — word the reason from `riskLevel` ("…score with N red flags") instead of asserting "Critical".

**[P3] [NEW]** `mule-detection/src/lib/detectionEngine.ts:1653-1659` (echoed into `AnalystReport.mule_type`, reportGenerator.ts:287) — muleType ternary ends `: prScore > 0.2 ? "network_mule" : "pass_through"`, duplicating the `is_pass_through` branch: a mule that is none of distributor/aggregator/pass-through/network is falsely typed `"pass_through"` — evidence: literal appears twice in the chain — final fallback should be a distinct label ("generic"/"unknown").

**[P3] [NEW]** `mule-detection/src/lib/transactionScorer.ts:231-233` — risk-factor copy "Amount is X% of sender's total inflow" prints `amount_ratio = amount/(total_in+1)` as a percentage: for the 23,312 zero-inflow accounts (verified count) ratio = amount, producing nonsense like "894% of sender's total inflow", and for all senders it overstates by the ignored +1 denominator — evidence: formula :167 vs copy :232 — cap/reword (e.g. "amount ≈ Nx sender's recorded inflow" or suppress when `total_in === 0`).

**[P3] [NEW]** `mule-detection/scripts/train_transaction_model.py:107-114` vs `mule-detection/src/lib/transactionScorer.ts:128-131,172-175` — falsy-coercion parity gap: trainer maps present-but-falsy values to defaults (`float(x.get(k, d) or d)` → calibrated 0/null/"" ⇒ 0.3, risk_score 0 ⇒ 10), while TS `safeNum(v, d)` keeps them (`Number(null)=0` is finite) — latent only today (verified: 0 missing/null/zero rows for both fields in `accounts_dataset.json`) but any future producer writing nulls/zeros silently diverges from training semantics — mirror Python's falsy-coercion in `safeNum` call sites or drop `or d` in the trainer.

**[P3] [UNFIXED]** `mule-detection/src/lib/mlModel.ts:230-232` — stale band cross-reference in `calibrateScore` docblock: "Risk bands medium/high/critical at 0.551/0.640/0.671 are likewise untouched" vs live HEAD bands 0.551/**0.66/0.71** (detectionEngine.ts:1601-1603, ITER-2 retune) — registered mid-swarm (digest:161, owner = this file), never corrected — update docblock to 0.551/0.66/0.71.

**[P3] [UNFIXED]** `mule-detection/src/lib/markovModel.ts:72` — `STATE_THRESHOLDS.mule_min_risk = 0.55` vs engine's `is_mule` cut 0.551 (detectionEngine.ts:1594): an account at calibrated 0.5505 is `is_mule=false` yet classified `confirmed_mule` here (audit02 LOW, never reconciled) — import one shared threshold constant.

**[P3] [UNFIXED]** `mule-detection/src/lib/markovModel.ts:180-185` — recent/early trend windows are identical slices when <10 observations, forcing "stable" regardless of monotone escalation (known reported-only, digest:158) — use disjoint halves below 10 obs.

**[P3] [UNFIXED]** `mule-detection/src/lib/detectionEngine.ts:1641-1645` → `markovModel.ts:110-126` — `analyzeTemporalEvolution` is still fed a single synthetic observation (`[{ timestamp: new Date(), … }]`), so states/trend are always "Single observation…stable" and the transition matrix contributes nothing (master #40 reported-only) — feed real per-day history or hide the section.

**[P3] [UNFIXED]** `mule-detection/src/lib/mlModel.ts:255`, `markovModel.ts:226,239`, `transactionScorer.ts:334` (+ `transactionXgboost.ts:267`) — dead exports with zero callers, verified by repo-wide grep: `computeECE`, `predictFutureState`, `transitionAnomalyScore`, `initTransactionModel`, async `computeTransactionRisk` (all previously reported-only, kept as "API surface") — wire or delete.

**[P3] [UNFIXED]** `mule-detection/src/lib/mlModel.ts:2-13` — header claims these trees are what runs "when the real XGBoost model … is unavailable or fails to load", but a model-load failure resolves inside `xgboostPredictor`'s own `weightedFallbackScore`; the hand trees only fire if `computeMLScoreSync` throws (detectionEngine.ts:1557-1560 catch) (known full-sweep LOW·ML) — restate the actual trigger condition in the header.

---

TOTALS sev=P0:1,P1:2,P2:3,P3:11 status=NEW:7,UNFIXED:10,REGRESSION:0
CLEAN: none — all four slice files carry at least one finding (`mlModel.ts`, `transactionScorer.ts`, `markovModel.ts`, `reportGenerator.ts`); the heaviest defects sit at the slice boundaries (`rawAccounts` id-key miss at the scorer entry, local-TZ/boundary drift in the engine that feeds the hand-authored trees and report narratives).