# D4-3 [DATA-REBUILD] Files: mule-detection/scripts/rebuild_full.py, build_real_mules.py. Focus: orchestration order dependencies, partial-write/truncation hazards (atomicity), CSV parsing assumptions (header names, quoting), mule-selection criteria vs UI copy ('8578 flagged' style numbers), memory blowups, hard-coded paths that break on other machines.
(agent_id=agent-78)

 outcome="completed">All evidence gathered — cross-checks complete (flag vocab consumed correctly by `canonicalFlag`, no repeated senders for any ACM receiver, banks vocabulary exact-match, alert refs 0 orphans). Final report:

---

## D4-3 [DATA-REBUILD] Defect Audit — `scripts/rebuild_full.py`, `scripts/build_real_mules.py`

Baseline: master #28 (reported-only: "dangerous stale regenerator"; dead `sum(1 for _ in ())`) — note the `sum(1 for _ in ())` debris is GONE at HEAD (removed in swarm commit `c1c86e4`, which also fixed the `in_txn_count=in+out` parenthetical: line 189/190 now use `txn_in`/`txn_out` correctly). Findings below are everything else found by full line review plus machine verification against the 993k-row CSV and the shipped 105,501-row artifact.

**[P1] [NEW] mule-detection/scripts/build_real_mules.py:80,158-164** — mule-selection criterion (`pattern-involved receivers` only) guarantees 2,053 orphaned transaction endpoints if rerun, recreating the exact epidemic its own docstring (lines 4-9) claims to fix — evidence: measured distinct ACM ids in `transactions_synthetic.json`/CSV = **6,961**, pattern-involved receivers = **4,908**, so rerun writes 4,908 mule rows and drops the other 2,053 referenced ACM ids (dataset shrinks 105,501 → 103,408 with dangling `from`/`to` endpoints; also discards the 40-zombie cleanup benefit by deleting rows wholesale) — fix: select `real_mules ∪ (ACM ids in txn universe)` and emit non-patterned ACMs as plain user rows (as `rebuild_full.py` does), or gate the write on `acm_in_txns - real_mules == ∅`.

**[P1] [UNFIXED] mule-detection/scripts/rebuild_full.py:177-217 + build_real_mules.py:115-155** — emitted ACM schema omits `account_age_days` (feature #0 of the account XGBoost), so every rebuilt mule row scores on the imputed default; known D4#23 still present at HEAD — evidence: **7,001/7,001** shipped ACM rows lack the key (key-shape census: 1 shape, no `account_age_days`), while `recompute_ml_scores.py:115` imputes `_num(acc.get("account_age_days", 365))`; commit `c1c86e4` added the field *only* to the fallback ACC branch (`rebuild_full.py:267`) — fix: compute `(last_ts-first_ts or today-first_ts).days` in `make_acm_row`/`make_mule_row` and emit it.

**[P2] [UNFIXED] mule-detection/scripts/rebuild_full.py:144-217 ↔ build_real_mules.py:90-155** — two sibling "rebuilders" write the same artifact with divergent, mutually exclusive math and clobber real model outputs with placeholders; known #28 "dangerous stale regenerator" residual, now with sharper evidence — evidence: score formulas differ (`55+min(28,deg*1.5)+10@if≥2pats` vs `40+min(35,deg*2)+{15|10|5}+5@if ratio>0.9`); ratio>0.9 score bonus exists only in rebuild_full while the pass-through *flag* uses >0.8 in both; bank `"Unknown"` vs partner-vote; shipped artifact carries real model outputs (ACM `ml_score` 0.388–0.466, `calibrated_score` 0.56–0.856, `behavioral_score` 47.0 ≠ `risk_score` 61.2 — independently computed) which a rerun replaces with `risk/100` placeholders (`rebuild_full.py:212-213`, `build_real_mules.py:150-151`) and `behavioral_score = score`; the documented remediation target (`recompute_ml_scores.py`, per comment `rebuild_full.py:211`) is itself CWD-fragile (its lines 9-18) and rewrites `is_mule` globally (line 170), so the final flagged population depends on undocumented script order — fix: delete one script, extract shared scoring into one module, and refuse to write unless `recompute_ml_scores.py` will run (or emit placeholders behind an explicit `--placeholders` flag).

**[P2] [NEW] mule-detection/scripts/rebuild_full.py:198 + build_real_mules.py:136** — `txn_velocity_per_day := degree/365` contradicts the dataset-wide convention `totalTransactions/180`, silently rescaling the mule velocity feature ÷1.97 vs all 98,500 ACC rows on rerun — evidence: 5,000-row random probe → **0/5,000** match `deg/365`, **0/5,000** mismatch `tt/180`; concrete pairs: ACC00012E05 tt=10→vel 0.0556 (=10/180), ACM000826FF75 tt=1→0.0056 (=1/180); feature feeds `xgboostPredictor.ts` (`f.txn_velocity_per_day`) — fix: `round((n_txn_in+n_txn_out)/180, 4)` in both scripts.

**[P2] [NEW] mule-detection/scripts/rebuild_full.py:123-134** — `assign_bank` majority vote is nondeterministic across runs: `partner_banks` is built by iterating a `set` (salted order), and `Counter.most_common(1)` breaks count-ties by insertion order, so tied votes pick process-dependent banks — evidence: `for p in partners` (line 126) over `set[str]` + `Counter(partner_banks).most_common(1)[0][0]` (129-131); the crc32 fallback (133) is deterministic but only reached when there are *no* partners; violates the repo's regeneration-stability convention (cf. domain-4 H4, rated HIGH for the same class) and makes the 92MB artifact byte-unstable — fix: break ties with `zlib.crc32(acm_id)`, e.g. `max(sorted(set(partner_banks)), key=lambda b: (partner_banks.count(b), -crc32(b)))` or count into a dict keyed by bank then tie-break on crc32(acm_id).

**[P3] [NEW] mule-detection/scripts/rebuild_full.py:291-292 + build_real_mules.py:163-164** — non-atomic in-place overwrite of the served 92MB artifact (`open(path,"w")` + `json.dump`, no temp-file + `os.replace`, no backup); a crash/Ctrl-C mid-dump leaves a truncated JSON — evidence: direct `open(...,"w")` on `ACCOUNTS_PATH` in both; mitigated only by the file being git-tracked (uncommitted recompute outputs would still be lost) — fix: dump to `path + ".tmp"` then `os.replace`.

**[P3] [NEW] mule-detection/src/components/AccountsContent.tsx:47 ← scripts/build_real_mules.py:80** — UI copy pins "the 8,578-account 'All Flagged' view" to a population neither rebuild script preserves: build_real_mules rerun yields 1,577 ACC-mules + 4,908 = 6,485 pre-recompute, and post-`recompute_ml_scores.py` the count is whatever `calibrated ≥ 0.551` produces — evidence: measured `is_mule` = 8,578 (= 1,577 ACC + 7,001 ACM) vs selection math above — fix: derive such copy from the API meta count, or assert the expected flagged count in the generator.

**[P3] [UNFIXED] mule-detection/scripts/rebuild_full.py:207 + build_real_mules.py:145** — `balance = tin − tout` perpetuates the negative-net-flow mislabeled-identity defect (known D4#24/M4) in both generators — evidence: formula verbatim in both `make_*_row`s; 89,803/105,501 shipped rows negative — fix: rename to `net_flow` or model opening balance floored at 0.

**[P3] [UNFIXED] mule-detection/scripts/build_real_mules.py:118-119** — hardwires `bank:"Unknown"`/`city:"Unknown"` for every emitted mule row; known L3 (city) still present, and the bank part now contradicts shipped state ("all banks assigned", commit `c9c86eb`) — evidence: lines verbatim; shipped ACM rows have real banks (Canara 735, PNB 727, …), 7,001/7,001 `city=="Unknown"` — fix: port `assign_bank` from rebuild_full and hash-derive city.

**[P3] [UNFIXED] mule-detection/scripts/rebuild_full.py:220-226** — zombie preservation: only ACMs present in the txn-JSON universe get rebuilt, leaving the 40 zero-txn ACM rows (empty `firstSeen`, stale schema) untouched; known M8/M3 residue — evidence: measured 7,001 shipped ACM − 6,961 covered = **40** empty-`firstSeen` rows at HEAD — fix: prune `existing_acm_ids - acm_in_txns` (with a printed count) instead of skipping.

**[P3] [NEW] mule-detection/scripts/rebuild_full.py:167,174** — dead clamp and unreachable tier: `base_score = 40 + min(35, …)` makes `max(30.0, …)` a no-op and the `"low"` risk_level branch unreachable — evidence: minimum possible score is 40.0 (degree 0, no patterns); line 167 `max(30.0, …)` — fix: drop the `max()` and the `"low"` ternary arm, or lower `base_score` if sub-40 mules are intended.

**[P3] [NEW] mule-detection/scripts/rebuild_full.py:4-9 vs 310-316** — docstring overclaims: "4. Alerts account references validated" (references are only *counted* and printed, never validated/fixed) and "5. Zero orphans guaranteed" (nothing enforces it); "not all 83" rationale is stale — evidence: docstring lines 4-9 vs print-only block 310-316 — fix: reword to "reports orphan counts" or implement a `--fix-alerts` pass.

**[P3] [NEW] mule-detection/scripts/rebuild_full.py:298-316** — integrity violations are print-only and the script always exits 0, so it can't gate CI/regeneration — evidence: `print(f"Orphaned … {len(orphaned_txn)}")` with no `sys.exit(1)` — fix: `sys.exit(1)` if `orphaned_txn or orphaned_alert` non-empty.

**[P3] [NEW] mule-detection/scripts/rebuild_full.py:19 + build_real_mules.py:28** — hard-coded browser-download filename `"transactions_1m (1) (1).csv"` (spaces/parens, single copy, no friendly existence check) breaks on any rename/move — evidence: `os.path.join(BASE, "transactions_1m (1) (1).csv")`; file exists today (73MB) but a bare `FileNotFoundError` surfaces if not — fix: glob `transactions_1m*.csv` or accept `--csv` argv override with a clear error message.

**[P3] [NEW] mule-detection/scripts/build_real_mules.py:171 + rebuild_full.py:338** — `min()`/`max()` on the mule-score list crash with a bare `ValueError` if selection ever yields zero rows (pattern-free CSV) — evidence: `scores = [r["risk_score"] for r in mule_rows]` then `min(scores)` unguarded; same for `acm_scores` at rebuild_full:335-338 — fix: guard with `if scores:` before the range print.

### Checks run and cleared (no defect)
CSV contract (header `txn_id,sender_id,receiver_id,amount,timestamp,mode,is_fraud_pattern` exact match; 0 blank cells; 0 unparseable amounts; 100% uniform ISO timestamps → lexicographic first/last-ts min/max and `[:10]` slicing valid); pattern vocabulary (`FANIN/PASSTHROUGH/FANOUT/CIRCULAR` exactly matches `PATTERN_FLAGS`; emitted flags `fanin_receiver/passthrough/circular_loop/fanout_source/pass_through` all recognized by `api/analytics/route.ts:11-14 canonicalFlag`); `KNOWN_BANKS` exactly equals the dataset's 10-bank set; `avg_in/out_amount` per-unique-sender formula coincides with the per-txn convention for every ACM row (0/4,908 have repeated senders); txn-JSON ACM ids (6,961) ⊆ CSV ids; alert→account refs 0/155 orphans; memory bounded (≤ ~993k set memberships); no timezone arithmetic; `BASE` is `__file__`-anchored (no CWD-relative paths in these two files).

TOTALS sev=P0:0,P1:2,P2:3,P3:10 status=NEW:10,UNFIXED:5,REGRESSION:0
CLEAN: none — both files carry findings (verified-clear aspects enumerated above)