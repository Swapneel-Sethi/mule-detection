# D5-5 [API-NORMALIZER] Files: mule-detection/src/lib/normalizers.ts, src/app/api/data-local/route.ts. Focus: ?? fallbacks dead because safeNum returns 0, nonNeg clamping hiding corrupt data, module cache no TTL, sort/param whitelisting (injection via dynamic property access), alert resolved double-count, silent slice(0,500), turnover definition (which one? Cr conversion math), flagged-filter predicate vs analytics predicate divergence.
(agent_id=agent-86)

 outcome="completed">Audit complete. All focus areas verified against dataset ground truth (python sampling of all three JSONs), cross-file contract checks, git diff classification, and `npx tsc --noEmit` (exit 0).

**Findings (worst-first):**

[P2|UNFIXED] mule-detection/src/app/api/data-local/route.ts:183-191 — turnover still disagrees with /api/analytics despite comment claiming agreement (register A3, narrowed 2.15×→1.014×) — data-local sums ALL txns touching flagged ids = ₹26,19,73,682 (₹26.20 Cr, includes 2,055 NON-flagged txns worth ₹36,25,128); analytics/route.ts:116-122 sums only `t.flagged===true` txns = ₹25,83,48,554 (₹25.83 Cr); DashboardContent.tsx:122 shows ₹26.2 Cr while Analytics shows ₹25.8 Cr; route.ts:184-185 comment "so Dashboard, Analytics, and data-local agree" is false — add `txn.flagged === true` to the loop predicate (or relabel Dashboard "Flagged-touching Turnover" and document the difference)

[P2|REGRESSION] mule-detection/src/app/api/data-local/route.ts:17-21,30-34,43-47 — failed dataset load is masked as a success-shaped empty payload, cached forever; master report D6#14/D5#9 claim "loader failures now log + surface" but only `console.error` was added, nothing surfaced — catch sets `cachedAccounts = []` which is truthy so the poisoned cache is returned by `if (cachedAccounts)` on every later request; client gets HTTP 200 `{accounts:[], stats zeros}` and useLocalData (only errors on `!res.ok`/`json.error`) renders an empty dashboard indistinguishable from a legit empty dataset, no error banner, no retry — set a `loadFailed` flag, return 503/`{error}` from GET when loads failed, and don't cache `[]` so the next request retries

[P2|UNFIXED] mule-detection/src/app/api/data-local/route.ts:59-61,78-88,100-105 — sort/order/risk/category/q params unvalidated; dynamic property access on dataset rows (register A9/S11) — `a[sortField]` accepts any string (`sort=name` → `Number(name)=NaN→0` for all rows → silent arbitrary order; `sort=__proto__` garbage-tolerant but meaningless); `riskFilter` compared raw-equality against unvalidated input → silent empty result; unknown `category` falls through to all-flagged; `q` uncapped → O(105k×|q|) scan per request — whitelist a SORT_FIELDS map (`risk_score|turnover|ml_score|…` numeric fields only), validate `risk ∈ {critical,high,medium,low}` and `category ∈ {all,mule,high}`, cap `q` length (~100)

[P2|UNFIXED] mule-detection/src/app/api/data-local/route.ts:7-9,12,24,37 — module caches process-lifetime, no TTL/invalidation (register A1); analytics deliberately documents the same strategy (analytics/route.ts:30-35, immutable artifacts) so this may be owner-intent, but combined with the failure-poisoning above a transient read error becomes permanent silent emptiness until restart; cold-start stampede (~118 MB double-parse on concurrent first hits) also open — share one cache/loader with analytics, retry failed loads, dedupe concurrent parses with a promise cache

[P3|NEW] mule-detection/src/lib/normalizers.ts:253-255 — justifying comment for the flagged rule is factually false post-data-regeneration — "A bare riskScore >= 60 cutoff undercounts badly — most mules in the dataset sit below 60": measured ground truth is 53/8,578 (0.6%) flagged below 60 (min 56.0, mean 65.0); a ≥60 cutoff would keep 99.4% — rewrite comment with the real rationale (honest disjoint tier rule, not the <60 claim)

[P3|NEW] mule-detection/src/app/api/data-local/route.ts:124,207-208,230-231 — CP1252 mojibake corrupts 3 em-dashes in comments introduced by the uncommitted edit (HEAD blob had proper U+2014) — raw bytes are `\xc3\xa2\xe2\x82\xac\xe2\x80\x9d` = "â€”" (UTF-8 re-encode of CP1252-decoded em-dash), e.g. line 124 "small dataset â€” return all" — restore em-dashes; audit whichever editor/tool rewrote this file's encoding

[P3|NEW] mule-detection/src/app/api/data-local/route.ts:220-222,225-226,234 — four dead/misleading response keys: `alertsTotal` is a misnomer (emits `activeAlerts`=new+investigating=107, name implies 155) and `alertsResolved`, `turnover`, `avgRisk`, `riskDistribution` have ZERO consumers repo-wide (grep verified; `riskDistribution` only appears as optional type decl useLocalData.ts:28) — and it counts only the FLAGGED subset (route.ts:179-197 loops `accounts`=flaggedAccounts), so its name invites dataset-wide misreading — delete unconsumed keys or rename `alertsTotal`; compute `riskDistribution` over `allAccounts` if kept

[P3|NEW] mule-detection/src/lib/normalizers.ts:16-28,201-230 — 17 MappedAccount fields are computed per row with zero UI consumers (grep over src/components+src/app: no hits): `balance, turnover, totalAmount, reasons, firstSeen, lastActivity, behavioralScore, graphScore, temporalScore, pagerankScore, communityScore, bridgeScore, mlScore, calibratedScore, explanation, inDegree, outDegree`; the sole nominally-consumed orphan `muleType` (DashboardContent.tsx:165) is doubly dead: 0 accounts have `bank==="Unknown"` and the dataset has no `muleType`/`mule_type` key so it maps to `""` always — prune the interface to what pages render, or document as API-shape preservation

[P3|NEW] mude-detection/src/lib/normalizers.ts:156-159 applied at :165-166,:172 — `nonNeg` silently clamps negative/NaN degrees and txn-counts to 0, hiding corrupt upstream data — a generator bug emitting `out_txn_count:-1` becomes a plausible 0-degree account (zero-degree is itself a fraud signal here), indistinguishable from genuine zeros — log-once warning when a non-null invalid value is clamped, or propagate null so downstream can distinguish missing from zero

[P3|UNFIXED] mule-detection/src/app/api/data-local/route.ts:125 — silent `slice(0,500)` alert cap, client unaware (register A8) — dormant today (155 alerts) but a latent contract break the moment alerts exceed 500; no `totalAlerts` hint in pagination — surface `alertsTotal`/`hasMoreAlerts` in the payload or make the cap a query param

[P3|UNFIXED] mule-detection/src/lib/normalizers.ts:268-273 — fallback `computeStats` `flaggedTransactions` approximation diverges 51× from truth and its justification comment is false (register L4 residue) — all 155 alerts have types in the hardcoded list {rapid_movement, fan_in, fan_out, behavioral_change} (dataset type census: 50/40/40/25) so the filter returns 155 vs the API's 7,959 `flagged===true` rows; comment "(there is no per-transaction flag here)" is wrong — transaction rows carry `flagged` (typed in useLocalData.ts:21, counted by the route itself); fires only if `json.stats` missing (never today) — pass transactions into computeStats and count `t.flagged===true`, or delete the fallback

[P3|UNFIXED] mule-detection/src/app/api/data-local/route.ts:111-122 — flagged∩account txn merge computed unconditionally even when `includeTransactions=false` wastes an O(100k)-row double scan + Map build per request (register A35) — guard lines 112-122 behind `if (includeTransactions)`

**Verified clean in this slice:** A30 (`??` dead fallbacks) genuinely fixed via `firstFinite` (normalizers.ts:146-153,172-176,208 — all fire correctly); A7 resolved-alert double-count fixed (else-if at route.ts:206-209, and the working copy removes the dead `resolved===true` branch — 0 alerts have that key); the HEAD bucket-split contradiction (data-local mules=8,578/highRisk=0 vs analytics 1,962/6,616) is FIXED in the uncommitted working copy (now severity-tier split matching analytics exactly, 1,962/6,616 both sides); flagged union predicate identical across both routes; `formatCurrencyINR` crore math correct (÷1e7, utils.ts:70-90); pagination/slice math sound; sort does not mutate cached source arrays; txn dedupe safe (0 duplicate ids); `npx tsc --noEmit` exit 0.

TOTALS sev=P0:0,P1:0,P2:4,P3:8 status=NEW:5,UNFIXED:6,REGRESSION:1