# D2-6 [ML-ARTIFACTS] Files: mule-detection/scripts/export_xgboost.py, convert_model.py, combine_ml_params.py, recompute_ml_scores.py, recompute_transaction_scores.py + artifacts public/model_weights.json, transaction_model.json (4MB - sample!), ml_params.json, transaction_feature_importances.json + scripts/_learned_weights.json. Machine-verify with python: base_score values (expect bug: txn base_score 0.5 vs true intercept ~0.0801), leaf counts zero?, feature name/order arrays match TS consumers' expected order, ml_params advertised metrics (AUC 1.0?) vs plausible. Report every mismatch with measured numbers.
(agent_id=agent-69)

 outcome="completed">Audit complete. All slice files read line-by-line; artifacts machine-verified; cross-file contracts checked against `xgboostPredictor.ts`, `transactionXgboost.ts`, `transactionScorer.ts`, `detectionEngine.ts`, `mlModel.ts`, and producers `train_transaction_model.py`/`train_meta_learner.py`.

## Findings (worst-first)

[P0] [UNFIXED] mule-detection/public/transaction_model.json:1 (byte offset 367) — shipped transaction model exports `base_score: 0.5` (probability-space default) instead of the trained intercept ≈0.0800667 — verified by parse: `"base_score": 0.5`, unique occurrence; consumer `src/lib/transactionXgboost.ts:94-99` adds `logit(base_score)` = logit(0.5) = **0** to the margin, while training-time `predict_proba` included logit(0.0800667) ≈ **−2.4413** ⇒ every live transaction score biased **+2.44 logits** (odds ×11.5; a zero-tree-margin txn serves p=0.500 vs train-consistent 0.080, +42 pp) — master-report P0 DATA item; artifact mtime Aug 23 23:01 predates the fixed producer (`train_transaction_model.py:234-256 resolve_base_score` now reads `learner_model_param.base_score`, its own comment cites "[8.0066666E-2]") and was never regenerated — fix: re-export via the fixed `train_transaction_model.py` export path, then re-derive `FLAG_THRESHOLD` per the regime note in `transactionScorer.ts:28-66`.

[P2] [UNFIXED] mule-detection/public/ml_params.json:2-19 — artifact contradicts runtime constants on all three advertised parameter blocks (master #44, still open; write-only — zero `src/` consumers, grep-verified) — measured: thresholds critical 0.671/high 0.64/medium 0.551 vs `detectionEngine.ts:1601-1603` 0.71/0.66/0.551; ensemble_weights B 0.3968/G 0/T 0/C 0.2032/ML 0.4 vs runtime `detectionEngine.ts:1140-1147` 0.35/0.20/0.10/0.10/0.25/0; platt A=−39.8078/B=12.6312 vs runtime `mlModel.ts:246-247` A=−7/B=2.0256 — fix: delete the orphan artifact or regenerate + wire a single canonical constants module.

[P2] [NEW] mule-detection/public/ml_params.json:24-46 — artifact is not reproducible by its own generator at HEAD and disagrees with its own input file — evidence: (a) contains a `"metrics_note"` key that `scripts/combine_ml_params.py:96-117` never emits; (b) `_meta.ensemble_auc: null` but the generator copies input `auc` verbatim (`combine_ml_params.py:109`) and `scripts/_learned_weights.json:19` has `auc: 1.0`; (c) `_meta.ml_score_separable: true` (line 44) vs input `_learned_weights.json:31` `false`; (d) required input `scripts/_learned_thresholds.json` is ABSENT (`ls` fails), so `combine_ml_params.py:34-35` now raises FileNotFoundError before any write — the shipped file was hand-edited or written by a deleted generator version — fix: regenerate `_learned_thresholds.json` + `_learned_weights.json`, rerun the script, or retire the artifact.

[P3] [UNFIXED] mule-detection/public/transaction_feature_importances.json:1-18 — still consumed by nothing (no references under `src/**`; grep-verified), while runtime recomputes frequency-based importances from trees (`transactionXgboost.ts:280-292`) — master #45 unchanged; values themselves are a consistent normalized-gain snapshot matching `DEFAULT_IMPORTANCES` (0.392/0.279/0.107/0.063/0.050) — fix: consume it or delete it.

[P3] [UNFIXED] mule-detection/scripts/export_xgboost.py:139-140 — unrunnable at HEAD: trains from `dataset_output/ml_features_100k.csv`, which does not exist (`ls dataset_output` → No such directory) — known theme G, still open; secondary: line 129 writes `public/feature_importances.json`, a filename nothing consumes and easily confused with `transaction_feature_importances.json` — fix: produce the CSV or switch the script to load accounts_dataset.json features; rename importance output.

[P3] [NEW] mule-detection/scripts/convert_model.py:7 — unrunnable: loads `scripts/mule_xgboost_model.pkl`, absent from repo (`ls` fails) ⇒ combined with export_xgboost.py being blocked, the served account model (`model_weights.json`) is irreproducible from any in-repo producer — fix: commit the pkl (or a regeneration recipe) or mark the script deprecated.

[P3] [NEW] mule-detection/public/model_weights.json:1 — provenance break: `version "1.0.0"` and `learning_rate 0.1` match neither in-repo producer (`export_xgboost.py:109,114,156` emits `"1.0"` + trains lr 0.05; `convert_model.py:117` emits `"1.0"`, 0.1 only as its fallback default) — the served artifact came from a third/deleted source; cosmetic today since `xgboostPredictor.ts` never validates `version`, but it defeats reproducibility audits — fix: re-export through a committed pipeline so header fields match the producing script.

[P3] [NEW] mule-detection/scripts/recompute_ml_scores.py:159-170 — parity comments mislabel the gated quantity: applies engine band constants (0.71/0.66/0.551) and `is_mule` cut 0.551 to `ml_normalized` (min-maxed raw model score), while `detectionEngine.ts:1586,1594,1601-1603` applies the identical numbers to `calibratedScore = calibrateScore(weighted ensemble)` — two different functions sharing cutoffs by historical accident, yet the comment asserts "must match TypeScript logic"; dataset `is_mule`/`risk_level` will silently diverge from any future engine run — fix: reword the comment to state what is actually gated, or compute the engine quantity.

[P3] [NEW] mule-detection/scripts/recompute_ml_scores.py:144 — tree-validity check is vacuous: counts a tree valid on KEY PRESENCE (`"leaf" in t or ("feature" in t and ("left" in t or "right" in t))`), so the historic degenerate stub `{feature, threshold, left: null, right: null, missing: null}` — the exact inert-artifact failure mode previously shipped — prints "Valid trees: 200/200" — fix: require truthy children or ≥1 reachable leaf.

[P3] [NEW] mule-detection/scripts/recompute_ml_scores.py:78-84 — the eta NOTE's premise about the TS consumer is false at HEAD: it says this script "intentionally DIVERGES from xgboostPredictor.ts (which sums post-eta dump leaves as-is)", but `xgboostPredictor.ts:171-178` also multiplies each leaf by `learning_rate` — the two implementations currently agree; the comment describes a pre-fix consumer — fix: update the NOTE (and the matching `xgboostPredictor.ts:162-170` rationale) to state both sides multiply, leaving `transactionXgboost.ts:157-162` as the sole documented non-multiplier.

[P3] [NEW] mule-detection/scripts/recompute_ml_scores.py:16-18 — CWD-relative `MODEL_PATH`/`ACCOUNTS_PATH`/`OUTPUT_PATH` ("public/…"), the same hazard class fixed for sibling scripts in master D2#29 (`__file__`-anchored `ROOT`); docstring merely admits it; also `OUTPUT_PATH == ACCOUNTS_PATH` in-place rewrite — fix: anchor to `Path(__file__).resolve().parent.parent`.

[P3] [NEW] mule-detection/scripts/recompute_transaction_scores.py:88 (docstring :9) — third night-window definition: boost gate `0 <= hour < 5` vs training `0 <= hour < 6` (`train_transaction_model.py:136`) and serving `hour < 6` (`transactionScorer.ts:157-158`, UI copy "Night-time transaction (00:00–06:00)" at :229) — hour-05 transactions get no +0.05 boost in the stored dataset `riskScore`/`flagged` but count as night everywhere else — fix: align to `< 6`.

[P3] [NEW] mule-detection/scripts/convert_model.py:88-90 — `build_tree` silently fabricates `{'leaf': 0.0}` for dangling node ids instead of raising, so a truncated/corrupt text dump exports as a structurally valid-looking model with zero-filled subtrees — fix: raise on unresolved `yes/no/missing` references.

TOTALS sev=P0:1,P1:0,P2:2,P3:9 status=NEW:9,UNFIXED:4,REGRESSION:0

CLEAN: mule-detection/scripts/_learned_weights.json

Verification notes: `model_weights.json` is healthy against its consumer — 200 real trees (446 splits / 1,092 leaves, depth ≤3), 16 `feature_names` in exact `buildFeatureVector` order, `base_score 0.0` correctly mapped to +0 margin by `baseScoreLogOdds`; the historic zero-leaf/root-duplicate defects are gone at HEAD. `transaction_model.json` likewise has full structure (29,963 splits / 60,126 leaves, depth 6) and its `feature_names` match `extractTransactionFeatures`/trainer row order exactly; scorer↔trainer formula parity (UTC hours, night<6, `amount/(total_in+1)`, defaults 0.3/10, weekday) verified equivalent. The candidate "_learned_weights AUC=1.0 is mathematically impossible" was disproven — its `auc` is the weighted-ensemble AUC (`train_meta_learner.py:133,195`), achievable via the label-leaked COMMUNITY component (known theme E), so it was not reported.