# D1-6 [UI/UX-CROSS] Cross-cutting sweep (read-only, no single-file ownership): grep ALL of mule-detection/src/components/*.tsx + src/app/**/*.tsx for phantom Tailwind classes/tokens not defined in globals.css (e.g. old transition-default-style leftovers), hardcoded px/font sizes duplicating tokens, text-[8px]/[9px] readability, contrast-violating combos, missing aria-pressed/aria-current on toggles/nav, en-US vs en-IN formatting inconsistencies, emoji/icon strategy drift. Report per-file findings with counts.
(agent_id=agent-63)

 outcome="completed">All verification complete. Compiling the final report.

---

## D1-6 [UI/UX-CROSS] — Cross-Cutting Sweep Report

Baseline: `audit/07-SWARM36-MASTER.md` + `01-ui-ux.md` + `DEFECT_REGISTER.md` absorbed. Old-report targets `NetworkGraph/BipartiteNetwork/HierarchicalHypergraph` no longer exist (replaced by `MuleGalaxy/SankeyChart/PlotlySankey`); statuses below are judged against HEAD code. All 32 TSX files read line-by-line; className corpus machine-scanned (every `className="…"`/`` `{`…`}` `` token extracted and diffed against `globals.css` tokens).

**Verified FIXED at HEAD (not re-reported):** phantom-class family (`transition-default` now a real `@utility` ×11 uses; `bg-obsidian/text-fog/btn-primary` et al. gone), `prefers-reduced-motion` block, font-weight loading (400/600/700 + 400/500/700), `Column.width` now applied, row-key `__row_${index}` guard, sortable-`th` keyboard + `aria-sort` (omitted when inactive), `defaultRender` en-IN, `FilterBar` active-filter sentinel + guarded Clear + drawn chevron + full-opacity placeholder, sidebar body-scroll-lock/focus-trap/Esc/focus-return, `inert` drawer wiring, skip-link `tabIndex={-1}`, `max-lg:pt-14` hamburger offset, responsive KPI grids, PageHeaders on Dashboard/Accounts/Transactions load+error states, safeStat preserving 0, disjoint mule/highRisk tiers, epoch-ms sort precompute, Analytics abort/error/retry, crore-labeled Y-axis + tooltip, Sankey `pointNumber` click guard + OTHER-fold + sub-₹1K formatINR + `aria-pressed` on its legend, galaxy score `%` correct (API normalizes to 0–100, route.ts:138-148), radiusCache cleared per build, viewport/themeColor export, per-page metadata.

---

[P1] [NEW] mule-detection/src/components/AnalyticsContent.tsx:171 (with :25-34, :320-324) — "Transaction Amount by Pattern" FANOUT bar filled `var(--color-charcoal)` (#1a1a1a) on Card `bg-surface-1` (#111111) → contrast 1.09:1, bar invisible whenever no pattern filter is active (the default view) — computed ratio (0.0103+0.05)/(0.0052+0.05)=1.09; FANOUT is a populated canonical pattern (Sankey/PATTERN_LINES render it) so real data renders as blank space; grid strokes are the same charcoal so the bar blends into the grid — give FANOUT its own visible fill (e.g. reuse `#f6ad55` from PATTERN_LINES or `--color-chart-quaternary`) instead of `CHART_COLORS.charcoal`.

[P2] [UNFIXED] mule-detection/src/components/MuleGalaxy.tsx:836, 884-899, 932, 1010-1012 — every stateful toggle encodes on/off purely in color/fill: view-mode segmented control (×3), bank-cluster chips, pattern chips, "Follow the money" — none carries `aria-pressed` (or radiogroup semantics), while the rewritten Sankey got the fix (SankeyChart.tsx:232) — grep `aria-pressed` in MuleGalaxy.tsx = 0 hits vs 1 in SankeyChart.tsx; WCAG 1.4.1 state-by-color — add `aria-pressed={active}` to all four control groups.

[P2] [UNFIXED] mule-detection/src/components/MuleGalaxy.tsx:842, 938 — low-contrast gray text survives in the rewritten graph UI: `placeholder:text-ash/70` on `bg-surface-1` input (≈3.4:1) and the 10px controls-help paragraph `text-ash/70` on void (≈3.4:1, needs 4.5:1) — H8's exact recipe; FilterBar's twin was fixed to `placeholder:text-ash` (FilterBar.tsx:89) but this file kept `/70` — use full `text-ash`.

[P2] [UNFIXED] mule-detection/src/components/{MuleGalaxy,AnalyticsContent}.tsx (app-wide) — sub-readable type floor persists at HEAD: `text-[10px]` ×41 and `text-[9px]` ×3 (MuleGalaxy.tsx:865,866,1025) plus Recharts `fontSize: 9` ticks (AnalyticsContent.tsx:389, 441) — histogram from mechanical scan: `[11px]:57, [10px]:41, [13px]:13, [12px]:7, [9px]:3`; H9 called for an 11-12px floor; nothing below 10px was raised — floor at 11px (`--text-caption`), bump Recharts ticks to ≥10.

[P3] [REGRESSION] mule-detection/src/components/AlertsContent.tsx:49-63, 151, 156-187 — master report D5-fix #12 claims "PageHeaders added to all loading/error states" + stale-data parity banners, but Alerts' loading (`<div className="p-8"><LoadingState/></div>`) and error states have neither PageHeader nor the `max-w-[1200px] mx-auto` wrapper, and there is no "Refreshing…"/refresh-failed banner when refetch fails with data present (silent stale table) — compare DashboardContent.tsx:83-99/108-118, AccountsContent.tsx:54-70/78-88 which all have them — mirror the Dashboard pattern (PageHeader + banners + aligned wrapper).

[P3] [UNFIXED] mule-detection/src/components/ui/StatCard.tsx:24 + src/lib/utils.ts:12-16 — the locale-split engine (H3) still lives: numeric `value` formats via `formatNumber`→`getUserLocale()`→`navigator.language || "en-US"` while all ~30 other call sites hardcode `"en-IN"`; sole live trigger AnalyticsContent.tsx:197 `<StatCard label="Alerts" value={data.totalAlerts}/>` (number) — utils.ts docstring "falls back to en-US" contradicts app convention; latent hydration divergence + "1.2K"-style compact output for ≥1000 vs en-IN grouping beside it — pass pre-formatted strings everywhere (as Dashboard/Analytics already do) or pin the locale to `en-IN`.

[P3] [NEW] mule-detection/src/components/** (app-wide) — hardcoded px sizes duplicate defined tokens in a split-brain pattern: `text-[11px]` ×57 and `text-[13px]` ×13 in page-level code vs `--text-caption: 11px` / `--text-body: 13px` which the ui/ primitives themselves use (`text-caption` ×8, `text-body` ×6); plus tokenless `text-[10px]` ×41, `text-[12px]` ×7, `text-[15px]` ×2, `text-[14px]` ×1 — mechanical histogram cited above; PageHeader.tsx:18 subtitle is `text-[11px]` two lines below StatCard using the token for the same role — mass-replace `text-[11px]`→`text-caption`, `text-[13px]`→`text-body`.

[P3] [UNFIXED] mule-detection/src/components/** — `tracking-[-0.02em]` repeated ×82 untokened (L6 open); `tracking-tight` ×2 and `tracking-wide` ×1 coexist — define `--tracking-tight`/utility or a `.h-label` class.

[P3] [NEW] mule-detection/src/components/AlertsContent.tsx:115 + MuleGalaxy.tsx:813, 977, 981 — four off-token Tailwind-default palette classes: `text-amber-400` (alert status "investigating"), `text-red-300` (galaxy error), `text-sky-300` + `bg-slate-300` (legend) — grep inventory: exactly these 4 hits repo-wide; everything else uses `--color-*` tokens; worse, the `bg-slate-300` "High-value flow" legend swatch corresponds to nothing rendered (high-value corridors differ only by ribbon width/particle count, colored `#93c5fd`/`#f87171`, MuleGalaxy.tsx:400-412) — map to semantic tokens (e.g. `text-risk-medium` for investigating) and draw the high-value swatch as a wider `#93c5fd` bar.

[P3] [NEW] mule-detection/src/components/MuleGalaxy.tsx:78-81, 916, 946, 985 — galaxy node-tier palette literals `#ef4562`/`#f2a35c`/`#65a9fa`/`#182130` + slider `accent-[#ef4562]` diverge from the semantic risk tokens (`--color-risk-critical:#ef4444`, `--color-risk-high:#f97316`) used by RiskBadge/Dashboard bars/CategoryBadge for the same concepts — canvas needs literals but they should come from one shared TS token object (old-audit theme reintroduced by the rewrite); e.g. a "critical mule" dot in the galaxy is a different red than a "critical" badge two pages away — extract a `GALAXY_COLORS` const mirroring `--color-risk-*` values.

[P3] [NEW] mule-detection/src/components/AnalyticsContent.tsx:33, 39-43, 167-171 — same fraud pattern renders under two different colors on one page: CIRCULAR is `#ef6c6c` in the trends line chart + filter-chip dot (PATTERN_DOT_COLORS) but `#e15759` (`accentCircular`) in the adjacent bar chart; likewise FANIN/FANOUT/PASSTHROUGH line colors (`#7fd1f0/#f6ad55/#b8bab9`) match nothing in the bar chart (frost/charcoal/ash) — pattern→color identity breaks as the eye moves between stacked cards — derive bar fills from the same PATTERN_LINES palette.

[P3] [UNFIXED] mule-detection/src/components/{AnalyticsContent,SankeyChart}.tsx:38-43 / 19-31 + src/app/globals.css:23-26, 121-124 — pattern palette duplicated as literals in two files while the purpose-built `--color-chart-accent-{fanin,passthrough,circular,fanout}` tokens and `.chart-accent-*` utilities have zero consumers (reported-only item #31 still true at HEAD); SankeyChart.tsx:257 also hardcodes `#444345` — the exact legacy design-tokens.md Charcoal hex that token rot was supposedly purged for (real token `#1a1a1a`) — delete the dead accent tokens/utilities or adopt them as the shared source.

[P3] [UNFIXED] mule-detection/src/components/MuleGalaxy.tsx:398 vs :970 — legend "Flagged corridor" swatch `#f87171` vs actual ribbon render `rgba(239,69,98,.26)` — same legend-vs-render mismatch registered for the deleted NetworkGraph (D3 reported-only #19) persists verbatim in the replacement component — swatch `rgba(239,69,98,.55)` or recolor links to the swatch.

[P3] [NEW] mule-detection/src/components/{SankeyChart,AnalyticsContent,MuleGalaxy}.tsx:209,274 / :233,289,293 / :393 — hardcoded font stacks `"JetBrains Mono, monospace"` / `"JetBrains Mono"` in Plotly layouts, node-label HTML and ~8 Recharts `tick` configs bypass `--font-mono`; a font swap leaves chart text behind — import one shared `MONO_FONT` constant.

[P3] [UNFIXED] mule-detection/src/components/ui/RiskBadge.tsx (whole file) + AlertsContent.tsx:90-98 + DashboardContent.tsx:34-44 — RiskBadge still has zero importers (M3 open): Alerts hand-rolls severity colors (unknown severity falls through to neutral `text-bone` — reads as ordinary, contradicting RiskBadge's documented "unknown must not look safe" rule at RiskBadge.tsx:10-17), Dashboard invents CategoryBadge; consequently the `.risk-badge-*` CSS blocks (globals.css:111-114) and `.risk-{critical,high,medium,low}`/`.chart-*` classes (globals.css:106-109, 116-124) are dead CSS — adopt RiskBadge in Alerts/Dashboard or delete component + dead blocks.

[P3] [UNFIXED] mule-detection/src/components/AlertsContent.tsx:171 — severity dropdown offers "Info" which no alert in the dataset/API can have (domain-4 reported item #32, still present) — remove the option.

[P3] [UNFIXED] mule-detection/src/components/ui/EmptyState.tsx:42 — error variant announces via `role="status"` (polite) instead of `role="alert"` (reported-only #31; ErrorState.tsx:18-19 comment even acknowledges inheriting it) — set `role={variant === "error" ? "alert" : "status"}`.

[P3] [UNFIXED] mule-detection/src/components/ui/Button.tsx:50 + ui/LoadingState.tsx:25, 36 — spinner SVG duplicated ×3 and Button's is fixed `h-4 w-4` regardless of size (both clauses of reported-only #31) — extract `<Spinner/>`; size from the size map.

[P3] [UNFIXED] mule-detection/src/components/Sidebar.tsx:111 — footer `v2.4` vs package.json `"version": "0.1.0"` (verified) — version chaos theme I unresolved — derive from package.json or pick canonical.

[P3] [UNFIXED] mule-detection/src/app/layout.tsx:24 — `twitter.card: "summary_large_image"` with no `og:image`/`metadataBase` anywhere (grep: zero hits) — reported-only #27 open — add image + metadataBase or downgrade to `summary`.

[P3] [UNFIXED] mule-detection/src/components/Sidebar.tsx:66-67 — `aria-hidden`/`inert` depend on `isMobileViewport` state set only in a post-hydration `matchMedia` effect, so the pre-hydration mobile drawer renders without either (briefly tabbable off-screen) — reported-only #28 open — default the state via inline `matchMedia` lazy initializer or CSS `visibility` at <lg.

[P3] [UNFIXED] mule-detection/src/components/Sidebar.tsx:61 + src/app/layout.tsx:38 — 200px sidebar width magic number still duplicated across the two files (reported-only #29) — shared const or CSS var.

[P3] [UNFIXED] mule-detection/src/components/DashboardContent.tsx:53 + src/app/api/data-local/route.ts:235 — fabricated-stat fallback remains: `safeStat(s.totalInDataset, 105501)` presents a magic dataset-size as live data if the field is ever NaN/missing, and the route's error-path stats emit `totalInDataset: 0` so the card can equally show a false "0" (A20/M1 residue; safeStat fix only preserved zeros, didn't remove the fiction) — fall back to recomputed `accounts.length` or "—".

[P3] [UNFIXED] mule-detection/src/components/DashboardContent.tsx:122 — "Turnover" still labels the flagged-accounts-only sum (₹55.64 Cr) unqualified while Analytics relabeled the identical figure "Flagged Turnover" (AnalyticsContent.tsx:195) — reported-only #21 clause open — rename Dashboard label "Flagged Turnover".

[P3] [UNFIXED] mule-detection/src/components/TransactionsContent.tsx:97-103 — decorative From→To separator column still renders a nameless empty `<th>` (`header: ""`) announced as an unlabeled column (reported-only #21) — `headerClassName:"sr-only"` with aria-hidden content or span the row.

[P3] [UNFIXED] mule-detection/src/components/TransactionsContent.tsx:153 + AlertsContent.tsx:138 — timestamp cells format in the viewer's local timezone while the analytics API buckets hourly/day data in IST (route.ts:161,202) — the same event shows different hours on Alerts vs Analytics (A32/A12 open) — format with `timeZone: "Asia/Kolkata"` or label the column with the tz.

[P3] [UNFIXED] mule-detection/src/components/MuleGalaxy.tsx:564-591, 559/876, 613-616 — carried-over perf trio from D3 reported-only #19, still true in the rewrite: FPS meter calls `setFps` every 500ms re-rendering the whole tree forever; `zoomToFit(…,24)` margins ignore the 400px overlay panel (nodes hide behind it); hover handler rescans all links O(n) per event — throttle FPS to the panel only / pad margins by panel width / precompute adjacency for hover.

[P3] [UNFIXED] mule-detection/src/** — icon strategy still three-way split (M13 open): lucide-react only in error.tsx/not-found.tsx; hand-rolled SVGs for hamburger/close/search/chevron (Sidebar.tsx:52,78; FilterBar.tsx:53,91); geometric glyphs ▲▼●■◆ (StatCard/RiskBadge/DataTable sort), ◉ ✕ → (MuleGalaxy:1025, AnalyticsContent:220, TransactionsContent:101) — standardize chrome icons on lucide, keep glyphs as data markers with `aria-hidden`.

[P3] [NEW] mule-detection/src/components/AnalyticsContent.tsx:220 + MuleGalaxy.tsx:888 — decorative `✕` inside clear-filter/clear-bank button text is not `aria-hidden`, so SRs announce "Clear filter times"/"ballot x" — wrap glyph in `<span aria-hidden>✕</span>`.

[P3] [NEW] mule-detection/src/components/MuleGalaxy.tsx:840-871 — live search suggestions render as a bare absolutely-positioned button list with no combobox semantics (no `role="listbox"`/`aria-expanded`/`aria-activedescendant`, no arrow-key navigation; Enter silently flies to whichever hit ranked first) — implement the ARIA combobox pattern or minimally add `role="listbox"`+`aria-expanded` and ArrowUp/Down selection.

[P3] [NEW] mule-detection/src/app/accounts/page.tsx:10 — metadata overclaims "Browse 100k+ analyzed accounts — mule classifications, risk scores, KYC and behavioral flags": the page lists only the ~8.5k flagged subset (category param, DISPLAY_CAP 500 hint) and shows no KYC column anywhere — reword to flagged-accounts scope.

[P3] [NEW] mule-detection/src/components/{AnalyticsContent,AlertsContent}.tsx:192 / :151 — page-wrapper convention split: Analytics uses bare `p-8 space-y-6` and Alerts bare `p-8` (no `max-w-[1200px] mx-auto`) while Dashboard/Accounts/Transactions all wrap `p-8 max-w-[1200px] mx-auto` — D5-fix #7 aligned only its three owned pages; tables/cards stretch full-bleed on ultrawide for these two — apply the same wrapper.

[P3] [UNFIXED] mule-detection/src/app/globals.css:74, 143-147 — `*,*::before,*::after{box-sizing:border-box}` duplicates Tailwind preflight and the 4px `#333` scrollbar/`::selection` remain (reported-only #31 / L4) — drop the redundant rule; widen scrollbar to 8-10px with hover state.

[TOTALS] sev=P0:0,P1:1,P2:3,P3:29 status=NEW:10,UNFIXED:22,REGRESSION:1

CLEAN: mule-detection/src/components/ui/Card.tsx, mule-detection/src/components/ui/Skeleton.tsx, mule-detection/src/components/SidebarOverlay.tsx, mule-detection/src/components/plotly/PlotlySankey.tsx, mule-detection/src/app/global-error.tsx, mule-detection/src/app/page.tsx, mule-detection/src/app/graph/page.tsx, mule-detection/src/app/alerts/page.tsx, mule-detection/src/app/analytics/page.tsx, mule-detection/src/app/transactions/page.tsx