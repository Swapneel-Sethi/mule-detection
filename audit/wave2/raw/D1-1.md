# D1-1 [UI/UX-DESIGN] Files: mule-detection/src/app/globals.css + mule-detection/design-tokens.md. Focus: token definitions vs actual usage app-wide, degenerate clamp() math, layer/order bugs, dark-mode/contrast math (WCAG ratios), dead/duplicate selectors, reduced-motion support, print styles.
(agent_id=agent-58)

 outcome="completed">All evidence gathered. Compiling the audit report.

---

**D1-1 [UI/UX-DESIGN] — globals.css + design-tokens.md, line-by-line audit**

Baseline checked against `audit/07-SWARM36-MASTER.md`, `audit/DEFECT_REGISTER.md` (UI section empty — master is authoritative), `audit/01-ui-ux.md`. Verified FIXED at HEAD and not re-reported: C1–C4 (loading/error/not-found now use real utilities; `.transition-default` is a real `@utility`, emitted in bundle, consumed 12× across 8 files), #3/#19 (`edge-*`, `.risk-dot` gone from CSS *and* src), #5/#18/#28 (skip-link target, reduced-motion guard, inert drawer), M5 (Inter 400/600/700, JBM 400/500/700 — all `font-medium` uses are mono-context), M7-partial/PageHeader/CardTitle tokenization, #9 StatCard trend inference, L1 (version label full-ash), L10 (global-error hexes aligned), L11 (viewport/themeColor export). Token-level WCAG math on all core pairs passes: ash-on-charcoal 4.91:1 (closest margin), risk-critical-on-void 5.58:1, all badge black-on-color ≥5.58:1. Font wiring proven sound in the compiled bundle (next/font emits `.inter_*__variable{--font-display:"Inter","Inter Fallback"}` on `<html>`, beating the layered theme emission) — but see finding 3.

[P2] [NEW] mule-detection/src/app/globals.css:19-26 + design-tokens.md:15 — chart/risk color tokens are CSS-vars-only with no literal export path, so consumers hand raw `var()` strings into SVG presentation attributes where they don't resolve — `src/components/AnalyticsContent.tsx:28,171,176,187-188,230-442` passes `"var(--color-charcoal)"`/risk vars into Recharts `stroke=`/`fill=` props; Recharts 3.10.1 renders them as `<path>` attributes (`node_modules/recharts/es6/shape/Rectangle.js:212-215` spreads them via JSX props, only `animationStyle` goes to `style:`); the file's own comment at `AnalyticsContent.tsx:31-32` admits "Literal hex for chart internals that cannot rely on CSS vars" — then violates it 20+ times; unresolved attr values fall back to initial (stroke:none grids, initial-black fills) — add a shared TS token module exporting resolved hexes (H11's prescribed fix, never landed) and route every Recharts/Plotly/canvas consumer through it; visual outcome unverifiable headlessly, flagged accordingly.

[P2] [NEW] mule-detection/src/app/globals.css:94-101 — dark-only app declares no `color-scheme: dark` anywhere (src-wide grep: zero matches; layout.tsx sets only themeColor) — native `<select>` popup lists (`ui/FilterBar.tsx:41`), autofill, date/input chrome and Firefox scrollbars render in LIGHT UA styling against the pure-black UI; also the sole reason the custom 4px webkit scrollbar silently does nothing on Firefox — add `color-scheme: dark` to the `html` rule (or `<meta name="color-scheme" content="dark">`).

[P3] [NEW] mule-detection/src/app/globals.css:40-47,68-72 — documented fallback is false: without next/font's class the alias pair is a genuine custom-property cycle, collapsing fonts to UA serif — compiled chunk `.next/static/chunks/2c_9r9gns2e30.css` contains verbatim `--font-display:var(--app-font-display), system-ui, sans-serif;` and `:root{--app-font-display:var(--font-display,"Inter");`; css-variables §3.4 makes both guaranteed-invalid (the `"Inter"` fallback inside a cyclic `var()` does not rescue); `html{font-family:var(--font-sans)}` then becomes invalid → inherited UA default, contradicting the comment "falling back to the raw family names when the runtime variables are absent"; works today only because the bundle always ships the unlayered font class — swap roles: give next/font's `variable:` option the `--app-font-display/--app-font-mono` names and define theme tokens as `var(--app-font-display, Inter), system-ui, sans-serif`; `design-tokens.md:22` repeats the same false claim.

[P3] [UNFIXED] mule-detection/src/app/globals.css:32-38,48-53 — fluid-type adoption still absent (known M7): six of seven `--text-fluid-*` tokens have zero consumers src-wide — grep finds only `ui/Button.tsx:24` using `text-fluid-base` plus the html root; `--text-label` (50) and `--text-display` (53) likewise referenced nowhere outside definition, while arbitrary `text-[8px]…[15px]` persists app-wide — adopt for headings/labels or delete the dead tokens.

[P3] [UNFIXED] mule-detection/src/app/globals.css:19-26,117-124 — all eight `--color-chart-*` tokens and all eight `.chart-*` utility classes remain zero-consumer (known #31): comprehensive grep for both class form and `var(--color-chart-*)` string form returns only globals.css self-references; AnalyticsContent maintains its own hybrid literal/var map instead — delete or wire up.

[P3] [UNFIXED] mule-detection/src/components/MuleGalaxy.tsx:842 — placeholder contrast residue (known H8, partially fixed): `placeholder:text-ash/70` on `bg-surface-1` ≈ **3.19:1** (blend 0.7·#888 + 0.3·#111 ≈ #646464; L=0.1274 vs surface-1 0.0056) vs required 4.5:1 — FilterBar was fixed to full `text-ash` (5.33:1); this search input was missed — use `placeholder:text-ash`.

[P3] [NEW] mule-detection/src/app/globals.css:106-109 — `.risk-critical/.risk-high/.risk-medium/.risk-low` plain classes are dead selectors: every component consumes Tailwind-generated `text-risk-*`/`bg-risk-*`/`border-risk-*` instead (grep: zero bare-class usages; only `.risk-badge-*` at 111-114 is live via `ui/RiskBadge.tsx:4-7`) — two parallel mechanisms for the same colors, one orphaned — delete the four rules.

[P3] [NEW] mule-detection/src/app/globals.css:9,7 — input/select control identification fails WCAG 1.4.11 non-text contrast: surface-1 field on void page = **1.11:1**, `border-frost/10` blends to ≈#262626 on surface-1 = **1.25:1** (required 3:1 for control boundaries) — affects every FilterBar select/search (`ui/FilterBar.tsx:46,89`) rendered on the black page background — brighten border to `frost/25`+ or add a faint outer ring.

[P3] [NEW] mule-detection/design-tokens.md:34 + globals.css:85-92 — reduced-motion claim overcovers: guard neutralizes CSS animations/transitions only, while rAF-driven animation continues (grep: `requestAnimationFrame` ×2 in `MuleGalaxy.tsx` particle/FPS loops, ×2 in `lib/useLocalStorage`-adjacent `useLocalData.ts`), and canvas redraws ignore the preference — doc line "a global prefers-reduced-motion guard neutralizes animations and transitions" reads as complete coverage — gate rAF loops on `matchMedia('(prefers-reduced-motion: reduce)')`.

[P3] [NEW] mule-detection/src/app/globals.css:98 — `html{font-size:clamp(13px,…,15px)}` pins the root em basis in absolute px: overrides users' browser default-font-size preference entirely, and because Tailwind v4 spacing/text/radii are rem-based, every `p-4`/`text-sm`/`rounded-md` app-wide silently rescales ~13→15px across breakpoints — coupling beyond the intended fluid body copy (no comment acknowledges it) — apply the fluid size to `body` and leave `html` at 100%.

[P3] [NEW] mule-detection/src/app/globals.css:103 + src/app/layout.tsx:33,38 — body min-height declared three ways with one dead: globals sets `min-height:100vh`, layout adds `min-h-full` (percentage against auto-height `html` → computes to nothing) and `main` carries `min-h-screen`; 100vh also overshoots mobile URL-bar viewports — collapse to a single `min-h-dvh` on body and drop the redundant classes/rules.

[P3] [UNFIXED] mule-detection/src/app/globals.css:74,143-147 — known #31/L4 items still present at HEAD: `box-sizing` universal rule duplicates Tailwind preflight; scrollbar hardcoded 4px wide with `#333` thumb and `::selection` hardcoded `#333` (no tokens, no hover state); Firefox gets no `scrollbar-width`/`scrollbar-color` fallback at all — delete the box-sizing rule, token the greys, add Firefox equivalents.

[P3] [UNFIXED] mule-detection/src/components/Sidebar.tsx:111 — version drift unchanged (reported-only #26): sidebar footer `v2.4` vs `package.json:3` `"version": "0.1.0"` (backend reports 2.4.0) — pick one canonical source and derive the rest.

[P3] [NEW] mule-detection/design-tokens.md:34 — motion doc mislabels the curve: ".transition-default (150 ms ease-out)" vs actual `cubic-bezier(0.4, 0, 0.2, 1)` (`globals.css:81`) — that bezier is an ease-in-out (accelerate-decelerate) curve — correct to "150 ms ease-in-out" or change the token to a true ease-out.

[P3] [NEW] mule-detection/src/app/globals.css (whole file) — no `@media print` styles exist anywhere (src-wide grep: zero): the all-black app prints as ink-solid black pages with unreadable dark-styled tables/charts — add a minimal print block (white background, black text, hide sidebar/scrollbar chrome, force chart backgrounds transparent).

[P3] [NEW] mule-detection/design-tokens.md:14-15 vs src/components/AnalyticsContent.tsx:39-42,47-50 — active chart palettes bypass the semantic tokens entirely, including a legacy pre-redesign hex: `PASSTHROUGH: "#b8bab9"` is the OLD Ash value that design refresh replaced with `#888888` (doc table in audit 01 confirms the rename), and FANIN/FANOUT/CIRCULAR/OTHER (`#7fd1f0/#f6ad55/#ef6c6c/#6b7075`) match no token; MuleGalaxy legend (`MuleGalaxy.tsx:970`) uses yet another set (`#ef4562` vs token `#ef4444`) — consolidate on the token set (via the literal-hex module from finding 1) or document the chart palettes as intentional extras.

TOTALS sev=P0:0,P1:0,P2:2,P3:14 status=NEW:11,UNFIXED:5,REGRESSION:0