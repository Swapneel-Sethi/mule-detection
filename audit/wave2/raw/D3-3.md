# D3-3 [VIZ-API] Files: mule-detection/src/app/graph/page.tsx, src/app/api/graph/mule-galaxy/route.ts. Focus: cachedPayload frozen generatedAt / no TTL, sync 113MB-parse cold path, raw error.message leakage, meta.highRisk semantic inversion, HTTP codes, payload size/streaming, force-dynamic flags, dead references to deleted NetworkGraph/HierarchicalHypergraph routes.
(agent_id=agent-72)

 outcome="completed">Audit complete. Verification preamble: `npx tsc --noEmit` exit 0; dataset facts sampled programmatically (accounts 105,501 / txns 99,952; flagged universe 8,578; corridors 7,952, cap 40k not hit; calibrated ∈ [0.053, 0.856]). Negative checks: **no** dead references to deleted NetworkGraph/HierarchicalHypergraph routes or bipartite artifacts anywhere under `src/` (grep: zero matches; sole API consumer is `MuleGalaxy.tsx:189`); A16 error-leak fix, Cache-Control fix, and `GalaxyLink.lastDay` interface fix all intact (no REGRESSIONs found); `force-dynamic` confirmed valid against the bundled Next 16 docs.

[P1][UNFIXED] mule-detection/src/app/api/graph/mule-galaxy/route.ts:49,77,162-163 — module-global `cachedPayload` never invalidated and `generatedAt` frozen at first cold hit, so regenerated datasets are invisible until restart; on build failure cache stays null so every retry re-parses ~110 MB (stampede amplification) — register A4; evidence: `if (!cachedPayload)` gates everything, `generatedAt: new Date().toISOString()` only inside that branch; catch block leaves `cachedPayload === null` — fix: invalidate on source mtime change (`stat` both JSONs, rebuild when either differs) and derive `generatedAt` from file mtimes, not request time

[P2][UNFIXED] mule-detection/src/app/api/graph/mule-galaxy/route.ts:51-54,180-182 — sync ~110 MB parse cold path plus uncacheable multi-MB response: `readFile`+`JSON.parse` of accounts_dataset.json (~92 MB) + transactions_synthetic.json (~18 MB) blocks the event loop for seconds per cold instance, then the whole payload (~2.8 MB estimated: node/link JSON × 8,578/7,952) is buffered in one `NextResponse.json` under `Cache-Control: no-store`, forcing a fresh download every visit; no `functions.memory`/`maxDuration` (register S13) so serverless cold hits risk timeout/OOM 500s — register S10 — fix: precompute the galaxy payload to a small static JSON at generation time (pattern already used by sibling views), else compress/stream and add `s-maxage` + stale-while-revalidate

[P3][UNFIXED] mule-detection/src/app/api/graph/mule-galaxy/route.ts:136-138 — score heuristic's non-primary branches both dead: `calibrated > 1 ? calibrated : calibrated > 0 ? calibrated * 100 : fallbackScore` — measured `calibrated_score` max=0.856, min=0.053, count>1=0, count==0=0, and `fallbackScore` (`risk_score`) ≡ calibrated×100 anyway (max 85.6 = 0.856×100), so the ternary is always `calibrated*100` while carrying register A28's mixed-scale hazard — fix: collapse to `Math.round(calibrated * 100)` and delete the boundary branches

[P3][UNFIXED] mule-detection/src/app/api/graph/mule-galaxy/route.ts:167-168 — meta naming inverted vs sibling API on identical data: galaxy `meta.mules`=critical/high-tier mules only (would emit 1,962) and `meta.highRisk`=watchlist tier (6,616), while `/api/data-local` `computeStats` reports `muleCount`=8,578 and `highRiskCount`=0 for the same dataset; consumer stat labels "Active Mules"/"Watchlist" (`MuleGalaxy.tsx:802-803`) inherit the inversion — register A29 — fix: rename fields to `severeMules`/`watchlist` or reuse data-local's disjoint definitions

[P3][UNFIXED] mule-detection/src/app/api/graph/mule-galaxy/route.ts:158-160 — `validLinks` filter redundant: link endpoints were already constrained to the allowlist built from the same `text(account_id)` keys that key `nodeById` (lines 87, 95, 128); verified 0 empty and 0 duplicate ids across 105,501 rows — master D3 reported-only #20 — fix: drop the filter or convert to a dev-time assertion

[P3][UNFIXED] mule-detection/src/app/api/graph/mule-galaxy/route.ts:169 — `meta.totalVolume` computed (₹258,359,658.00 verified from data) but consumed by nothing; sole consumer reads only nodes/links/mules/highRisk/flaggedVolume (`MuleGalaxy.tsx:800-804`) — master D3 reported-only #20 — fix: delete the field or surface it in the stats strip

[P3][UNFIXED] mule-detection/src/app/api/graph/mule-galaxy/route.ts:127,149 — 53 flagged accounts have zero intra-flagged corridors (verified `isolated flagged nodes = 53`; matches master D3 #27) yet are emitted as `degree: 0` nodes and render as disconnected dots — fix: filter them from the payload or give the client an explicit empty-degree badge/filter

[P3][NEW] mule-detection/src/app/api/graph/mule-galaxy/route.ts:98,105 — `lastDay` buckets days by UTC slice of ISO stamps (`'2026-04-11T20:18:05.000Z'.slice(0,10)`), so corridor activity between 00:00–05:30 IST lands on the previous day, off-by-one-shifting the time-scrubber range (`dayRange`, `MuleGalaxy.tsx:217-224`) for an INR/IST product; systemic tz mixing was reconciled elsewhere (A12/A32 class) but not here — fix: bucket on `new Date(ts + 5.5h)` or pin and document a single UTC convention across APIs

[P3][NEW] mule-detection/src/app/api/graph/mule-galaxy/route.ts:66-68 vs 129-130 — same field normalized two ways in one file: universe filter compares raw `account.risk_level === "critical"|"high"` while node building lowercases/trims first (`text(...).toLowerCase()`, then coerces anything unknown to `"medium"`), so a future `"Critical"` row would vanish from the graph yet would otherwise map to tier critical — verified current vocab is all-lowercase `{low:96923, medium:6616, critical:1943, high:19}` — fix: normalize once in a shared helper consumed by both paths

[P3][NEW] mule-detection/src/app/graph/page.tsx:8-12 — metadata copy inaccurate/inconsistent: title "Network Graph" vs sidebar label "Graph" (`Sidebar.tsx:12`; all five sibling pages match their sidebar labels exactly), description claims "ML-flagged" accounts though the universe predicate is the rule band `is_mule || risk_level∈{critical,high}` with `ml_score` never consulted (route.ts:82-84), and "live money-flow corridors" though the snapshot is a frozen synthetic dataset — fix: title "Graph" (or "Mule Galaxy") and reword to "flagged"

TOTALS sev=P0:0,P1:1,P2:1,P3:8 status=NEW:3,UNFIXED:8,REGRESSION:0