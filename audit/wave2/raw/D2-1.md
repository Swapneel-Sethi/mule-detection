# D2-1 [ML-ENGINE-1H] File: mule-detection/src/lib/detectionEngine.ts LINES 1-950. Focus: scoring math (weights, normalization, percentile claims), FANIN/FANOUT/PASSTHROUGH/CIRCULAR detection logic vs documented definitions, windowing/boundary bugs, division guards, alias normalization completeness (account vs txn fields), comment-code contradictions.
(agent_id=agent-64)

 outcome="completed">Audit complete. All findings verified against HEAD source and live dataset samples (`public/accounts_dataset.json`, `public/transactions_synthetic.json`); V8 spread-limit hypothesis was tested and **dropped** (Node handles 105k-element spreads fine here).

## D2-1 [ML-ENGINE-1H] — `mule-detection/src/lib/detectionEngine.ts` L1-950

- **[P2] [NEW]** mule-detection/src/lib/detectionEngine.ts:871-878 — Multi-window velocity features are anchored to `Date.now()`, not to the dataset horizon, so they are silently constant-zero today — evidence: `const now = Date.now()`; dataset sample shows txn timestamps span `2026-01-01 … 2026-06-29`, so after 2026-07-06 the 7d/30d windows are empty ⇒ `velocity_7d_180d = 0/(n/25)` ≡ 0 for every account; downstream `>3` gates (computeTemporalScore L1232, red flag L1359, reason L1615) are unreachable and the "DAN Framework" feature family is dead weight — fix: anchor `now` to `max(txn.timestamp)` (or accept an evaluation-date param) instead of wall clock.
- **[P2] [NEW]** mule-detection/src/lib/detectionEngine.ts:469,847,858,892 — Engine reads dataset `balance` as *retained balance*, but the generator writes net flow (tin−tout), negative for ~85% of rows, making every low-retention/balance gate vacuous — evidence: sampled record `"total_out_amount":14246.08,"balance":-12562.84`; `balance < totalIn * 0.1` is trivially true when balance<0 ⇒ `pass_through` severity gate (L472), `passThroughFrequency` (L892), `nearZeroBalance`→`is_transit` (L858, feeding the highest-weight behavioral signal 0.8 at L1179) all over-fire; root cause is known D4 #24 (generator side, REPORTED-only) but this consumer-side consequence is unreported; `a_balance ?? ` first leg is dead (no dataset field `a_balance`) — fix: compute retained balance from graph totals (`totalIn-totalOut`) or clamp `Math.max(balance,0)` and treat negative explicitly until data regenerates.
- **[P2] [NEW]** mule-detection/src/lib/detectionEngine.ts:253 — `detectRapidMovement` uses `Math.abs(outTime - incTime)`, counting pairs where money left *before* it arrived, contradicting its own comment "receives funds and forwards them onward within the window" (L241-243) — evidence: an out-edge timestamped 20 min *before* the in-edge yields `diffMin=20 ≤ 30` ⇒ false "rapid_movement" high/critical alerts; physically impossible flows — fix: require `outTime >= incTime && outTime - incTime <= windowMs`.
- **[P2] [NEW]** mule-detection/src/lib/detectionEngine.ts:864-866 (with 1505-1513 call site) — Per-account feature extraction re-filters the entire transaction list (`transactions.filter(...)` inside `extractEnhancedFeatures`, called once per account) → O(A×T) ≈ 105,501 × ~100k ≈ 10¹⁰ ops; `detectRapidMovement` similarly rescans full in/out edge lists once per trigger txn (O(T·d_in·d_out)) — evidence: L864 `const accountTxns = transactions.filter(...)`, loop at L1505; at dataset scale the engine could never complete if wired — fix: pre-bucket txns by account (`Map<string, Transaction[]>`) once before the account loop.
- **[P3] [NEW]** mule-detection/src/lib/detectionEngine.ts:265-269 — Rapid-movement alert evidence is mislabeled: `incoming_txn: txn.id` cites the *trigger* txn while `amount_in` comes from an arbitrary `inc` edge that may be a different transaction, and the outgoing leg has no reference at all (`EdgeData` L115-119 carries no id) — evidence: L245 `inc` iterates ALL in-edges of `txn.to_account`, only one of which is `txn` itself; stale leftover from the pre-fix pairing logic (master D2 #1) — fix: put `EdgeData.txnId` in the graph and emit `{incoming_txn: inc.txnId, outgoing_txn: out.txnId}`.
- **[P3] [NEW]** mule-detection/src/lib/detectionEngine.ts:74-75 — `PatternType` members `"community_cluster"` and `"bridge_account"` are never emitted by any detector — evidence: repo-wide grep hits only the declaration lines (muleSeed.ts uses "bridge_account" as an unrelated flag string); `detectCommunities`/betweenness produce scores/maps, never `DetectedPattern`s — fix: drop both union members or emit patterns from `detectCommunities`/`computeBetweennessCentrality`.
- **[P3] [NEW]** mule-detection/src/lib/detectionEngine.ts:333-341 — Circular-transfer `total_amount` sums ALL parallel edges per hop regardless of whether they belong to the cycling flow or its timeframe, and the sorted-node-set dedup key destroys cycle orientation — evidence: `edges.get(from->to)` reduced over every historical txn between the pair; `cycleKey = [...path].sort().join(",")` collapses A→B→C vs A→C→B — engine-side twin of register item A13 — fix: sum only edges whose timestamps form a consistent forward chain within a bounded window; keep directional key.
- **[P3] [NEW]** mule-detection/src/lib/detectionEngine.ts:544-559,565 — Community fast-flow metric is ill-defined: `fastFlows` counts (in,out) *pairs* within |Δt|<1h while `totalInternalFlows` counts flows once, so the "ratio" can exceed 1 before the `min(1,…)` cap, and `Math.abs` again credits out-before-in flows — evidence: an account with 10 internal ins + 10 outs in one hour yields fastFlows=100 vs totalInternalFlows=10 → "ratio" 10.0 — fix: mark each out-edge fast at most once (per nearest preceding in-edge) and require `outTime > incTime`.
- **[P3] [NEW]** mule-detection/src/lib/detectionEngine.ts:801-819 — PageRank iteration does not conserve mass: dangling predecessors vanish (L805-814 loops only over predecessors), and `personalization = initial*0.3` (L815) plus `(1-damping)/n` are injected *every* iteration (~20×0.3 = 6× initial mass over 20 iters), so this is not a stationary distribution despite the "PageRank Risk Propagation" header — the post-hoc min-max (L822-828) hides the inflation while distorting relative ranks toward seed priors — fix: standard formulation (dangling mass redistributed uniformly; personalization folded into `(1-damping)*pref` once per iteration).
- **[P3] [NEW]** mule-detection/src/lib/detectionEngine.ts:370,374-376 — Layering-chain amount test reads `edges[0].amount` — the arbitrary first parallel edge — ignoring sibling transactions between the same pair, and DFS param `start` is unused — evidence: L375 `if (edges.length > 0) amounts.push(edges[0].amount)`; `start` never referenced in body (contrast L332 in the circular DFS) — fix: aggregate parallel edges (sum or mean) per hop; delete `start`.
- **[P3] [NEW]** mule-detection/src/lib/detectionEngine.ts:276,326,361,407-411,444 — Silent order-dependent truncation caps throughout the detector suite: rapid `.slice(0,50)`, circular `MAX_CYCLES=30` + `activeNodes.slice(0,100)`, layering `slice(0,30)` starts/`.slice(0,20)`, structuring `.slice(0,20)` — all keyed off `Set` insertion order, so results depend on input ordering and suppressed counts are unreported (summary counts truncated arrays, L1729+) — fix: rank candidates by a severity proxy before capping and log/report dropped counts.
- **[P3] [NEW]** mule-detection/src/lib/detectionEngine.ts:43-52 — `Transaction` interface contract is unsound vs the only shipped dataset: declares required `from_account`/`to_account`/`risk_score`, but `transactions_synthetic.json` keys are `from`/`to`/`riskScore` (sampled: `['amount','flagged','from','id','riskScore','timestamp','to','type']`); `from/to` get fallback-normalized at L1443-1445 but `riskScore→risk_score` nowhere, forcing `as Transaction[]` lies at call sites; `type`/`risk_score` are consumed by nothing in the file — fix: align the interface to the dataset shape (or normalize `riskScore` alongside `from/to`) and drop unused required fields.
- **[P3] [NEW]** mule-detection/src/lib/detectionEngine.ts:958-964 (counterpart L1376) — `beneficiary_concentration` is the share of outgoing *transaction count* captured by the top recipient, but explanation/red-flag copy claims "% of outgoing **funds** go to a single recipient" — count-share ≠ value-share (one ₹9L txn vs nine ₹10 ₹txns flip the story) — fix: compute the share on `amount` sums, or reword the copy to "% of outgoing transactions".
- **[P3] [NEW]** mule-detection/src/lib/detectionEngine.ts:461 — Pass-through detector requires ≥2 in- AND ≥2 out-edges, excluding the canonical single-relay mule (exactly 1 in, 1 out) that its own header describes ("receive funds and immediately forward them"); additionally a missing account row defaults `balance` to 0 (L469), auto-passing the retention gate — fix: allow `inEdgesList.length >= 1 && outEdgesList.length >= 1`, and skip unknown accounts instead of crediting them a zero balance.
- **[P3] [UNFIXED]** mule-detection/src/lib/detectionEngine.ts:664-667,895-927 — Temporal features use local-TZ `getHours()`/`getDay()` on UTC ISO stamps (master #41 reported-only, still present at HEAD) — sites: night_owl L665, hour histogram L897, weekend L912-913, night L918-919, business hours L924-925 — fix: parse UTC hours explicitly (`new Date(t.timestamp).getUTCHours()`) matching the training pipeline's tz-aware parsing.
- **[P3] [UNFIXED]** mule-detection/src/lib/detectionEngine.ts:860,887-888 — Divide-by-zero sentinel `999` still written into persisted features/UI-facing explanations (master #41) — `inOutRatio`, `creditToDebitCount`, `creditToDebitAmount` can surface as "999x"/ratio 999 — fix: emit null/0 with a boolean `is_one_sided` flag instead of 999.
- **[P3] [UNFIXED]** mule-detection/src/lib/detectionEngine.ts:877-878 — Undocumented `/25` and `/6` magic denominators in velocity baselines (master #41) — `window180d.length / 25` and `/ 6` approximate 7/180 and 30/180 but neither comment nor constant explains them — fix: `window7d.length / (window180d.length * (7/180))` with a named comment.
- **[P3] [UNFIXED]** mule-detection/src/lib/detectionEngine.ts:508,567-572 — `communityId` gaps (master #41): id increments only when `communityScore > 0.3`, producing sparse `community_N` keys — fix: derive id from a monotonically incremented counter independent of the acceptance branch.
- **[P3] [UNFIXED]** mule-detection/src/lib/detectionEngine.ts:586-588,645,822-828 — Degenerate-graph zero collapse (master #41): `n<=2` betweenness returns all zeros; PageRank `range===0` maps every node to 0 — fix: return uniform small constants or flag `degenerate:true` so downstream weights don't silently zero out.
- **[P3] [UNFIXED]** mule-detection/src/lib/detectionEngine.ts:848 (counterpart L1429) — Silent `age_days ?? 365` imputation persists for the ~7k ACM rows lacking `account_age_days` (related D4 #23), silently deflating `txns_per_day`/`money_in_out_velocity` and the model's `account_age_days` feature — fix: propagate an explicit `age_missing` marker and exclude imputed rows from velocity-sensitive signals.

No regressions: master-fix D2 #1 (same-node rapid pairing, L240-250) and D2 #2 (distinct `beneficiary_concentration`, L955-964) are correctly in place at HEAD.

TOTALS sev=P0:0,P1:0,P2:4,P3:16 status=NEW:14,UNFIXED:6,REGRESSION:0

CLEAN (within slice, reviewed line-by-line, no defects): `DirectedGraph` (L158-228), `detectFanIn`/`detectFanOut` (L279-321), `detectStructuring` core banding math (L414-445), `detectBurstActivity` (L685-737), `detectAutomatedTiming` (L739-778), `computeClustering`/`computeEgoDensity` (L1076-1104, read as counterparts), Brandes back-propagation mechanics in `computeBetweennessCentrality` (L600-642).