# D4-2 [DATA-GEN-TS] File: mule-detection/scripts/generate-synthetic-data.ts. Focus: drift vs generate_synthetic_data.py (same-named params different values), field naming snake/camel mismatches, risk_score scale, alert schema vs mapAlert(), dead outputs (writes files nobody reads), Node fs edge cases.
(agent_id=agent-77)

 outcome="completed">Audit complete. All evidence gathered — compiling the final report.

---

## D4-2 [DATA-GEN-TS] Defect Report — `mule-detection/scripts/generate-synthetic-data.ts`

Baseline reconciled against `audit/07-SWARM36-MASTER.md` (Domain 4), `audit/04-synthetic-data.md`, and DEFECT_REGISTER lookups. Known items re-confirmed still present are marked UNFIXED; everything else is NEW. No REGRESSIONs found — all prior fixes inside this file hold at HEAD (rails vocabulary :62-64/:123, status vocab :216/:275/:294/:311, self-transfer exclusion :136-138, paired-generation header contract :1-10).

**[P1] [UNFIXED]** `mule-detection/scripts/generate-synthetic-data.ts:82,87,125-126,147,155,215-216,233-234,251-275,293-310` — generator fully unseeded (`Math.random()` ×14 sites, `Date.now()` window anchor), so every run yields different amounts/flags/statuses/timestamps — same finding as 04-H5 (reproducibility matrix row "generate-synthetic-data.ts ✘ Math.random"), never ported to the repo-convention seed 42 that siblings got (audit fix #2, `generate_synthetic_data.py:5`, `convert_csv_transactions.py:36`) — evidence: grep shows zero `seed`/PRNG identifier in the file — fix: thread a mulberry32(42) instance through all randomness and derive the window anchor from a constant or `--as-of` arg.

**[P2] [NEW]** `mule-detection/scripts/generate-synthetic-data.ts:225-235,240-255` — fan-in/fan-out alert copy mixes run-scoped counts with lifetime totals: sender/receiver counts come from this run's ≤8,000-txn universe (:225,:243) while the ₹ amount is the account's lifetime `total_in_amount`/`total_out_amount` (:231,:249) — evidence: **all 40 shipped fan_in alerts read "received funds from 0 distinct accounts totaling ₹…"** (measured via join: cited senders 0 for 38/40, 1 for 2/40, vs dataset `unique_senders` 34–1,254 for the same IDs; e.g. ALT00076 "0 distinct accounts totaling ₹22,77,094.46" vs `unique_senders: 1254`) — fix: quote dataset fields consistently (`acc.unique_senders` / `acc.unique_receivers`) in the description, and use run txns only for the `transactions` evidence list.

**[P3] [NEW]** `mule-detection/scripts/generate-synthetic-data.ts:157` — flag rule `riskScore > 70 || is_mule` contradicts the shipped-file semantics this output replaces: `convert_csv_transactions.py:142` flags ⟺ fraud-pattern ≠ NONE (shipped: 7,959/99,952 flagged, unflagged max 38.3), and mockData's post-audit comment documents "no unflagged row above 38.3" (`src/lib/mockData.ts:119`); a regen produces unflagged rows up to 70, silently breaking that invariant — fix: adopt the pipeline flag convention (pattern/lifetime-based) or lower the cutoff to the aligned ~40 threshold.

**[P3] [NEW]** `mule-detection/scripts/generate-synthetic-data.ts:194-195` — `dormantAccounts`/`veryDormant` gates on `account_age_days > 365/730` exclude **every one of the 7,001 ACM confirmed-mule rows**, which lack the field (measured: `account_age_days` missing = 7,001, exactly the ACM population; `undefined > 365` → false) — the two dormancy alert families can never fire for confirmed mules — fix: treat missing age as unknown and fall back to `firstSeen`-derived age, or gate on activity recency instead.

**[P3] [NEW]** `mule-detection/scripts/generate-synthetic-data.ts:195,262-266,300-314` — the `circular` branch is effectively dead and the `dormant_activation` copy contradicts its own filter: circular needs a reciprocal a→b AND b→a pair inside an 8k-txn random universe over 105,501 accounts (per-account run-degree ≈ 0–5 ⇒ ~0 cycles over the 30 candidates; shipped artifact confirms 0/155 circular), while `veryDormant` requires `totalTransactions < 5` lifetime yet the alert claims "suddenly active" (:308) — evidence: shipped type histogram {rapid_movement:50, fan_in:40, fan_out:40, behavioral_change:25}, circular/dormant_activation both 0 — fix: mine cycles from the full lifetime graph (or drop the branch), and reword dormancy copy to match the ≤4-lifetime-txn evidence.

**[P3] [NEW]** `mule-detection/scripts/generate-synthetic-data.ts:125-126` — regen window `[now−180d, now]` postdates the dataset's hardcoded `lastActivity: "2026-08-22"` for all 98,500 ACC rows, reintroducing the exact account-level chronology inversion class the swarm repaired (D4 #5, 4,305 rows) — evidence: `endDate = new Date()` (runs after Aug 22) vs sampled uniform `lastActivity` — fix: clamp `endDate` to min(now, max account lastActivity) or refresh `lastActivity` in the same write.

**[P3] [NEW]** `mule-detection/scripts/generate-synthetic-data.ts:207,215,233,251,274,293,310` — alert `timestamp` is `now − rand·7d`, decoupled from the evidence txns (which span 180 days), and :207 keeps the *first* 5 generated txns rather than the most recent — same defect class already fixed for the seed generators (D6 #8: alerts anchored +1h after referenced txn) but never applied here — fix: set `timestamp` = latest referenced txn time (+ small delta) and select evidence by descending `timestamp`.

**[P3] [UNFIXED]** `mule-detection/scripts/generate-synthetic-data.ts:231,249` — bare `.toLocaleString()` makes alert currency formatting host-locale-dependent (known 04-L1, open) — evidence: shipped file happens to carry en-IN grouping ("₹2,04,678.93") only because the generating host's ICU locale was en-IN; a CI/Vercel run (en-US) emits "₹2,046,678.93" — fix: `new Intl.NumberFormat("en-IN", {style:"currency", currency:"INR"})` or `toLocaleString("en-IN")`.

**[P3] [NEW]** `mule-detection/scripts/generate-synthetic-data.ts:99,319,324` — `process.cwd()`-relative paths plus zero invocation wiring make the script runnable only by ad-hoc incantation from the repo root: both Python siblings are `__file__`-anchored (`generate_synthetic_data.py:9`, `convert_csv_transactions.py:38`, audit fix #19), and repo-wide grep finds no npm script, no `tsx`/`ts-node` devDependency, and no doc/CI reference (only auto-generated `graphify-out/*` mentions) — fix: resolve via `path.dirname(fileURLToPath(import.meta.url))` and add a `"generate:synthetic"` package script.

**[P3] [NEW]** `mule-detection/scripts/generate-synthetic-data.ts:320,325` — outputs pretty-printed with `JSON.stringify(…, null, 2)` while the repo convention established during the audit is compact separators (`convert_csv_transactions.py:147`, `generate_synthetic_data.py:322`, fix #12) — ~2× size inflation on files that API routes parse per cold start — fix: `JSON.stringify(x)` (compact), matching siblings.

**[P3] [NEW]** `mule-detection/scripts/generate-synthetic-data.ts:85-95,101,320` — robustness bundle: `JSON.parse(raw)` cast to `RawAccount[]` unchecked (:101) so schema drift surfaces as silent `undefined` math, which `JSON.stringify` serializes as literal `null` (e.g. NaN `riskScore`) rather than erroring; `weightedRandomChoice` on an empty array reads `weights[-1]` → NaN totalWeight → returns `undefined` account, crashing later with an unrelated TypeError (:86-94); and `writeFile` non-atomically overwrites live-consumed `public/*.json` — a truncated write poisons the three API routes whose loaders swallow parse errors into empty arrays (`data-local/route.ts:30-33`) rendering "no data" with no error — fix: validate parsed shape (assert non-empty + key fields), guard empty-input in `weightedRandomChoice`, and write temp-file + rename.

**[P3] [NEW]** `mule-detection/scripts/generate_synthetic_data.py:59-65,78-85,88,93,95,298` ↔ `generate-synthetic-data.ts:123-129,157,162,166` — same-named generator concepts diverge with no shared constants: transaction count 50,000 vs 8,000; window 30d vs 180d; flag rules (mule ∨ risk≥0.75 ∨ amount>₹10k ∨ 5% random vs mule ∨ risk>70); id formats `txn_000001` vs `TXN0000001`; timestamps naive-local vs UTC-Z; and the Python side still emits the legacy vocabulary `routine|normal|high_value|suspicious_transfer` (:63-65) — rails alignment (fix #8) was applied only to the TS side — impact contained because py writes the dead `synthetic_dataset.json` (known #22), but any revival re-fragments the schema — fix: extract one shared spec (counts, windows, thresholds, vocab, id format) or archive the py generator per the existing owner-call recommendation.

**Verified-clean within the slice:** emitted Alert shape matches `mapAlert()`/`RawAlert` field-for-field (`normalizers.ts:125-135,233-250`; all type/severity/status literals ∈ valid sets); `risk_score` handling is correctly on the 0–100 scale end-to-end (dataset range 5.3–85.6 sampled; output clamped [0,100] at :155/:169, consumers expect 0–100); `RawAccount` interface matches all 37 dataset keys exactly (sampled union); outputs are NOT dead — both files consumed by `api/data-local`, `api/analytics`, `api/alerts/count`, `api/graph/mule-galaxy`; emitted fields cover every consumer read (`from/to/amount/timestamp/type/flagged/riskScore` incl. mule-galaxy's `timestamp.slice(0,10)` day bucketing); binary-search in `weightedRandomChoice` is correct for non-decreasing cumulative weights.

TOTALS sev=P0:0,P1:1,P2:1,P3:10 status=NEW:10,UNFIXED:2,REGRESSION:0