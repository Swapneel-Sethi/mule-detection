# D1-5 [UI/UX-NAV] Files: mule-detection/src/components/Sidebar.tsx, SidebarOverlay.tsx, ui/FilterBar.tsx, ui/PageHeader.tsx, ui/RiskBadge.tsx. Focus: drawer focus trap/Esc/scroll-lock/inert pre-hydration, active-route detection, FilterBar filter-value sentinel ('' vs 'all') vs every consumer's reset value, RiskBadge unknown-level + case sensitivity, version-string claims.
(agent_id=agent-62)

 outcome="completed">Audit complete. All slice files read line-by-line, baseline cross-checked against SWARM36 master + old UI/UX report + defect register, every cross-file contract verified (FilterBar consumers ×3, tokens, versions, layout padding, route existence, tsc exit 0).

---

**[P2] [NEW]** `mule-detection/src/components/Sidebar.tsx:24-30` + `SidebarOverlay.tsx:15-69` — breakpoint-crossing modal leak: opening the drawer below 1024px then crossing to ≥1024px (e.g. iPad portrait→landscape rotate) leaves the modal machinery permanently armed into desktop mode where the aside is a persistent sidebar — `document.body.style.overflow="hidden"` (SidebarOverlay.tsx:23) and the capture-phase Tab/Esc trap (:58) persist because the effect keys only on `isOpen`; the mq listener sets only `isMobileViewport`, never closes. Result: page unscrollable with zero visible escape (overlay `lg:hidden` :77, hamburger `lg:hidden`), keyboard Tab wraps forever inside 6 nav links, and the trap's focusable list matches the display-none close button (`lg:hidden`, Sidebar.tsx:74) via `button:not([disabled])` (SidebarOverlay.tsx:41) — visibility never checked — whose `.focus()` silently no-ops. Also stale `aria-expanded=true` on the hidden hamburger. — Evidence: deps `[isOpen, onClose]` (:69); grep confirms nothing calls `closeDrawer` on resize. — Fix: in the mq `update` handler add `if (!mq.matches) setIsOpen(false)`; filter focusables by `el.offsetParent !== null` (or `checkVisibility()`).

**[P3] [NEW]** `mule-detection/src/components/SidebarOverlay.tsx:7-11,74-82` — open drawer announced to AT as plain navigation, not a modal: rendered backdrop is `role="presentation" aria-hidden="true"` and the aside keeps `role="navigation"` (Sidebar.tsx:64) with no `role="dialog"`/`aria-modal="true"`, and `<main>` gets no `aria-hidden`/`inert` while open → SR browse mode can read/activate background content that sighted keyboard users are locked out of; docstring claims "when open this is a modal dialog". The pre-rewrite code had `role="dialog" aria-modal="true"` (old audit H6 quotes it); the rewrite dropped both semantics without replacement while adding the trap. — Fix: when `isOpen && isMobileViewport`, put `role="dialog" aria-modal="true" aria-label="Main navigation"` on the aside, or toggle `aria-hidden`/`inert` onto `<main>`.

**[P3] [UNFIXED]** `mule-detection/src/components/Sidebar.tsx:22,66-67` — pre-hydration off-screen drawer still tabbable on mobile (SWARM36 reported-only #28): `isMobileViewport` initializes `false`, so SSR HTML ships no `inert`/`aria-hidden` and until hydration a keyboard user tabs through the `-translate-x-full` invisible nav. — Fix: CSS-based guarantee independent of hydration — add `invisible` in the closed branch plus `lg:visible` (visibility transitions discretely, preserving the slide), or `useSyncExternalStore` for matchMedia pre-hydration.

**[P3] [UNFIXED]** `mule-detection/src/components/ui/RiskBadge.tsx:19` — orphaned component: zero importers anywhere (`grep -rn "RiskBadge" src docs *.md` → only its own definition); AlertsContent.tsx:90-97 still hand-rolls severity coloring. Known M3 aspect; swarm fixed internals (#10) but adoption never happened, leaving dead export + duplicated severity styling. — Fix: adopt in AlertsContent's Severity cell and Dashboard's category chip, or delete the file.

**[P3] [UNFIXED]** `mule-detection/src/components/Sidebar.tsx:111` — footer claims `v2.4` while `package.json:3` says `"version": "0.1.0"` (backend reports 2.4.0 — master theme I "Version chaos"). Known #26, owner decision still pending. — Fix: single source of truth — generate a `const APP_VERSION` from package.json at build time, or drop the badge.

**[P3] [UNFIXED]** `mule-detection/src/components/ui/PageHeader.tsx:18` — subtitle still hardcodes `text-[11px]`, duplicating `--text-caption: 11px` / `--text-label: 11px` (globals.css:48,50): half of swarm-fixed claim #11 which named **both** `text-[32px]`/`text-[11px]`; only the title got `text-heading` (line 13 ✓). — Fix: `text-caption` (or `text-label`) on the subtitle `<p>`.

**[P3] [NEW]** `mule-detection/src/components/Sidebar.tsx:87-89` — active-route detection lacks a segment boundary: `pathname.startsWith(item.href)` matches prefix-stems, so any future sibling like `/alerts-archive` or `/accounts-tools` highlights two items simultaneously; latent today (no current collision among `/`, `/accounts`, `/transactions`, `/graph`, `/alerts`, `/analytics`). — Fix: `pathname === item.href || pathname.startsWith(item.href + "/")`.

**[P3] [NEW]** `mule-detection/src/components/SidebarOverlay.tsx:22-23` — scroll lock without scrollbar-gutter compensation: on classic-scrollbar platforms (narrow <1024px window on Windows/Linux) opening the drawer removes the ~15px gutter → whole-page horizontal layout shift each open/close. — Fix: add `padding-right: (window.innerWidth - document.documentElement.clientWidth)px` to body while locked, or `scrollbar-gutter: stable` on `html`.

**[P3] [UNFIXED]** `mule-detection/src/components/Sidebar.tsx:61` — `w-[200px]` magic number duplicated with `layout.tsx:38` `lg:ml-[200px]` (known #29); changing sidebar width silently desyncs content offset. — Fix: define once (e.g. `--sidebar-w: 200px` in `@theme`, consumed as `w-(--sidebar-w)` / `lg:ml-(--sidebar-w)`).

TOTALS sev=P0:0,P1:0,P2:1,P3:8 status=NEW:4,UNFIXED:5,REGRESSION:0

CLEAN: `mule-detection/src/components/ui/FilterBar.tsx` — sentinel contract verified sound: all three consumers (AccountsContent:22/99, AlertsContent:17-18/165-184, TransactionsContent:17/192) reset to `"all"` == `options[0].value`; ids via `useId`; custom arrow drawn in reserved `pr-8`; Clear properly gated on `onClear && hasActiveFilters`; `transition-default` confirmed a real `@utility` (globals.css:78); tsc `--noEmit` exit 0.

Verified-fixed baseline (not re-reported): overlay scroll-lock + forward-Tab leak (#6), Sidebar callback stability (#7), RiskBadge unknown-level/case/aria-label (#10), FilterBar `hasActiveFilters`+Clear guard+ids+arrow (#12/#13), hamburger/title overlap (old H5, `max-lg:pt-14`), version-tag & placeholder contrast (old H8/L1), skip-link target (old #5), reduced-motion (old #18).