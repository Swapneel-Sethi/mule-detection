# D6-4 [SEC-BACKEND] Files: mule-detection/backend/main.py, backend/requirements.txt. Focus: CORS wildcard + credentials, missing auth/rate-limit on mutating endpoints, sklearn/joblib pickle-load RCE surface if models uploaded, input validation (negative amounts), sync blocking calls in async handlers, dependency pinning (requirements unpinned?), whether anything in the Next app even calls this service (grep fetch/axios to :8000).
(agent_id=agent-91)

 outcome="completed">Audit complete. All evidence gathered — writing up the D6-4 backend findings.

## D6-4 [SEC-BACKEND] Findings

**Known-baseline disposition:** every Domain-6 "Fixed" item for `backend/main.py` was re-verified present at HEAD (Query ge/le on limit, parallel-edge accumulation, sync handlers, unified `active_alerts`, `logger.warning` replaces silent pass, `_parse_ts` Z-tolerance, loopback bind default, GET-only methods, cached `/api/analysis`, version dedup) — **no regressions**. Register S19's CORS half is fixed; its rate-limit half is not (below).

- **[P2] [NEW]** `mule-detection/backend/main.py:1-599` — entire FastAPI service is orphaned: zero consumers anywhere in the repo — grep across `src/`, `package.json` scripts (`dev/build/start/lint/typecheck` — no backend script), `*.md` docs, `.env.example`, and root helpers finds **no reference** to `:8000`, `uvicorn`, `FastAPI`, or `MULEGUARD_*` outside `main.py` itself (only graphify-out index artifacts mention it); the Next app reads precomputed JSON from `public/*.json` directly — this is a second unauthenticated, unrate-limited HTTP API serving fabricated financial data (~600 lines incl. mock generator + NetworkX engine) that nothing calls — delete the directory or wire it up deliberately.
- **[P3] [UNFIXED]** `mule-detection/backend/main.py:483-591` — no auth and no rate limiting on any endpoint (register S19's remaining half) — all 12 routes are open GETs; contrast the Next-side proxy limiter which protects only Next routes — add SlowAPI/simple token-bucket middleware if the service is kept at all.
- **[P3] [NEW]** `mule-detection/backend/main.py:28-33,41` — `MULEGUARD_CORS_ORIGINS="*"` env override silently recreates the wildcard+credentials combo the audit fixed: `ALLOWED_ORIGINS` accepts `*` verbatim, and Starlette echoes the request Origin when `allow_all_origins ∧ allow_credentials=True`; `allow_credentials=True` is itself pointless (no cookies/auth exist) — reject/strip `*` during env parsing or set `allow_credentials=False`.
- **[P3] [NEW]** `mule-detection/backend/main.py:139-144,148-150,183` — mock-generator thresholds contradict the dataset conventions the adjacent comments claim to mirror — comment says status semantics "match accounts_dataset.json" yet backend uses `under_review` iff `risk_score>=40`, while dataset truth is `under_review` ⟺ mule ⟺ calibrated ≥~55.1 (measured: `under_review` min=56.0, n=8,578 = exact mule count); risk_level bands use 80/60/40 vs canonical 67.1/64.0/55.1 (dataset bands: critical min=67.2, high 64.8–66.1, medium ≤63.7); txn `flagged` uses `risk>70` vs dataset flagged rule ≥~40.8 — align thresholds or delete the false-parity comments.
- **[P3] [NEW]** `mule-detection/backend/main.py:9,135-187` — mock data unseeded: `random.random()`/`randint` at import make every restart serve different accounts/txns/stats, breaking reproducibility (repo convention elsewhere is seed 42) — `random.seed(42)` at module top.
- **[P3] [NEW]** `mule-detection/backend/main.py:528-545,572-591` — analysis siblings uncached while `/api/analysis` caches: `/centrality`, `/communities`, `/risk-scores`, `/patterns`, and `/stats` (via `_compute_patterns`) recompute O(VE) betweenness / modularity / full pattern scan per request — cache like `_cached_analysis` or derive from it.
- **[P3] [NEW]** `mule-detection/backend/main.py:483-485,528-569,572-590` — 7 of 12 routes lack `response_model` (root, centrality, communities, risk-scores, patterns, graph, stats) → unvalidated dict output; the swarm's "response_models enforced" fix is incomplete — add typed response models.
- **[P3] [NEW]** `mule-detection/backend/main.py:190-200` — `_parse_ts` drops `tzinfo` instead of normalizing to UTC: an offset-bearing stamp (`+05:30`) silently misorders against `Z` stamps by its offset (09:00+05:30 = 03:30Z compares later than 05:00Z), corrupting rapid-movement windows; latent (all current stamps are Z) — convert aware values via `astimezone(timezone.utc)` before stripping.
- **[P3] [NEW]** `mule-detection/backend/main.py:330-353` — `nx.simple_cycles` is consumed to exhaustion before the `max_length` filter helps: exponential cycle enumeration on dense graphs (DoS-perf trap; safe only because the engine is fed 80 mock txns) — replace with length-limited DFS or bound input size.
- **[P3] [NEW]** `mule-detection/backend/requirements.txt:1` — top-level pins are exact (fastapi==0.115.0 etc., answering the "unpinned?" concern: they ARE pinned) but transitives float: fastapi 0.115.0 resolves starlette <0.39, excluding the CVE-2024-47874 fix in 0.40.0 — currently unreachable (GET-only, no multipart, `python-multipart` absent) — add a hash-locked freeze (`pip freeze` / `uv lock`) if kept.
- **[P3] [NEW]** `mule-detection/backend/main.py:557` vs `:412` — same field name `risk_score` means static account score in `/api/graph` but computed heuristic graph score in `/api/analysis` nodes — rename one (`graph_risk_score`) or document.

**RCE surface verdict (spec focus):** clean — zero matches for `pickle|joblib|sklearn|torch|UploadFile|@app.post|Body(` in `backend/`; sklearn/numpy were removed from requirements (Domain-6 fix #16 holds); no model-upload path exists, so no deserialization trust boundary.

TOTALS sev=P0:0,P1:0,P2:1,P3:10 status=NEW:9,UNFIXED:1,REGRESSION:0