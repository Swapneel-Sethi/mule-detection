# D2-2 [ML-ENGINE-2H] File: mule-detection/src/lib/detectionEngine.ts LINES 950-1844. Focus: alert generation thresholds vs documented blind-set stats, explanation/factor contribution bounds, Markov/state features keyed on flags the engine actually emits, dedup keys, performance (O(n^2) loops over 8k+ accounts), exported API surface used by pages.
(agent_id=agent-65)

 outcome="completed">All verification complete — dataset schema sampled, counterpart contracts checked, callers grepped. Composing the final defect report for lines 950–1844.

---

## DEFECT AUDIT — `mule-detection/src/lib/detectionEngine.ts` L950–1844

Baseline: `audit/07-SWARM36-MASTER.md` (Domain 2), `audit/02-ml-model.md`; `DEFECT_REGISTER.md` ML-MODEL section is a placeholder. Swarm fixes verified intact at HEAD (rapid-movement pairing, contribution clamping, FLAG_THRESHOLD rewiring, alias normalization, markov leakage fix) — no regressions found.

[P1] [NEW] mule-detection/src/lib/detectionEngine.ts:1720 — `scoreAllTransactions` receives **raw** accounts, but every shipped row keys on `account_id` (zero rows have `id`), so its lookup map (`transactionScorer.ts:305-308`, keyed by `acc.id`) misses for all 99,952 txns → every transaction scored with training-default `calibrated_score=0.3`/`risk_score→0.1`, collapsing 6 of 16 model features to constants — evidence: python sample of `public/accounts_dataset.json`: `rows missing id: 105501 | rows with account_id: 105501`; `runDetection` builds `normalizedAccounts` with `id` at :1422-1431 but passes `rawAccounts`; contradicts transactionScorer.ts:49-52 docstring "production wiring — detectionEngine passes raw dataset accounts, which always carry these fields" — fix: pass `normalizedAccounts` instead of `rawAccounts`.

[P1] [NEW] mule-detection/src/lib/detectionEngine.ts:1543-1550 — account-XGBoost inputs `in_txn_count`/`out_txn_count`/`avg_in_amount`/`avg_out_amount` are recomputed from graph **degree** (unique counterparties) although the dataset ships trained-scale `in_txn_count`≠`unique_senders` columns, duplicating `unique_senders`/`unique_receivers` and skewing 4/16 features — evidence: python over 20,000 rows: `in_txn==unique_senders AND out_txn==unique_receivers: 6194 | differing: 13806` (e.g. ACC000117A1 out_txn=14 vs unique_receivers=13); engine sets `in_txn_count: inDeg` where `inDeg` **is** `unique_senders` (:1544), contradicting its own :1520-1525 "prefer the dataset-provided columns … feeding proxies would be train/serve skew" convention — fix: read `finite(account.in_txn_count)`/`account.out_txn_count` (and dataset `avg_in_amount`/`avg_out_amount`) with graph-degree only as fallback.

[P2] [NEW] mule-detection/src/lib/detectionEngine.ts:1262-1263 — first two explanation factors use ghost feature keys `"fan_in"`/`"fan_out"` but the features record emits `is_fan_in`/`is_fan_out` (:1015-1016) → `features["fan_in"]` is `undefined`, gate `value && value > 0` fails, and "Receives funds from multiple sources" (w 0.6) / "Distributes funds to multiple recipients" (w 0.6) **never fire** in any account explanation or top-3 summary — evidence: key diff between :1262-1263 and :1015-1016 — fix: rename to `is_fan_in`/`is_fan_out`.

[P2] [NEW] mule-detection/src/lib/detectionEngine.ts:1505-1513 — per-account loop rescans the full transaction list inside `extractEnhancedFeatures` (`transactions.filter(...)` at :864) plus ~10 sub-filters, O(N_accounts×N_txns) ≈ 105,501 × 99,952 ≈ **1.05e10** iterations per run (hours-scale); `clusterSize` IIFE (:1681-1686) adds O(A×Σ members) membership scans — evidence: call site :1511 with 105,501 accounts / 99,952 txns (dataset counts above) — fix: pre-index txns per account once (`Map<string, Transaction[]>`) before the loop.

[P2] [NEW] mule-detection/src/lib/detectionEngine.ts:1772-1775 — `validTransactions.find(t => t.id === txnId)` inside `flatMap` over flagged txns is O(H×T); with FLAG_THRESHOLD=0.3 flagging ~13–18% of 99,952 txns (scorer docstring figure) that is ≈1.3–1.8e9 linear-scan comparisons — evidence: `.filter(([,score]) => score.flagged)` yields H≈13–18k against T=99,952 — fix: build `Map<txnId, Transaction>` once and look up.

[P2] [NEW] mule-detection/src/lib/detectionEngine.ts:1786 — pattern-alert IDs collide: `circular_transfer` patterns carry no `account/target_account/source_account` (built at :343-352), so every cycle alert gets `PATTERN-circular_transfer-undefined-<same ms>`; multiple rapid_movement hits for the same account within the same millisecond also collide (dedup key at :256 admits several per account) — evidence: id template `${pattern.pattern}-${pattern.account || pattern.target_account || pattern.source_account}-${Date.now()}` evaluated in one synchronous loop — fix: append a pattern-sequence index (or `crypto.randomUUID()`).

[P2] [NEW] mule-detection/src/lib/detectionEngine.ts:1471-1482, 1728-1738 — `allPatterns.slice(0, 100)` truncates by detector order, so once fan-in/fan-out (unbounded, ≥3 sources on most of 105k nodes) fill the quota, structuring/night_owl/burst/automated/pass-through patterns are silently dropped from `accountPatterns`, analyst reports and alerts; summary then prints mutually contradictory counts (`patterns_found` ≤100 vs `fan_in_patterns`/`rapid_movements` etc. summed pre-slice) — evidence: `.slice(0, 100)` at :1482 applied after concatenation while :1729-1738 report untruncated arrays — fix: raise/remove the cap or slice per detector; derive `patterns_found` consistently.

[P3] [NEW] mule-detection/src/lib/detectionEngine.ts:1620-1621 vs 1600-1603 — flag taxonomy disagrees with risk bands: `critical_risk` at calibrated ≥0.70 while `risk_level` "critical" starts at 0.71 (an account at 0.705 is flag-critical but band-high), and `high_risk` at ≥0.50 vs "high" band at 0.66 — evidence: literal thresholds in adjacent lines — fix: derive both from one shared constants table.

[P3] [NEW] mule-detection/src/lib/detectionEngine.ts:1697-1703 (dup 1653-1659) — non-fan, non-transit mules are force-labeled `mule_type: "pass_through"` even when `is_pass_through` is false — evidence: final else branch of the ternary returns `"pass_through"` unconditionally — fix: add an explicit `"other"/"isolated"` fallback.

[P3] [NEW] mule-detection/src/lib/detectionEngine.ts:1373-1378 — red flag says "**% of outgoing funds** go to a single recipient" but `beneficiary_concentration` is a share of outgoing **txn count** (:955-964), and reportGenerator.ts:128 repeats the "% of funds" claim; the :955-957 comment asserting "Matches the red-flag/report copy" is therefore false — evidence: `recipientCounts` counts transactions; copy says funds — fix: reword copy to "of outgoing transfers" or switch metric to amount share.

[P3] [NEW] mule-detection/src/lib/detectionEngine.ts:1359-1364 — spike red flag says "7-day transaction **volume** is X.Xx the 180-day baseline" but `velocity_ratio_7d_180d` is a txn-**count** ratio over a 7.2-day-equivalent window (`window7d.length/(window180d.length/25)`, :877) — evidence: counts, not amounts, at :873-877 — fix: reword to "transaction count/activity".

[P3] [NEW] mule-detection/src/lib/detectionEngine.ts:1390 — community red flag quotes "`${community_score*100}`% internal density" but `community_score = min(1, density*2 + fastFlowRatio)` (:565) conflates density and flow speed, overstating density up to ~2× — evidence: formula vs copy — fix: quote `density` from pattern details or relabel "cluster risk index".

[P3] [NEW] mule-detection/src/lib/mlModel.ts:231-232 (cross-file vs detectionEngine.ts:1600-1603) — calibrateScore docstring still claims risk bands "medium/high/critical at 0.551/0.640/0.671 are likewise untouched" while the engine's ITER-2 retune moved high/critical to 0.66/0.71 — evidence: side-by-side literals — fix: update the mlModel docstring (counterpart file; not edited here).

[P3] [NEW] mule-detection/src/lib/detectionEngine.ts:1292 — `const value = features[signal.feature] as number` casts **boolean** features (`is_transit`, `is_pass_through`) to number; the factor pipeline only works via implicit boolean→number coercion (`true > 0`, `Math.min(1, true)`) — evidence: features emit booleans at :1015-1017, :1038 — fix: branch on `typeof value === "boolean"` (value: 1|0) before the numeric path.

[P3] [NEW] mule-detection/src/lib/detectionEngine.ts:1803-1816, 1485-1496 — `centralityApproximation` divides degree by `n*0.5` (n=105,501 → divisor 52,750), so centrality ≈ 0 for every node at production scale; the `c*100` term in PageRank seeding and the CENTRALITY component of `calculateRiskScores` are numerically dead — evidence: formula vs dataset n; contrast blind-set comments assuming meaningful centrality — fix: normalize by max observed degree, not graph size.

[P3] [NEW] mule-detection/src/lib/detectionEngine.ts:109-113, 121, 144 — exported `DetectionResult` references non-exported `Alert`/`UpdatedAccount` interfaces, so external consumers cannot name result member types — evidence: `interface Alert {` / `interface UpdatedAccount {` lack `export` while `export interface DetectionResult` uses them — fix: export both.

[P3] [NEW] mule-detection/src/lib/detectionEngine.ts:981 vs 708 — burst definition inconsistent inside the file: feature extractor uses strict `< 300000` ms while `detectBurstActivity` uses `diffMin <= 5` (inclusive), so a gap of exactly 5 min counts as a burst in one and not the other — evidence: `if (diff < 300000)` vs `if (diffMin <= burstWindowMinutes)` — fix: share one constant/comparison.

[P3] [NEW] mule-detection/src/lib/detectionEngine.ts:1171-1191, 1240 — the whole pass-through/cycle signal family gates on `balance` believing it is retained funds, but the dataset stores net flow (tin−tout), negative for 89,803/105,501 rows (Swarm36 D4#24), making the `balance < totalIn*0.1` retention guard (:892/:469) vacuously true and pass-through effectively a ratio-only test — evidence: audit D4#24 counts + in-slice consumers weighting `is_pass_through` at 1.0 — fix: clamp negatives / require balance ≥ 0 before treating as retention.

[P3] [UNFIXED] mule-detection/src/lib/detectionEngine.ts:887-888 → 1195, 1292-1312, 1366-1372 — divide-by-zero sentinel `999` (credit/debit ratios, in-out ratio) still flows into behavioral volume signals (>3 always true), explanation factor values and the "Abnormal credit-to-debit ratio" red flag/reason — evidence: `creditCount > 0 ? 999 : 1` literals; Swarm36 D2 reported-only #41 — fix: emit null-safe flag (e.g. cap at a documented bound and suppress narrative for sentinel).

[P3] [UNFIXED] mule-detection/src/lib/detectionEngine.ts:1641-1645 — Markov evolution still fed exactly one current-timestamp observation → trajectory is always the deterministic "Single observation…stable" echo — evidence: array literal wraps `[current]` only; Swarm36 D2 #40 — fix: feed per-day history or hide the panel for <2 observations.

[P3] [UNFIXED] mule-detection/src/lib/detectionEngine.ts:1771 — ML-HIGHRISK alert severity `riskScore >= 70 ? "critical" : "high"` remains unreachable: documented transaction-model max ≈66.3 — evidence: transactionScorer.ts:42 blind-set stats; Swarm36 D2 #41 — fix: calibrate the cut to the measured distribution (e.g. p99≈12.6 or max-band).

[P3] [UNFIXED] mule-detection/src/lib/detectionEngine.ts:1563-1564 — min-max normalization window `[0.262, 0.466]` still hard-coded literals (drifts on retrain; out-of-window scores silently pin to 0/1) — evidence: `const ML_SCORE_MIN = 0.262; const ML_SCORE_MAX = 0.466;` — fix: ship train-time min/max in model_weights.json.

[P3] [UNFIXED] mule-detection/src/lib/detectionEngine.ts:1553-1555 — `hub_score ← pagerankFeature` / `authority_score ← betweenness_centrality` proxy fallback persists (audit-02 HIGH); mitigated in practice because all 105,501 dataset rows ship `hub_score`/`authority_score` (verified: coverage 105,501/105,501), leaving the skew on the fallback path only — fix: drop proxies, fail loudly when columns missing.

[P3] [UNFIXED] mule-detection/src/lib/detectionEngine.ts:1558-1560 — catch-path fallback `mlScore()` remains the documented near-constant [0.073, 0.111] heuristic indistinguishable from real model output (Swarm36 D2 #39) — fix: surface `{score, source}` provenance instead of silent substitution.

TOTALS sev=P0:0,P1:2,P2:5,P3:17 status=NEW:18,UNFIXED:6,REGRESSION:0

CLEAN (segments of the slice reviewed with no defects found): mule-detection/src/lib/detectionEngine.ts:955-1074 (feature assembly tail incl. redesigned beneficiary_concentration math), 1106-1208 (ensemble-weight block + sharpened behavioral scorer), 1210-1243 (graph/temporal/community scorers), 1413-1504 (alias normalization, graph build, detector wiring), 1749-1780 (ML-HIGHRISK alert body minus the find() loop), 1801-1844 (legacy helpers minus centrality scaling).

Verification performed: schema sampling of `public/accounts_dataset.json` (92.5 MB) and `public/transactions_synthetic.json` (18 MB) via short python snippets; grep confirmed `runDetection` has zero callers (matches the module-header "unreachable" notice — systemic theme D, not re-reported). `npx tsc --noEmit` not re-run; all reported issues are runtime-semantics defects that type-checking passes by construction (legal casts/coercions), and the Swarm36 log records exit 0 at audit close.