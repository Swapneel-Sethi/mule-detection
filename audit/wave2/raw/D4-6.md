# D4-6 [DATA-ARTIFACT-INTEGRITY] Machine-verify shipped artifacts (python sampling only): public/accounts_dataset.json (92MB), alerts_synthetic.json (60KB). Verify: top-level shape matches what src/app/api/data-local/route.ts expects (field names EXACT), risk_level enum values casing, risk_score 0-100, is_mule boolean vs 0/1, account id uniqueness, alert.account_id referential integrity vs accounts, kyc_status values present (ML comment claims absent?), date formats ISO-8601 vs epoch mix, counts vs numbers quoted in UI copy/mockData. Report every mismatch with measured values.
(agent_id=agent-81)

 outcome="completed">All checks complete. Compiling the final defect report.

---

## D4-6 DATA-ARTIFACT-INTEGRITY — Findings (worst-first)

Verified by Python sampling of both artifacts against `src/app/api/data-local/route.ts`, `src/lib/normalizers.ts`, `useLocalData.ts`, filter configs, and UI copy. Baseline: `audit/07-SWARM36-MASTER.md` + Domain-4 sections of register/report.

**Held fixes confirmed at HEAD (not re-reported):** top-level array shapes match route's `JSON.parse as Record<string,unknown>[]`; 105,501 unique `account_id`s (0 dupes); `is_mule` strict boolean 8,578/96,923; `risk_score` all numeric 5.3–85.6 (0–100 ✓); `risk_level` lowercase enum matches route/normalizer, and band boundaries respect calibration thresholds (low ≤28.3 < 55.1 ≤ medium 56.0–63.7 < 64.0 ≤ high 64.8–66.1 < 67.1 ≤ critical ≥67.2); 0 month-13 dates; 0 chronology inversions; `reasons` bijectively mirror `flags`; alert→account refs 171/171 resolve; alert ids unique; alert timestamps ISO-8601 UTC within 2026-08-15…22; alert `transactions[]` refs stripped-empty as documented; KYC comment corrected (`detectionEngine.ts:1539`); `runDetection` alias map covers `account_age_days` (`detectionEngine.ts:1429`).

- [P2] [UNFIXED] mule-detection/public/accounts_dataset.json (all ACC rows, col `pass_through_ratio`) — ratio column spans 0 → 1.173e10, breaking every 0–1-semantics consumer — buckets: 17,751 rows ≤1, 65,334 in 1–100, 1,198 in 100–1e6, **21,218 rows >1e6**, max 1.173e10; e.g. `ACC00004BEC`: ratio 1,978,829,000 = `total_out_amount 19788.29 ÷ 1e-5` exactly (epsilon divide); consumers assume ~1-centered ratio: `scripts/generate_dataset_json.py:67` flags pass_through only in 0.85<r<1.15, `src/lib/mlModel.ts:53` threshold 0.85, `scripts/recompute_ml_scores.py:126` feeds raw value as XGBoost feature #12 — already flagged in `audit/swarm36_digest.txt:516` but absent from master/register and still present — fix: regenerate ACC rows as `tout/tin if tin>0 else 0` (ACM producer `build_real_mules.py:135` is correct, ACM max 1.497) or clamp+reflag.
- [P2] [UNFIXED] mule-detection/public/accounts_dataset.json (7,001 ACM rows) — `account_age_days` key absent on every ACM mule, XGBoost feature #0 — key census: present on 98,500/105,501 rows, missing exactly on all 7,001 `is_mule:true` ACM rows (known D4 reported-only #23) — fix: backfill from earliest txn timestamp or explicit sentinel documented in normalizer.
- [P2] [UNFIXED] mule-detection/public/accounts_dataset.json (col `txn_velocity_per_day`) — dual incompatible formulas by family under one column/consumer cutoff — ACC range 0–1116 (txns/180 style) vs ACM 0–0.1389 (degree/365 style); any cutoff (e.g. galaxy rapid-mover `>0.05`) classifies families on different scales (known D4 #25) — fix: recompute ACM rows with the ACC formula.
- [P3] [UNFIXED] mule-detection/public/accounts_dataset.json (40 ACM rows) — empty `firstSeen` renders blank date after normalization — measured 40 rows, all mules, `firstSeen:""` and also missing `account_age_days`+`explanation`; `normalizers.ts:186-197` falls through age-based branch only when age exists → yields `""` (known M3 partial; month-13 half was fixed, empties weren't) — fix: backfill dates for these 40 stubs.
- [P3] [UNFIXED] mule-detection/public/accounts_dataset.json (98,500 ACC rows) — `lastActivity` hardcoded `"2026-08-22"` on 93.4% of rows — distinct-value census: 98,500 identical vs 182 real values on ACM rows (known M3) — fix: derive per-account from max txn timestamp.
- [P3] [UNFIXED] mule-detection/public/accounts_dataset.json (`flags`) — dual flag vocabulary persists in one array — ACC mules: `fan_in` 32,930 / `fan_out` 98,231 / `high_velocity` 11,679 / `dormant` 48; ACM mules: `fanin_receiver` 3,417 / `fanout_source` 3,532 / `circular_loop` 679 / `pass_through` 5,095 (known H3; `pass_through_pattern` rename itself held) — fix: canonicalize ACM names to ACC vocabulary data-side.
- [P3] [UNFIXED] mule-detection/public/accounts_dataset.json (col `graph_score`) — mixed scales by family — ACC 0–5 (integers) vs ACM 0–2.5 continuous (known D4 #26) — fix: rescale to one convention.
- [P3] [UNFIXED] mule-detection/public/accounts_dataset.json (7,001 ACM rows, col `pagerank`) — pagerank identically 0 for the entire ACM family (max 0.0 vs ACC max 0.0873) — graph features meaningless for 6.6% of data incl. all high-ML-score mules (consequence of stub backfill-as-0, master D4 #16 / D2 #41) — fix: run the graph pass over full 105,501-node universe.
- [P3] [UNFIXED] mule-detection/src/components/AlertsContent.tsx:165-172 — severity filter offers `low` AND `info`, neither occurs in the 155-alert artifact — measured severity census: medium 129 / high 25 / critical 1 / low 0 / info 0 (register item named Info only; Low is equally dead) — fix: drop both options or generate low-sev alerts.
- [P3] [NEW] mule-detection/src/components/AlertsContent.tsx:178-185 — status filter offers `dismissed`, matched by 0/155 alerts — measured status census: new 61 / investigating 46 / resolved 48 / dismissed 0; no code path ever emits `dismissed` — fix: remove option until a dismissal flow exists.
- [P3] [UNFIXED] mule-detection/src/app/api/data-local/route.ts:209 — `a.resolved === true` disjunct is dead — field absent on all 155/155 alerts (key census) (known D4 #32) — fix: drop the boolean check now that schema has no `resolved`.
- [P3] [UNFIXED] mule-detection/src/components/DashboardContent.tsx:54-63 — fallback recomputes diverge from API stat definitions — `muleCount` fallback counts only severe mules (isMule && critical/high = 1,962) vs route's all-mules (`route.ts:173`, 8,578); fires whenever `stats.muleCount` is missing (known A34; latent because route always emits it) — fix: make fallback `isMule` alone, mirroring `route.ts:173`.
- [P3] [NEW] mule-detection/src/lib/useLocalData.ts:7-12 — docstring justifies local `ApiTransaction` type by a mock/demo union ("transfer"/"payment"/…) that no longer exists — `mockData.ts:23` was fixed to the rails union (`upi|imps|neft|rtgs`) and this hook never imports mock runtime data anyway — fix: rewrite comment to reference the API payload shape only.
- [P3] [NEW] mule-detection/src/lib/xgboostPredictor.ts:210-213 — comment claims `kyc_status`/`account_type`/`authority_score` are "unavailable … conservative defaults", contradicting the artifact — all three ship on 100% of rows (`kyc_status` '1'×83,542/'0'×21,959; `account_type` '0'/'1'/'2'; `authority_score` numeric on 105,501/105,501); same false claim class as master D2 fixed #10, missed in this file — fix: reword to "defaults apply only when caller records lack these fields".

TOTALS sev=P0:0,P1:0,P2:3,P3:11 status=NEW:3,UNFIXED:11,REGRESSION:0

CLEAN: mule-detection/public/alerts_synthetic.json (artifact level: unique ids, 0 dangling account refs, ISO-8601 UTC timestamps, enums within consumer vocab, internally consistent title/description↔accounts citations); plus artifact-level cleanliness of accounts_dataset.json on: top-level shape, id uniqueness, is_mule typing, risk_score range, risk_level casing/banding, kyc_status presence, date formats (no epoch mix), chronology, flags↔reasons mirroring.