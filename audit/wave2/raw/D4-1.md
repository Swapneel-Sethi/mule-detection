# D4-1 [DATA-GEN-PY] File: mule-detection/scripts/generate_synthetic_data.py. Focus: RNG seeding/reproducibility, distribution parameters vs documented ones, mule-fraction consistency with other generators, timestamp generation tz, amount units, ID collision space, referential integrity guarantees, written-file schema vs normalizers.ts expectations.
(agent_id=agent-76)

 outcome="completed">Audit complete. All checks were read-only: full line review of the target script, statistical sampling of `accounts_dataset.json` (105,501 rows: `risk_score` 5.3–85.6, zero rows <1.0 → the `/100` normalization fix is correct against live data; mules 8,578 = 8.13%; `risk_score>=75` → 1,139 rows, all `critical`), byte-probing of shipped `synthetic_dataset.json` (280.5 MB, sampled 17,568 txn records: 100% of `riskScore` values out-of-range, 476.1–5,896.8 — the pre-fix unit bug is still baked into the shipped artifact), grep proof that no `src/` file references the artifact or the generator's vocabularies, and contract reads of `normalizers.ts` / `mockData.ts` / `convert_csv_transactions.py`.

**Scope:** `mule-detection/scripts/generate_synthetic_data.py` (342 lines, matches HEAD `c1c86e4`, zero importers, output consumed by nothing — consistent with reported-only master item D4#22).

[P2][UNFIXED] mule-detection/scripts/generate_synthetic_data.py:81,144,164,184,200,255,315 — output remains non-reproducible despite seed 42 because all 7 timestamp sites call wall-clock `datetime.now()` (txn/transfer stamps, all 4 alert stamps, `stats.generated_at`), so two runs diverge byte-for-byte; contradicts master-audit D4-Fixed#2's "seeded, byte-stability verified" claim, which cannot hold for this file — evidence: `timestamp = datetime.now() - timedelta(...)` at :81/:255 feeds `isoformat()` into every record; only the `random` stream is deterministic — pin a module-level anchor (e.g. `NOW = datetime(2026, 8, 22, tzinfo=timezone.utc)` or env-overridable) and stamp everything from it

[P2][NEW] mule-detection/scripts/generate_synthetic_data.py:95,267 (and all alert/stats `isoformat()` sites) — timestamps are naive local-time ISO strings with no UTC offset, while the pipeline's documented convention (convert_csv_transactions.py:10-11) is "naive source values treated as UTC, normalized to `...Z`" — the two sibling generators emit the same field name under different implicit timezones, so any future consumer cross-comparing them skews by the local-UTC offset (up to ±5:30 IST) — evidence: `timestamp.isoformat()` produces e.g. `2026-08-25T19:24:03.565123` with no suffix; shipped artifact shows `"timestamp": "2026-08-22T23:16:41.362448"` — emit tz-aware UTC (`datetime.now(timezone.utc).isoformat()`) or append `Z` after treating the anchor as UTC

[P3][NEW] mule-detection/scripts/generate_synthetic_data.py:58-65,243-250 (types), :137,157,178,194 (alert types) — transaction type vocabulary `suspicious_transfer|high_value|normal|routine` and alert types `mule_activity|high_value_transaction|suspicious_pattern|high_risk_accounts` match nothing in the codebase: `mockData.ts:23` union is rails (`upi|imps|neft|rtgs`) and `mockData.ts:30` alert union is `rapid_movement|fan_in|fan_out|circular|behavioral_change|dormant_activation`; grep across `src/` for all 8 strings = 0 hits — the TS sibling got the rails conversion (master D4-Fixed#8) but this Python sibling was left on the dead vocabulary, so any regeneration per D4#22 re-bakes schema-violating records — map types onto the rail enum (or drop the families) before any regen

[P3][UNFIXED] mule-detection/scripts/generate_synthetic_data.py:261-280 vs :87-104 — `transfer_*` records still lack the `flagged` key that every `txn_*` record carries, keeping the schema-asymmetric record families documented in known D4#22; only `from_is_mule`/`to_is_mule` were added (D4-Fixed#10) — evidence: transfer dict literal at :261-280 contains no `'flagged'` entry while :94 emits it for txns; a consumer computing flagged-rates over the combined array silently reads `undefined`→false for ~84% of rows — add `'flagged': transaction_risk >= 0.75 or amount > 10000` to the transfer dict

[P3][UNFIXED] mule-detection/scripts/generate_synthetic_data.py:73 — 5% pure-random flagging (`random.random() < 0.05`) persists, decoupling `flagged` from behavior for ~1-in-20 rows; called out in known C2, still present at HEAD — evidence: line 73 `random.random() < 0.05  # 5% chance of being flagged randomly`; with ~50k txns ≈ 2,500 innocent rows flagged — remove the term (noise flags belong to the scoring layer, not the label)

[P3][NEW] mule-detection/scripts/generate_synthetic_data.py:190-203 — `high_risk_accounts` alert carries `severity: 'low'` while listing exactly the accounts the dataset labels `critical`: measured `risk_score>=75` → 1,139 rows, `Counter({'critical': 1139})` (of 1,943 critical total, so the ad-hoc ≥75 cutoff also strands 804 critical-tier accounts with no rationale); most-severe population tagged lowest severity — evidence: :195 `'severity': 'low'` vs data sample above — set severity from the selected tier (at least `'medium'`/`'high'`) and align the cutoff to a band boundary (≥80) or to `risk_level == 'critical'`

[P3][NEW] mule-detection/scripts/generate_synthetic_data.py:39-44 vs :228-231 — mule amount distributions diverge between the two families (txns: `U(5000,50000) * U(0.5,2.5)` → ₹2.5k–₹125k; transfers: flat `U(5000,50000)` → ₹5k–₹50k) although :234-235 asserts the generators "stay comparable"; the heavier tail exists only in the txn family, biasing any family-level amount statistic — evidence: :40 `amount = base_amount * random.uniform(0.5, 2.5)` absent at :229 — apply the same multiplier in `generate_network_transfers`

[P3][NEW] mule-detection/scripts/generate_synthetic_data.py:118 — dead legacy fallback `.get('type', 'transfer')`: unreachable (every record sets `type` at :92/:279) and carries the retired `'transfer'` value; the identical empty-mode fallback in the sibling converter was migrated to `'upi'` (master D4-Fixed#20) — evidence: line 118 `txn_type = txn.get('type', 'transfer')` — change to `.get('type')` with explicit skip, or `'routine'`

[P3][NEW] mule-detection/scripts/generate_synthetic_data.py:208 vs :295 — dead default `max_transfers_per_account=10` is never exercised (sole call site passes `5`), so the signature advertises twice the actual transfer volume (~5.3/account expected vs ~2.5); docstring states neither window — evidence: `def generate_network_transfers(accounts_dict, max_transfers_per_account=10)` vs `generate_network_transfers(accounts_dict, max_transfers_per_account=5)` — set default to 5 and document the expected volume

Verified clean within this slice: seeding present and convention-matching (`random.seed(42)`, comment at :4 truthful — convert_csv_transactions.py:36 confirmed), `randint` bounds inclusive-correct, self-transfer exclusion correct in both families, `/100` risk normalization matches live data scale, rupee units throughout, `riskScore` emitted 0–100 post-fix, ID schemes collision-free (`txn_%06d` sequential; `transfer_*` suffixed by global counter), referential integrity guaranteed by construction (endpoints drawn solely from loaded account ids), alert `status:'new'` within `VALID_ALERT_STATUSES` (normalizers.ts:45), embedded accounts match `RawAccount` snake_case aliases, `[OK]` ASCII prints cp1252-safe, compact JSON separators, standalone script with zero importers.

TOTALS sev=P0:0,P1:0,P2:2,P3:7 status=NEW:6,UNFIXED:3,REGRESSION:0