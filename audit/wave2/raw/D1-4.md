# D1-4 [UI/UX-PRIM-B] Files: mule-detection/src/components/ui/DataTable.tsx, EmptyState.tsx, ErrorState.tsx, LoadingState.tsx. Focus: sort/keyboard semantics, aria-sort values, row keys, column width contract, locale-aware number rendering (en-IN), live-region correctness, skeleton alignment.
(agent_id=agent-61)

 outcome="completed">Audit complete. All checks done: baseline docs read, four files read line-by-line, token/utility existence verified against `globals.css`, font-var wiring traced through `layout.tsx` (next/font `variable:` classes are unlayered → no cycle; `font-mono`/`font-display` resolve), consumer contracts checked across all 3 `DataTable` call sites, data sampled (99,952 txns → 0 duplicate ids; 155 alerts → 0 missing/dup ids), `npx tsc --noEmit` exit 0. Previously-fixed items verified holding: `transition-default` is a real `@utility`, reduced-motion block present, `Column.width` applied in all three render paths, row-key `""` collision guarded, skeleton header align/width restored, `defaultRender` now `en-IN`, redundant `hoveredRow` state gone, ErrorState double-live-region gone.

---

[P3] [NEW] mule-detection/src/components/ui/DataTable.tsx:35-40,60-96,119-147 — entire sort API, `defaultRender`, and skeleton branch are unreachable dead code: no consumer can exercise the paths Swarm-36 just fixed (#8 width, #21 keys, H7 keyboard, L9 aria-sort) — evidence: grep `sortable|align:|width:|sortBy|onSort|loadingCount` over `src/components` matches DataTable only; all 3 `<DataTable>` blocks (AccountsContent.tsx:104-162, AlertsContent.tsx:189-195, TransactionsContent.tsx:202-207) pass only `columns/data/keyField/caption/emptyMessage`; every one of the 15 columns supplies `render`; nobody passes `loading` (pages gate whole tables behind `<LoadingState>` instead) — delete `defaultRender` + sort machinery or wire real producers; if kept, note the latent gaps: `th` has `tabIndex`/`onKeyDown` but no button role/affordance text for SR users (DataTable.tsx:113-149), `aria-sort` coerces `sortOrder===undefined` to `"descending"` (135-141), and the null-placeholder `text-ash/40` em-dash computes to ≈1.9:1 contrast on `surface-1` (0.4×`#888888` over `#111111` → `#414141`, WCAG 1.4.3 needs 4.5:1)

[P3] [UNFIXED] mule-detection/src/components/ui/EmptyState.tsx:42 (via ErrorState.tsx:21-23) — error variant still announces politely as `role="status"` instead of assertively, the exact item left reported-only in Swarm-36 master Domain-1 #31 — evidence: root hardcodes `role="status"` for all variants including `variant="error"`; `ErrorState` (the failure UI on Dashboard/Accounts/Transactions/Alerts, e.g. AccountsContent.tsx:67) composes it — make root `role={variant === "error" ? "alert" : "status"}` (and update ErrorState.tsx:18-19's accurate-but-now-stale comment)

[P3] [NEW] mule-detection/src/components/ui/DataTable.tsx:85 vs 172-181 — skeleton body cells drop `col.width`, `col.className`, and `col.align` that rendered body cells apply, while skeleton *header* cells keep width/align/headerClassName (71-74) — asymmetric with the Swarm-36 #21 fix intent ("per-column align/bg") — evidence: loading `<td key={col.key} className="px-4 py-3">` has no `style`/`className` interpolation; loaded `<td>` interpolates all three — mirror lines 175-179 into the loading `<td>`

[P3] [NEW] mule-detection/src/components/ui/DataTable.tsx:62 vs 57,107-109 — loading branch silently discards the `className` and `caption` props the other two branches honor (loaded wrapper applies `className` :107 and sr-only `<caption>` :109; loading wrapper hardcodes `aria-label="Loading table data"` and drops `className`) — a caller-supplied layout class vanishes during load → swap-time shift — apply `${className}` and derive the loading `aria-label` from `caption`

[P3] [NEW] mule-detection/src/components/ui/DataTable.tsx:100,107 — both wrappers emit `role="region"` landmarks with generic/ephemeral names: `"Data table"` default duplicates once per table (Accounts + Transactions pass no `caption`), and the empty state's landmark is literally named by its message (e.g. "No accounts match your filters"), polluting SR landmark roters — evidence: `role="region" aria-label={emptyMessage}` :100, `aria-label={caption || "Data table"}` :107 — drop `role="region"` (the `<table>` element already exposes table semantics) or label only when `caption` exists

[P3] [UNFIXED] mule-detection/src/components/ui/LoadingState.tsx:25-28,36-39 — identical 3-line spinner SVG duplicated inline twice in one file (part of Swarm-36 master Domain-1 #31 "spinner SVG duplicated ×3", still present) — evidence: byte-identical `<circle …/><path d="M4 12a8 8 0 018-8V0C5.373…" />` pairs differing only in `h-5 w-5` vs `h-8 w-8` — extract a `const SpinnerSvg = ({ className }) => …` local

CLEAN: mule-detection/src/components/ui/ErrorState.tsx (its only issue is the politeness role inherited from EmptyState, attributed above; its live-region comment is accurate against current EmptyState code, `Button size="sm"` contract verifies, and all four consumers' `description={error}` are type-safe under truthy-narrowing — `useLocalData.ts:48` types `error: string | null`)

TOTALS sev=P0:0,P1:0,P2:0,P3:6 status=NEW:4,UNFIXED:2,REGRESSION:0