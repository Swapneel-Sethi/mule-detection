# D2-5 [ML-TRAIN-PY] Files: mule-detection/scripts/train_transaction_model.py, train_meta_learner.py, auto_calibrate_thresholds.py. Focus: label leakage (labels derivable from features shown to model), train/test split integrity, base_score export on xgboost 3.x (silent None?), threshold calibration tautology (fit to min mule score), metric computation on training data, random seeds, dtype issues.
(agent_id=agent-68)

 outcome="completed">Audit complete. All three files read line-by-line; cross-file contracts verified against `transactionScorer.ts`, `transactionXgboost.ts`, `detectionEngine.ts`, `combine_ml_params.py`, `recompute_ml_scores.py`, the shipped JSON artifacts, and both datasets (sampled via Python; xgboost 3.2.0 behavior verified with a live mini-fit).

**Baseline resolution (not re-reported):** base_score xgboost-3.x export (#19/#21) — FIXED and verified correct on 3.2.0 (`save_config` → `[3.75E-1]` probability format handled by `.strip("[] \t")`; `booster.attr` None path handled). Per-fold min-max normalization (#27), CWD-anchored paths (#29), grouped-CV introduction (#23) — FIXED. Threshold tautology + label leakage — known/documented.

---

[P1|NEW] mule-detection/scripts/train_transaction_model.py:142-143 — CV "groups" are endpoint *pairs*, not accounts, contradicting the comment's claim "CV folds never share an account" — data: 99,952 usable txns → 98,044 unique pairs vs only 63,556 senders / 25,716 receivers; **23,629 senders span >1 pair**, avg 3.8 pairs per receiver, so the same account (with identical calibrated/hub/velocity features) lands in both train and test folds — grouped-CV AUC and the "held-out" fold remain inflated via shared accounts — fix: group by connected components of the sender–receiver graph (union-find over txn endpoints) so no account straddles folds.

[P2|UNFIXED] mule-detection/scripts/auto_calibrate_thresholds.py:9-11 (+ sinks detectionEngine.ts:1601-1603, recompute_ml_scores.py:160-170, public/ml_params.json) — documented manual-propagation contract is broken: three mutually inconsistent threshold/weight sets coexist at HEAD — detectionEngine/recompute hardcode **0.71/0.66/0.551**, shipped ml_params.json holds **0.671/0.64/0.551/0.551** with platt A=-39.8078/B=12.6312 still stale (known #44 verified present), and `_learned_thresholds.json` doesn't even exist in the tree — evidence: `python -c` on ml_params.json prints `{'critical': 0.671, 'high': 0.64, 'medium': 0.551, 'flagged': 0.551}` / platt `-39.8078, 12.6312` — fix: single generated constants module or a CI check that fails when any hardcoded band drifts from a freshly calibrated run.

[P2|NEW] mule-detection/scripts/auto_calibrate_thresholds.py:91-96 — percentile design degenerates on the current snapshot: mule calibrated_score percentiles are p10=0.612, p25=0.612, p50=0.613, p75=0.629 (all four bands within 0.017), so a rerun would emit critical=0.629 *below* production's high-band 0.66 and collapse level semantics; the printed separation gap (mule min 0.56 > clean max 0.283) is checked by eye only, never asserted — fix: assert strict inter-level spread ≥ ε and `flagged > non_mule_scores.max()`, failing loudly instead of exporting collapsed bands.

[P3|UNFIXED] mule-detection/scripts/train_meta_learner.py:39-45 — components remain label-derived (`COMMUNITY` slot fed raw `risk_score`, plus graph_score/ml_score), so NNLS collapses all non-ML weight onto the relabeled label: `_learned_weights.json` shows BEHAVIORAL/GRAPH/TEMPORAL/INTERACTION = 0.0, COMMUNITY = 0.6, AUC=1.0/CV=1.0 — master theme E/#47 tautology persists at HEAD; additionally the name `COMMUNITY` denotes a real community metric at detectionEngine.ts:565 but risk_score here — weights are semantically non-transferable if ever wired — fix: drop label-derived columns from the learner (or rename slot + document that weights apply to risk_score).

[P3|UNFIXED] mule-detection/scripts/train_transaction_model.py:182,379 — exported artifact is still fit on the FULL X,y including the held-out fold used for the printed metrics; SUMMARY juxtaposes CV AUC with the exported model (known domain-report leakage-chain step 4); feature importances printed under EVALUATION (:223-227) also come from the full-fit model — fix: print "final model refit on all data" explicitly or export the held-out clone alongside metrics.

[P3|NEW] mule-detection/scripts/train_transaction_model.py:250-256 — `resolve_base_score` falls back to silent 0.5 if `save_config` parsing throws AND attr is None — the exact known-P0 bias can silently recur on a future xgboost schema change with zero diagnostics — fix: `print("WARNING: base_score unavailable; defaulting to 0.5")` on the fallback path.

[P3|NEW] mule-detection/scripts/train_transaction_model.py:107,113-114 — falsy-`or` coercion maps legitimate zeros to defaults (`calibrated_score 0→0.3`, `risk_score 0→10`) — latent today (verified: 0 rows with `"calibrated_score": 0` or `"risk_score": 0`; mins 0.053/5.3 across 105,501 accounts) but wrong for any future sparse dataset — fix: `v = sender.get(k); float(v) if v is not None else default`.

[P3|NEW] mule-detection/scripts/train_transaction_model.py:102-104 — unparseable timestamps silently impute hour=12/weekday=0 (feeding hour_of_day/is_night/is_weekend) with no counter, unlike the amount-skip path which reports — latent (0 parse failures in current 99,952 txns) — fix: count and print like `skipped`.

[P3|NEW] mule-detection/scripts/train_transaction_model.py:289 — exporter omits the missing branch when it duplicates the yes branch (`"missing": null`), but transactionXgboost.ts:130 routes non-finite features to `node.missing ?? null` → tree contributes leaf 0 instead of following the yes branch, diverging from XGBoost NaN semantics; unreachable today only because computeTransactionRiskSync:245-252 diverts any non-finite vector to the heuristic — fix: always export the missing branch (or make TS fall back to `left` when missing is null).

[P3|NEW] mule-detection/scripts/train_transaction_model.py:210 — held-out precision/F1 reported at fixed 0.5 while the model trains with `scale_pos_weight=neg/pos`, which shifts predict_proba off calibration — 0.5 is not any deployed operating point (production FLAG_THRESHOLD=0.3 post-scaling), so the printed classification table is arbitrary — fix: also report at the deployed/exported threshold or sweep thresholds.

[P3|NEW] mule-detection/.gitignore:25 vs scripts/auto_calibrate_thresholds.py:22 — output `scripts/_learned_weights.json` is gitignored but sibling output `scripts/_learned_thresholds.json` is NOT (grep: only line 25 matches), so running the documented pipeline leaves a committable derived-data artifact; relatedly combine_ml_params.py:34 crashes FileNotFoundError since the file is currently absent — fix: add ignore entry; guard/order the pipeline.

[P3|NEW] mule-detection/scripts/auto_calibrate_thresholds.py:7 — stale usage note "Run from the project root" although paths became `__file__`-anchored (BASE, :20-22); runs correctly from any CWD — fix: delete the CWD instruction.

[P3|NEW] mule-detection/src/lib/transactionXgboost.ts:86 (cross-ref into slice file) — comment cites "train_transaction_model.py:302-306" for the exporter base_score fallback; after refactor the code lives at :234-261 (fallback return :255-256) — stale line reference misleads navigation — fix: update citation or reference by function name `resolve_base_score`.

TOTALS sev=P0:0,P1:1,P2:2,P3:10 status=NEW:10,UNFIXED:3,REGRESSION:0