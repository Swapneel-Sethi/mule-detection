# D5-6 [API-ANALYTICS-CROSS] Files: mule-detection/src/app/api/analytics/route.ts, api/alerts/count/route.ts. Focus: txnByPattern double-counting per-flag sets, hourly IST vs daily UTC mixed policy, dayKey year-merge, sankey slice(-6) suffix collisions, circular-loop amount semantics, TTL synchronous recompute blocking, alerts/count returning 200-with-zero on failure + dead-consumer status, Cache-Control interplay. THEN cross-check: same metric computed in data-local vs analytics vs normalizers computeStats — find ANY numeric-definition disagreement (turnover 55.64Cr vs 25.83Cr class bugs) and enumerate them precisely.
(agent_id=agent-87)

 outcome="completed">Audit complete. All focus areas verified against shipped data; `tsc --noEmit` exit 0.

**Verified-FIXED at HEAD (not re-reported):** hourly IST vs daily UTC mixed policy (all buckets now `Asia/Kolkata`); txnByPattern within-txn per-flag duplication (Set-dedupe works); analytics TTL recompute (process-lifetime cache now); Cache-Control interplay now benign (immutable artifacts + `s-maxage=60`). `alerts/count`'s 60s TTL sync re-read is 60KB ≈ negligible — the real problems there are below.

[P1] [NEW] mule-detection/src/app/api/analytics/route.ts:69-79 — headline "Mule"/"High Risk" KPIs contradict `/api/data-local` on the same dataset, while both comments claim they mirror/agree — measured: analytics muleCount=1,962/highRiskCount=6,616 (mule∧severity-tier vs rest-of-universe) vs data-local 8,578/0 (all `is_mule` vs non-mule∧tier); Dashboard renders "Mule 8,578 / High Risk 0", Analytics renders "Mule 1,962 / High Risk 6,616"; comment "the numbers coincide only while no critical/high non-mule accounts exist… true for the current dataset" is numerically false (they coincide never: 6,616≠0), and AnalyticsContent.tsx:182-183 asserts "identical numbers to the Dashboard's Account Categories card"; a third definition lurks in DashboardContent.tsx:58-63 fallback (mules-below-tier=6,616 labeled High Risk, summing 15,194 > 8,578 flagged) — unify on ONE partition (recommend data-local's: all confirmed mules vs non-mule∧tier) in analytics/route.ts:78-79 and delete the false coincidence commentary.

[P2] [UNFIXED] mule-detection/src/app/api/data-local/route.ts:183-191 — turnover still disagrees with analytics despite comment "so Dashboard, Analytics, and data-local agree" — data-local sums ALL txns touching flagged ids = ₹26.20 Cr (9,999 rows incl. 2,055 unflagged) vs analytics flagged-only = ₹25.83 Cr (7,944 rows); Δ=₹36.25 L shown as Dashboard "Turnover" vs Analytics "Flagged Turnover" (A3 residual, now 1.014×) — add the `t.flagged === true` check in data-local computeStats or relabel Dashboard's card.

[P2] [NEW] mule-detection/src/app/api/analytics/route.ts:124-140 — "Transaction Amount by Pattern" bars use multi-label attribution (each txn counted once per distinct endpoint-flag pattern) and contradict the same page's Sankey which forces one bucket by priority — measured: FANOUT bar ₹8.90 Cr vs Sankey FANOUT ₹4.64 Cr (1.92×); PASSTHROUGH 7.49 vs 7.44 Cr; bars sum ₹30.14 Cr > the page's own "Flagged Turnover ₹25.83 Cr"; 1,580/7,944 txns attributed to >1 pattern — apply the same PATTERN_PRIORITY single-bucket rule in txnByPattern, or subtitle the bar chart "multi-pattern attribution".

[P2] [NEW] mule-detection/src/app/api/analytics/route.ts:345-353 — silent `slice(0, 20)` per pattern drops 94.6–98.9% of each pattern's Sankey volume and the consumer presents the censored subset as pattern truth — AnalyticsContent.tsx:200-215 filter chip computes "{sel.length} flows · {accts} accounts · ₹{total}" from capped flows: selecting FANIN shows 20 flows / ₹0.10 Cr while the adjacent bar chart says ₹8.73 Cr — ship uncapped flows (payload is tiny: 7,944 rows ≈ 500 KB) or cap client-side with an explicit "+N more" and chip disclaimer.

[P2] [UNFIXED] mule-detection/src/app/api/alerts/count/route.ts:33-40 — read/parse failure returns HTTP 200 `{count:0}`, masquerading as "no active alerts", while the last good `cache.count` sits discarded in scope; no negative caching so every request during an outage re-parses the file — return 503 (or serve stale cache with the failed refresh logged), per A15 — e.g. `if (cache) return NextResponse.json({count: cache.count, stale: true})` else 503.

[P2] [UNFIXED] mule-detection/src/lib/normalizers.ts:264-273 — fallback `computeStats` (used at useLocalData.ts:127 whenever `json.stats` is missing) defines three stats differently from both APIs under identical keys — `totalVolume`=Σ account.turnover aggregates = ₹14.84 Cr over fetched top-1000 (₹55.64 Cr over full universe — the original A3 number lives here) vs APIs' ₹26.20/₹25.83 Cr txn-based; `totalTransactions`=Σ per-account in+out = 20,242–37,942 vs 99,952 rows; `flaggedTransactions`=count of alerts by type (≤155) vs 7,959 flagged rows — make the fallback return `null`s/omit rather than fabricating incompatible numbers, or compute from transactions like the APIs do.

[P3] [UNFIXED] mule-detection/src/app/api/analytics/route.ts:178-189 — `volumeByDay` includes unflagged txns touching the flagged universe (9,999 rows) while sibling pattern/turnover charts are flagged-only (7,944) — A24 stands — add `t.flagged === true` or document the inclusion.

[P3] [UNFIXED] mule-detection/src/app/api/analytics/route.ts:172,190-196,243-258 — day keys are MM-DD (`format(...).slice(5)`), merging same month-day across years and breaking `localeCompare` chronological order at a year boundary — latent (txns span 2026-01-01→06-29, single year today) — keep the full YYYY-MM-DD internally, strip to MM-DD only at render (A25).

[P3] [UNFIXED] mule-detection/src/app/api/analytics/route.ts:292-301 — circular-loop "amount" sums lifetime edge volumes across all 99,952 txns (incl. unflagged/clean edges), ~2.8× the largest single leg (top path legSum ₹4,07,544 vs max leg ₹1,39,764), not the cycled amount; `key=[from,via,mid].sort()` destroys orientation so displayed from/via/to is whichever triple iterated first; comment "reflects the whole cycle" is misleading — A13/L8 residual — sum only cycle-correlated legs or label "total corridor volume".

[P3] [UNFIXED] mule-detection/src/app/api/analytics/route.ts:260-269,355-380 — payload fields `inOutData`, `muleAccounts`, `cleanAccounts`, `accountsTotal` are computed but consumed by nothing (sole consumer AnalyticsContent.tsx reads none of them; `accountsTotal` prop fed to SankeyChart is itself unused) — dead computation incl. an 8,578-element sort per cold compute — delete from payload and interface (known #29, enumerated precisely here).

[P3] [UNFIXED] mule-detection/src/app/api/alerts/count/route.ts:1-41 — entire route orphaned: grep of `src/` finds zero references outside its own log line; honest header comment admits it — delete route or wire a consumer (A27).

[P3] [NEW] mule-detection/src/app/api/analytics/route.ts:227-237 — comment claims "`circular` alerts are the generator's name for the CIRCULAR topology", but shipped alerts contain zero `circular` type (vocab: rapid_movement×50, fan_in×40, fan_out×40, behavioral_change×25); the CIRCULAR line is actually fed solely by behavioral_change, a behavior signal mislabeled as flow topology — fix comment and reconsider mapping behavioral_change→CIRCULAR.

[P3] [NEW] mule-detection/src/app/api/analytics/route.ts:98-107 — "Patterns" card ships raw flag vocabulary (new_account 7,042, fan_out 1,762, fan_in 684, high_velocity 362…) unreconciled with the canonical FANIN/FANOUT/PASSTHROUGH/CIRCULAR used by every other chart on the page; flags like `fan_in`+`fanin_receiver` appear as separate rows yet collapse to one bucket elsewhere — map raw flags through `canonicalFlag` here too (or retitle card "Account Flags").

[P3] [NEW] mule-detection/src/components/AnalyticsContent.tsx:167-171 — bar-chart pattern colors break the page's pattern color language: FANIN fill `--color-frost` #e2e2e2 vs legend/line FANIN #7fd1f0, and FANOUT fill `--color-charcoal` #1a1a1a is near-invisible on the void background (indistinguishable from the dimmed state) — reuse PATTERN_LINES colors for the bars.

[P3] [UNFIXED] mule-detection/src/app/api/analytics/route.ts:328-341 — Sankey node keys `account_id.slice(-6)` remain collision-prone: 4 colliding groups (8 accounts) exist in the flagged universe (45 groups/90 across all accounts); verified NO actual flow merge occurs today (zero suffix-triple key collisions), so prior claim "merges flows" downgrades to latent hazard; `\u007c` split is currently safe since ids are hex — append a disambiguator or use full ids.

[P3] [NEW] mule-detection/src/components/DashboardContent.tsx:123 — stat cards labeled identically "Alerts" show different metrics: Dashboard `stats.activeAlerts`=107 vs Analytics `data.totalAlerts`=155 — relabel one ("Active Alerts") or align values.

TOTALS sev=P0:0,P1:1,P2:5,P3:10 status=NEW:7,UNFIXED:9,REGRESSION:0