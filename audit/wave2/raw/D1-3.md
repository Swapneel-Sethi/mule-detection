# D1-3 [UI/UX-PRIM-A] Files: mule-detection/src/components/ui/Button.tsx, Card.tsx, Skeleton.tsx, StatCard.tsx. Focus: variant/size matrix correctness (no inversions like lg&lt;md), prop defaults vs consumer usage, aria attrs, forwardRef completeness, token-class existence.
(agent_id=agent-60)

 outcome="completed">Audit complete. All four slice files read line-by-line; token classes verified against `globals.css` (`@utility transition-default`, `--text-caption/body/fluid-base`, risk colors, radii all exist); consumers enumerated repo-wide; `npx tsc --noEmit` exit 0; inner-repo HEAD (08e6932…47c0671 "fix: audit findings") contains the current state, so statuses are judged against committed HEAD.

---

**[P2] [UNFIXED]** mule-detection/src/components/ui/Skeleton.tsx:1-31 — entire Skeleton/SkeletonGroup surface is transitively dead; the H12 remedy (skeleton-shaped loading) still renders nowhere — sole direct consumer DataTable is never given `loading`/`loadingCount` by any caller (grep: only declarations at DataTable.tsx:24-25,49-50), and SkeletonGroup's only consumer LoadingState.tsx:19 is reachable solely via `variant="skeleton"`, which none of the 6 `<LoadingState …>` call sites pass (AccountsContent.tsx:58, AlertsContent.tsx:52, DashboardContent.tsx:87, TransactionsContent.tsx:66, AnalyticsContent.tsx:164, MuleGalaxy.tsx:824) — all pages still do the full-page spinner swap H12 flagged — fix: pass `loading` from a real fetch path into `DataTable` (or render `LoadingState variant="skeleton"` during initial load) or delete Skeleton.tsx.

**[P3] [UNFIXED]** mule-detection/src/components/ui/StatCard.tsx:24 — locale-aware compact formatting via `navigator.language` keeps the H3/H14 hydration-mismatch + intra-row inconsistency mechanism alive (SSR pins en-US, client may differ → React 19 hydration text mismatch); sibling cards pre-format strings with hard `"en-IN"` so the row mixes engines — `value={data.totalAlerts}` (AnalyticsContent.tsx:197) and `safeStat(stats.activeAlerts)` (DashboardContent.tsx:123) are the only numeric paths; currently latent (values ≈155 format identically in both locales, divergence starts ≥1e5: "100K" vs "1L"), also covers registered D5#21 "mixed StatCard number-formatting paths" — fix: route through a fixed locale constant (`new Intl.NumberFormat("en-IN", …)`) or accept only pre-formatted strings.

**[P3] [UNFIXED]** mule-detection/src/components/ui/Button.tsx:50-53 — spinner fixed `h-4 w-4` regardless of size and SVG duplicated ×3 (Button.tsx:50, LoadingState.tsx:25,36), master D1 reported-only #31 never addressed — quote: `<svg className="animate-spin h-4 w-4"` inside a size ladder spanning 26–44px tall buttons — fix: derive icon size from `size` (`sm: h-3`, `md/lg: h-4`) and extract one shared `<Spinner/>`.

**[P3] [NEW]** mule-detection/src/components/ui/StatCard.tsx:9-11,28-40,51-63 — `trend`, `icon`, `variant` props are dead code: all 8 call sites (DashboardContent.tsx:121-124, AnalyticsContent.tsx:194-197) pass only `label/value/sub`, so the swarm-fixed trend-sign inference (master D1 #9, line 28) and the un-duplicated `compact` padding map (#23) are fixes in an unreachable branch; grep for `<StatCard` shows zero `trend=`/`icon=`/`variant=` occurrences — fix: wire trend/icon into the KPI rows or strip the props until a consumer exists.

**[P3] [NEW]** mule-detection/src/components/ui/Button.tsx:5-25,44,49-54 — most of the API surface has no callers: `secondary` variant, sizes `md`/`lg`, `fullWidth`, and `loading` are used by nobody (only call sites are FilterBar.tsx:102 `ghost`+`sm` and ErrorState.tsx:27 primary+`sm`; grep shows `fullWidth`/`loading=` appear only in Button.tsx itself) — the just-verified non-inverted size ladder (sm 11px < md 13px < lg clamp 13→15px) ships unexercised — fix: adopt `size="lg"` for the error-screen Retry or prune the matrix to what's consumed.

**[P3] [NEW]** mule-detection/src/components/ui/Skeleton.tsx:1 — exported `SkeletonVariant` type has zero importers while its only out-of-file consumer re-declares the union literally (LoadingState.tsx:9 `skeletonVariant?: "text" | "card" | "table" | "chart"`), silently narrowing if variants are ever added — master D1 #20 claimed this duplication fixed, but only the in-file copy was removed; grep: `SkeletonVariant` appears solely in Skeleton.tsx — fix: `import type { SkeletonVariant }` in LoadingState and drop the literal.

**[P3] [NEW]** mule-detection/src/components/ui/Card.tsx:14-17 — `CardTitle` hardcodes `<h3>`, producing h1→h3 heading-order skips on every card section (PageHeader.tsx:13 renders `<h1>`; e.g. DashboardContent.tsx:127-128, AnalyticsContent.tsx:226-227, MuleGalaxy.tsx:1033 — no `<h2>` between) — axe/WCAG 1.3.1 heading-order violation across ~20 instances — fix: accept an `as`/`headingLevel` prop defaulting to `"h2"`, or render a styled `<div role="heading" aria-level>`.

**[P3] [NEW]** mule-detection/src/components/ui/Card.tsx:1-18, mule-detection/src/components/ui/StatCard.tsx:5-13,15-23 — neither component forwards refs nor extends its HTML element attributes (`CardProps`/`StatCardProps` lack `HTMLAttributes<HTMLDivElement>`), so consumers can't attach ids, `data-*`, aria attrs, or handlers without wrapper divs — inconsistent with Button's complete `forwardRef` + `displayName` (Button.tsx:27-61); no current caller is broken, contract gap only — fix: `forwardRef<HTMLDivElement, HTMLAttributes<HTMLDivElement> & …>` on both (Skeleton is aria-hidden decorative and can stay as-is).

Verified-clean aspects, for the record: size matrix strictly monotonic (sm 11px / py-1.5·px-3 < md 13px / py-2·px-4 < lg fluid-base clamp(13,13+0.2vw,15) — master #17 inversion stays fixed); `trendPositive ?? sign-inference` correct (#9); `transition-default`, `text-risk-low/critical`, `rounded-sm/md/lg`, `text-fluid-base`, `outline-bone` all resolve to real generated CSS; Button `font-medium` backed by JetBrains Mono weight 500 (layout.tsx:7); `type="button"` correctly overridable via props spread order; `aria-busy`/`aria-hidden` usage sound; SSR guard present in `getUserLocale`; design-tokens.md rewritten and consistent with these components; `npx tsc --noEmit` exit 0.

TOTALS sev=P0:0,P1:0,P2:1,P3:7 status=NEW:5,UNFIXED:3,REGRESSION:0