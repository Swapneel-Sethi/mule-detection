# D3-1 [VIZ-GALAXY-1H] File: mule-detection/src/components/MuleGalaxy.tsx LINES 1-550. Focus: fetch/cache lifecycle, radiusCache staleness across dataset reloads, canvas/DPR scaling, event-handler leaks (removeEventListener), coordinate transforms, color scale vs legend thresholds, score range assumptions (0-100 vs 0-10000 artifacts).
(agent_id=agent-70)

 outcome="completed">Audit complete. Baseline reconciliation first: of the prior findings touching this slice, **A22 (radiusCache staleness) is FIXED at HEAD** — `radiusCache.clear()` now runs at `MuleGalaxy.tsx:521` before `graphData()` repopulation, and fixed items #1 (viewMode ternary), #4 (hover resurrecting scrubbed corridors), #6 (`lastDay` typed), #14 (maxDistance 1e6 < far 2e6) are all verified present and intact (no regressions). Producer contract verified sound: route emits `score` clamped 0–100 (`route.ts:148`) so `sqrt(score/100)` and the `%` displays are scale-correct; `formatCurrencyINR` crore/lakh math is correct; dataset sampling (99,952 txns) shows 0 malformed timestamps, amounts ₹8.94–₹196,720.91, `calibrated_score` ∈ [0.053, 0.856].

[P2] [NEW] mule-detection/src/components/MuleGalaxy.tsx:564-590 — FPS adaptive-quality latch misfires after any tab-hide/RAF suspension, permanently degrading DPR+bloom with no recovery — RAF pauses while hidden; on resume `now - fpsRef.current.last` spans the whole hidden period with ~1 frame counted, e.g. hidden 60s ⇒ `measured = 1×1000/60000 ≈ 0.02 < 42` ⇒ `pixelReduced=true` latched forever (both branches gated only on `engineReadyRef`, no consecutive-failure or visibility guard, no upgrade path) — reset `fpsRef.current.last = performance.now()` on `visibilitychange`→visible (and skip windows spanning the gap), or require 2–3 consecutive slow windows before latching.

[P3] [NEW] mule-detection/src/components/MuleGalaxy.tsx:316-337,339-346,382-417 — filter/scrubber state changed during the async module-import window is never applied to the built graph — effect at 316 no-ops while `graphRef.current` is null (dynamic imports at 349-354 pending), and `build()`'s accessor chain registers no `nodeVisibility`/`linkVisibility`; after build completes, default = everything visible, so tiers/patterns/scrub set in that window are ignored until an unrelated dep change (hover's `hoverChanged` fixes links only, never `nodeVisibility`) — apply pending refs (`visibleIdsRef`, `scrubCutoffRef`, `traceIdsRef`) at the end of `build()` right after `graphData()`.

[P3] [NEW] mule-detection/src/components/MuleGalaxy.tsx:226-231 — unguarded date math: a malformed `lastDay` makes `new Date(...).getTime()` NaN and `toISOString()` throw RangeError inside a useMemo, crashing render — `filter(Boolean)` (220) passes garbage strings; `new Date(NaN).toISOString()` throws. Current data clean (sampled 99,952 txns, 0 bad timestamps), so defensive-only — validate with `/^\d{4}-\d{2}-\d{2}$/` in the `dayRange` predicate or bail to `null` when either bound is non-finite.

[P3] [NEW] mule-detection/src/components/MuleGalaxy.tsx:573-585,656 — cleanup resets `engineReadyRef` but not `qualityRef`, so a rebuild (StrictMode dev remount, any future snapshot change re-triggering deps `[particleCutoff, snapshot]`) inherits latched `pixelReduced/particlesDisabled` and the adaptive branches at 573/578 can never re-engage — add `qualityRef.current = { pixelReduced: false, particlesDisabled: false };` beside line 656.

[P3] [NEW] mule-detection/src/components/MuleGalaxy.tsx:28-36,212-215 — degenerate snapshot (zero links) renders a bare void: `particleCutoff` falls back to `Number.POSITIVE_INFINITY` making every `amount >= cutoff` comparison false, yet the "High-value flow" legend entry (981-983) still promises a channel that cannot exist, and there is no empty-state (galaxy was not covered by prior M11) — branch to `<EmptyState>` when `snapshot.links.length === 0` and hide the High-value-flow legend row when cutoff is non-finite.

[P3] [NEW] mule-detection/src/components/MuleGalaxy.tsx:28-36 (+ route.ts:31,103-105) — field name `lastDay` contradicts its own documented semantics on both producer and consumer sides — interface comment says "Earliest activity day", route comment says "track the earliest day seen" (`if (day && (!existing.lastDay || day < existing.lastDay))`), and the scrubber consumes it as a first-appearance cutoff; the name invites a future consumer to read it as latest-activity — rename to `firstDay` across route interface, payload builder, and this component (or at minimum fix the identifier).

[P3] [NEW] mule-detection/src/components/MuleGalaxy.tsx:307-314 — outgoing/incoming flow lists silently truncate at 40 with no "+N more" indicator — `.slice(0, 40)` on corridors that can exceed 40 for hub accounts (same undisclosed-cap pattern already flagged for pattern chips) — show `+N more` suffix using the pre-slice length.

[P3] [NEW] mule-detection/src/components/MuleGalaxy.tsx:553-560 — ResizeObserver unconditionally calls `graph.zoomToFit(0, 24)` once engine-ready, yanking the camera away from any user-framed region on every container resize (window drag, orientation, any reflow touching the mount) — track last-seen dimensions and skip no-op resizes; only auto-fit on size-crossing thresholds, never while a pointer interaction is active.

[P3] [NEW] mule-detection/src/components/MuleGalaxy.tsx:191 — `await response.json() as GalaxySnapshot` blindly casts unvalidated network JSON; a schema drift (e.g. `flags` not an array) surfaces as a deep TypeError in render instead of the existing error card — minimally validate `Array.isArray(data.nodes)` / `data.meta` before `setSnapshot`.

[P3] [UNFIXED] mule-detection/src/components/MuleGalaxy.tsx:563-591,805 — FPS meter re-renders the whole tree every 500ms forever (master audit D3 reported-only #19) — `setFps(measured)` on a 500ms RAF cadence regardless of delta; still present verbatim — move FPS readout into an isolated component or write to a DOM ref.

[P3] [UNFIXED] mule-detection/src/components/MuleGalaxy.tsx:559,336 — zoomToFit margins ignore the 400px right detail panel (#19): `zoomToFit(700, 90)` / RO `zoomToFit(0, 24)` center on full canvas width, so framed content sits underneath the open panel — offset the fit region by panel width when `panelOpen`.

[P3] [UNFIXED] mule-detection/src/components/MuleGalaxy.tsx:604-634 (registered at 424-427) — hover scans all ~10k links and allocates a fresh Set per event, O(E) per mousemove (#19) — `for (const link of links)` over `galaxySnapshot.links`; build an adjacency map once per snapshot instead.

[P3] [UNFIXED] mule-detection/src/components/MuleGalaxy.tsx:398 vs 970 — legend swatch color ≠ rendered corridor color (#19): legend "Flagged corridor" `#f87171` vs actual `rgba(239,69,98,.26)` ribbon at 398 — align legend swatch to the rendered rgba (particle dots do use `#f87171`, so pick which channel the entry describes).

[P3] [UNFIXED] mule-detection/src/components/MuleGalaxy.tsx:283-303 — trace BFS traverses currently-hidden corridors (#19): walks full `adjacency` ignoring `visibleIds` and the scrub cutoff, so the layering chain highlights/selects invisible accounts — filter neighbor expansion by `visibleIds.has(nextId)` and `!(lastDay > cutoff)`.

[P3] [UNFIXED] mule-detection/src/components/MuleGalaxy.tsx:142,380,655 — `bloomRef` is write-only (#19): assigned at 380, nulled at 655, never read — drop the ref (keep the local `bloomPass`).

[P3] [UNFIXED] mule-detection/src/components/MuleGalaxy.tsx:550-551,592-601 — listener-leak window if `build()` throws between adding `pointerdown`/`wheel` listeners (550-551) and assigning `teardownRef.current` (592); the catch at 598 never unwinds the half-built graph/listeners (#19) — assign a minimal teardown immediately after each resource creation, or wrap post-graph-creation setup so the catch path invokes it.

TOTALS sev=P0:0,P1:0,P2:1,P3:15 status=NEW:9,UNFIXED:7,REGRESSION:0

CLEAN: (no fully-clean files in slice — `MuleGalaxy.tsx` carries the findings above; cross-checked counterpart files `src/app/api/graph/mule-galaxy/route.ts` and `src/lib/utils.ts` (`formatCurrencyINR`) verified contract-consistent: score 0–100 clamp, earliest-day scrub semantics, tier⟷riskLevel⟷meta alignment, ₹ lakh/crore math)