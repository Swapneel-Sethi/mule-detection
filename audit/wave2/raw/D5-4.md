# D5-4 [LIB-HOOK-MOCK] Files: mule-detection/src/lib/useLocalData.ts, mockData.ts, utils.ts. Focus: silent mock fallback masking outages (worst failure mode for fraud tool), 30s-timeout leaving loading:true, loadMore replacing vs appending, currentPageRef advance-before-resolve, DEFAULT_PAGINATION mismatch, mock data internal consistency (stats sum correctly?), formatCurrencyINR crore/lakh math correctness, Intl reuse/perf.
(agent_id=agent-85)

 outcome="completed">Audit complete. All three slice files read line-by-line; contracts proven against the route, normalizers, shipped JSON (sampled via python), and consumers. Register baselines checked: A18/A19/A31 (`loadMore` replace, `currentPageRef`, `DEFAULT_PAGINATION.limit=200`) are verified **fixed** in the `useLocalData.ts` rewrite — `mergeTransactions` appends+dedupes, `pagination.page` advances only after resolve, limit 1000 matches request. No regressions found.

[P2] [UNFIXED] mule-detection/src/lib/useLocalData.ts:130 — hook ships `riskScore` rows through unchecked, so the known mixed-scale corruption reaches the UI intact — python sample of public/transactions_synthetic.json: `rows<=1.0: 7469, rows>1: 92483, min 0.1, max 100.0`; consumer TransactionsContent.tsx:139-143 clamps 0–100, so 7.5% of flagged txns render as empty risk bars labeled "0"/"1" and sort against the wrong scale in a fraud-triage tool (known D4#29, unchanged at HEAD) — normalize at the hook boundary: `riskScore <= 1 ? riskScore * 100 : riskScore` (or regenerate the dataset).

[P3] [NEW] mule-detection/src/lib/useLocalData.ts:127 — `json.stats || computeStats(...)` fallback would silently swap metric definitions, masking an API regression — route's stats: totalVolume = txn-sample amounts touching flagged ids (route.ts:186-191, audit A3's ₹25.83Cr definition), flaggedTransactions = 7,959 rows (route.ts:233); fallback: totalVolume = Σ account turnover (normalizers.ts:260, the ₹55.64Cr definition, 2.15× off) and flaggedTransactions = alert-type count ≤155 (normalizers.ts:271-273) — computed over page-1's 1000 accounts only yet rendered as global stats — drop the fallback and throw/surface `"Malformed response"` like the other shape checks.

[P3] [NEW] mule-detection/src/lib/utils.ts:71 — `formatCurrencyINR(NaN/Infinity)` renders "₹0", silently falsifying corrupt amounts as zero rupees — condition `!Number.isFinite(value) || value === 0` lumps NaN with a legitimate 0, while sibling `Intl`-based formatters visibly print "NaN" — return `"—"` (or `"₹NaN"`) for non-finite input so bad data stays conspicuous.

[P3] [NEW] mule-detection/src/lib/utils.ts:12-16 — `getUserLocale()` returns "en-US" during SSR prerender but `navigator.language` on hydration; compact output diverges at ≥1e5 — node-verified: en-US `150K` vs en-IN `1.5L` → any StatCard value crossing ₹1L hydrates with mismatched text (React 19 hydration error + client re-render flash); latent today only because current card values stay below 1e5 or go through locale-independent formatCurrencyINR — pin a single locale constant for server+first-client render (note `setUserLocale` has zero callers, so there is currently no way to align them).

[P3] [UNFIXED] mule-detection/src/lib/useLocalData.ts:114 — hardcoded `limit=1000` caps the flagged universe at 1000 of 8,578 despite `hasMore:true`; residual of known reported-only item (D5#21/A5; route permits 5000) and `loadMore`/`setPage` remain destructured by nobody (grep: only `pagination` is consumed, by AccountsContent.tsx:23) — raise limit or wire `loadMore` in AccountsContent; expose `isLoadingMore` since append requests never set `loading:true` (line 108), leaving consumers no feedback to disable the control.

[P3] [NEW] mule-detection/src/lib/useLocalData.ts:8-12 — `ApiTransaction` docstring is stale: claims demo data uses a different `type` union `("transfer"/"payment"/…)` than the real dataset, but mockData.ts:21-23 was rails-aligned (D5#10) to `"upi"|"imps"|"neft"|"rtgs"` — "Same payment channels as public/transactions_synthetic.json" — the declared justification no longer exists; rewrite the comment (the interface itself may now alias `Transaction`).

[P3] [NEW] mule-detection/src/lib/utils.ts:23,36,47,63,85-89,97,114,137 — every formatting call constructs a fresh `Intl.NumberFormat`/`DateTimeFormat`/`RelativeTimeFormat` with zero memoization — called per table cell, per chart tooltip (AnalyticsContent.tsx:314 on mousemove) and per hover-rebuilt link label (MuleGalaxy.tsx:403); Intl construction resolves ICU data each time — memoize formatters in a module-level `Map` keyed by `locale + JSON.stringify(options)`.

[P3] [NEW] mule-detection/src/lib/utils.ts:8-166 — 11 of 13 exported symbols have zero external consumers (grep across src: only `formatNumber` ←StatCard and `formatCurrencyINR` ←3 components are imported): `setUserLocale`, `getUserLocale`, `formatNumberFull`, `formatCurrency`, `formatCurrencyFull`, `formatPercent`, `formatDate` (internal caller only), `formatRelativeTime`, `formatBytes`, `truncate`, `cn` — dead surface that must still be maintained/tested; delete or wire up.

[P3] [UNFIXED] mule-detection/src/lib/mockData.ts:155-208 — all runtime exports (accounts, transactions, alerts, stats, riskDistribution, transactionTimeline, patternTypes, getGraphData) remain dead: sole importer is `import type` in useLocalData.ts:5, which is erased at compile time — known reported-only item (D5#21 "mockData runtime exports ALL dead code") still true at HEAD; module also generates nondeterministic `Math.random()` data at module scope if ever imported for real (hydration/prerender hazard waiting to fire) — delete the runtime exports, keeping the interfaces.

[P3] [NEW] mule-detection/src/lib/mockData.ts:201-208 — `patternTypes` counts contradict the alerts actually generated: claims Rapid Movement 3 / Fan-Out 2 (Σ=10) vs `alertData`'s rapid_movement ×2 / fan_out ×1 (Σ=8, lines 131-140) — internal inconsistency in mock stats; recount from `alerts` (`alerts.filter(a => a.type === …).length`) instead of literals.

[P3] [NEW] mule-detection/src/lib/mockData.ts:116,149,151 — alerts can predate their referenced transaction: alert i stamped fixed Aug 15−i 2026 (line 149) but `transactions[i % length]` carries a random Jan–Aug 2026 timestamp (line 116, up to Aug 28) → evidence can postdate the alert by up to ~3 weeks; same defect class fixed for seed/muleSeed generators (D6#8) but never applied here — anchor alert timestamp to referenced txn +1h (dead code today, but a trap for revival).

TOTALS sev=P0:0,P1:0,P2:1,P3:10 status=NEW:7,UNFIXED:3,REGRESSION:0