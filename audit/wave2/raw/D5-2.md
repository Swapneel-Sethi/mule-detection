# D5-2 [PAGE-ACCT-ALERT] Files: mule-detection/src/components/AccountsContent.tsx, AlertsContent.tsx. Focus: limit=1000 truncation without indicator, search param contract (name vs city, q never sent?), pageIndex not reset on filter change, dead search inputs wired to no-op, severity/status filter enums vs API whitelist, sort client-side vs server contract.
(agent_id=agent-83)

 outcome="completed">Audit complete. All claims below verified against HEAD source and the shipped datasets (155-alert sample + full 105,501-account scan).

## Findings (worst-first)

[P2] [UNFIXED] mule-detection/src/components/AccountsContent.tsx:27-43,50,164-168 — search & browsing silently cover only the fetched first ≤1,000 of 8,578 flagged accounts; no indicator, false "No accounts match your filters" negatives — register A5/D5-reported-#20: hook hardcodes `limit=1000` (useLocalData.ts:114) though route allows 5,000 (route.ts:57) and universe is 8,578 (verified: `mule 8578`); during search `totalLabel` reports `filtered.length` counted inside that 1,000-row subset only; footer meanwhile shows true total 8,578 while just 500 rows are reachable (DISPLAY_CAP:43) — fix: paginate via the hook's dormant `setPage` (or raise limit), and label the search scope "searched first 1,000".

[P2] [NEW] mule-detection/src/components/AccountsContent.tsx:11-15,23 — "High Risk" filter (`category=high`) can never return a row on the shipped dataset — route.ts:83 defines it as `is_mule !== true && risk_level∈{critical,high}`; full-dataset count: `non-mule high/critical 0` (all 1,962 critical/high rows are mules), so selecting it always renders "No accounts match your filters · Showing 0 of 0 accounts" — fix: drop the option, or repoint `category=high` to `risk_level∈{critical,high}` regardless of is_mule (disjointness then breaks — needs owner decision).

[P2] [UNFIXED] mule-detection/src/components/AlertsContent.tsx:14 — Alerts page mounts the full-data hook, downloading ~2.7 MB (1,000 accounts + all flagged txns) it never renders — report-06 H6: useLocalData.ts:114 unconditionally sends `transactions=true&alerts=true&limit=1000`; AlertsContent destructures only `alerts/loading/error/refetch` — fix: add flags to `useLocalData` so Alerts fetches `transactions=false&accounts=false`.

[P3] [UNFIXED] mule-detection/src/components/AlertsContent.tsx:170-171 — severity options "Low" and "Info" match zero shipped alerts — dataset severities: `{medium:129, critical:1, high:25}` (155 rows; no low/info; domain4 #32 had flagged Info only, Low is equally dead) — fix: remove both options or derive options from data.

[P3] [NEW] mule-detection/src/components/AlertsContent.tsx:183 — status option "Dismissed" matches zero shipped alerts — statuses: `{new:61, investigating:46, resolved:48}`, dismissed absent — fix: remove until the workflow emits dismissals.

[P3] [UNFIXED] mule-detection/src/app/api/data-local/route.ts:125 + AlertsContent.tsx:199 — silent `slice(0, 500)` alert cap; "Showing X of Y alerts" counts only the capped set, client unaware (register A8; latent at 155 alerts today) — fix: return true total alongside the cap and surface it.

[P3] [UNFIXED] mule-detection/src/lib/useLocalData.ts:179-189 + AccountsContent.tsx:23 / AlertsContent.tsx:14 — `loadMore`/`setPage`/`pagination.hasMore` consumed by nobody (master D5 #21); the one mechanism that could fix the 1,000-row truncation ships dead — fix: adopt in AccountsContent or delete from the public hook surface.

[P3] [UNFIXED] mule-detection/src/lib/useLocalData.ts:114 + route.ts:90-98 — `q` still never sent and search-field contracts diverge: API matches id/name/bank (no city), client matches id/name/city/bank (AccountsContent.tsx:30-34) — half of register A10 remains; harmless today only because q is unused — fix: align field sets before anyone wires `q` up.

[P3] [NEW] mule-detection/src/components/AccountsContent.tsx:30-34 vs 107-118 — search matches holder `name` and `bank`, which render in no column (Account cell shows id+city only) — matched text invisible, bank hits inexplicable (residue of report-05 M4-inverse) — fix: display name in the Account cell or restrict predicate to rendered fields.

[P3] [NEW] mule-detection/src/components/AlertsContent.tsx:57-63 — refetch failure with stale alerts renders no error at all: guard `error && alerts.length === 0` drops the stale-data path that AccountsContent banners at :84-88 — silent failed refresh contradicts D5 #12 parity — fix: add the `role="alert"` "Refresh failed — showing previously loaded alerts" banner mirroring AccountsContent:84-88.

[P3] [NEW] mule-detection/src/components/AlertsContent.tsx:49-55,151 — loading/error shells are bare `p-8` (no PageHeader, no `max-w-[1200px] mx-auto`) and `if (loading)` swaps the whole page to a spinner even when stale alerts exist (focus loss + flash on every refetch); all four sibling content components use the wrapper (grep: 10 hits) and Accounts gates via `isInitialLoad` (:52) — fix: mirror AccountsContent's `isInitialLoad` gate + PageHeader/wrapper in all three states.

[P3] [NEW] mule-detection/src/components/AlertsContent.tsx:38-40 — sort builds two `new Date` objects per comparison and NaN-poisons ordering on invalid timestamps (`NaN - x` → implementation-defined order); Transactions got a `Date.parse(t)||0` guard under D5 #15, Alerts didn't (timestamps currently valid ISO-Z, so latent) — fix: precompute epoch ms in the memo (`const t = Date.parse(a.timestamp)||0`) and sort on deltas.

[P3] [NEW] mule-detection/src/components/AlertsContent.tsx:22-23 — trims the emptiness guard but filters with the untrimmed value: `"ACC-123 "` fails to match any field — TransactionsContent trims both (:40-41) — fix: `const q = search.trim().toLowerCase()`.

[P3] [NEW] mule-detection/src/components/AccountsContent.tsx:27-28 — no trim at all: a whitespace-only query passes `if (search)` and matches nearly every row (multi-word names/cities contain spaces), flushing the table — the exact defect D5 #15 claims fixed for these pages — fix: `if (search.trim())` + trimmed `q`.

[P3] [NEW] mule-detection/src/components/AlertsContent.tsx:113-119,90-95 — status/severity colors bypass semantic tokens: raw `text-amber-400` for investigating (same class as the orange-400 D6 #10 removed), and severity `low`/unknown falls through to bright `text-bone` (:94) although `.risk-low`/`--color-risk-low` #22c55e exists (globals.css:16,109) — low reads brighter than medium — fix: use token utilities (`text-risk-medium`, `text-risk-low`) for both columns.

[P3] [NEW] mule-detection/src/components/AlertsContent.tsx:138-143 — Time column formats in viewer-local zone (`toLocaleString("en-IN", …)` with no `timeZone`) while analytics buckets IST — same tz-policy split as register A32, now on a third page — fix: pass `timeZone: "Asia/Kolkata"` (or a shared formatter).

[P3] [NEW] mule-detection/src/components/AccountsContent.tsx:104-162 — DataTable rendered without `caption`, unlike Alerts (`caption="Alerts"`, AlertsContent:193) → generic `aria-label="Data table"` region and no sr-only caption — fix: pass `caption="Flagged accounts"`.

[P3] [NEW] mule-detection/src/components/AlertsContent.tsx:73,80,89,103,112,125,134 — seven inline `(typeof alerts)[0]` render signatures instead of the exported `MappedAlert` type (normalizers.ts:113); identical pattern was eliminated from Transactions under D5 #15 — fix: type columns as `Column<MappedAlert>[]`.

[P3] [UNFIXED] mule-detection/src/components/AccountsContent.tsx:132 — risk presented as bare `toFixed(0)` while Dashboard appends `%` (report-05 M8 presentation split, never reconciled) — fix: shared `formatRisk()` helper.

[P3] [UNFIXED] mule-detection/src/components/AccountsContent.tsx:144 — flag chips keyed `key={flag}`; duplicate flag entries would collide (register L1 / master D5 #21) — fix: `` key={`${flag}-${i}`} ``.

Verified-fixed known items (not re-reported): A33 pageIndex reset (AlertsContent:45-47 ✓), dead search input (wired ✓), Alerts pager replacing slice(0,50) ✓, empty-state disambiguation ✓, subtitle="\u00a0" removal ✓, cast-chain removal ✓, DEFAULT_PAGINATION.limit 200→1000 ✓, M6 NaN parseInt ✓, whitespace-search on Transactions ✓. Also verified non-defects: `text-risk-*` classes do resolve (`--color-risk-*` live in `@theme inline`, globals.css:13-16); alert timestamps are Safari-safe ISO-Z; alert ids unique/non-empty; server/client sort contracts agree (risk_score desc re-sort is redundant but consistent).

TOTALS sev=P0:0,P1:0,P2:3,P3:17 status=NEW:11,UNFIXED:9,REGRESSION:0