# D1-2 [UI/UX-SHELL] Files: mule-detection/src/app/layout.tsx, error.tsx, global-error.tsx, loading.tsx, not-found.tsx, page.tsx + all page.tsx route wrappers (accounts/alerts/analytics/graph/transactions). Focus: font wiring, metadata honesty (claims vs rendered content), viewport/themeColor exports, skip-nav target, error-boundary reset logic, class-name validity against globals.css tokens.
(agent_id=agent-59)

 outcome="completed">Audit complete. All machine checks done (compiled-CSS verification, bundled Next 16 docs for the error contract, symbol greps). Findings below.

**Verified-fixed baseline items (not re-reported):** C1–C3 dead classes now compile (`--text-fluid-base`, `.transition-default`, `.text-heading`, `.text-body`, `.skip-nav`, `--font-display`, `--app-font-display` all present in `.next/static/chunks/2c_9r9gns2e30.css`); L11 viewport/themeColor exported (layout.tsx:9-13); item 5 skip-nav `tabIndex={-1}` present; L10 global-error hexes now match tokens (`#e2e2e2`/`#888888`, radius 4). Error-boundary prop `retry` is **correct** for this Next version (bundled docs `error.md:27-30,117-121`; `retry` stable v16.3.0) — reset logic sound in both `error.tsx` and `global-error.tsx`.

---

[P3] [NEW] mule-detection/src/app/global-error.tsx:20-36 — standalone document renders no `<meta name="viewport">`, so on mobile the global-error screen lays out at ~980px virtual width with unreadable text — root layout's `viewport` export cannot apply because global-error replaces the entire layout (bundled doc `error.md`: "must define its own `<html>` and `<body>` tags"); JSX contains only inline-styled h1/p/button — add `<meta name="viewport" content="width=device-width, initial-scale=1"/>` beside the `<title>` (React 19 hoists it into head)

[P3] [NEW] mule-detection/src/app/accounts/page.tsx:10 — metadata description "Browse 100k+ analyzed accounts" overclaims the browsable universe ~12× — `api/data-local/route.ts:73,79`: base set is `flaggedAccounts = allAccounts.filter(r => r.is_mule === true || isHighRisk(r))` and even `category=all` returns only that flagged set (true size 8,578 per SWARM36 master D5#20; the 105,501 total surfaces only as the dashboard KPI via `stats.totalInDataset`, route.ts:128) — reword to e.g. "Browse 8.5k+ flagged accounts…" or add a route mode that actually lists non-flagged accounts

[P3] [NEW] mule-detection/src/app/error.tsx:21-51 — route-error screen replaces content silently with no live-region semantics, so screen readers never announce the failure (WCAG 4.1.3) — container div has only layout classes; neither it nor an inner wrapper carries `role="alert"`/`aria-live` — put `role="alert"` on the outer div (content is short; polite announcement of "Something went wrong" suffices)

[P3] [UNFIXED] mule-detection/src/app/layout.tsx:24 — `twitter: { card: "summary_large_image" }` still declared with no image and no `metadataBase` anywhere (SWARM36 Domain-1 reported-only #27) — `grep metadataBase|og:image|images:` over src/app returns zero matches — add `metadataBase` + `openGraraph.images`/`twitter.images`, or downgrade to `card: "summary"` until a real OG asset exists

[P3] [NEW] mule-detection/src/app/alerts/page.tsx:10 — description "alerts raised by the detection engine" misstates provenance: the shipped engine is unwired (`runDetection` defined at `src/lib/detectionEngine.ts:1413` with zero importers/callers across src), and displayed alerts are precomputed records served from static JSON (`api/data-local/route.ts:125` slices stored alerts) — reword to "precomputed fan-in / fan-out / rapid-movement / behavioral-change alerts from the trained-model pipeline" or wire the engine

[P3] [NEW] mule-detection/src/app/graph/page.tsx:11 — description promises "live money-flow corridors" but the graph API serves immutable artifacts per request: `mule-galaxy/route.ts:5` `force-dynamic` + `:52` `readFile(public/accounts_dataset.json | transactions_synthetic.json)` with a module cache; nothing streams or recomputes live (same "Live"-overclaim family as fixed C7/liveLabel) — drop "live" ("money-flow corridors between ML-flagged accounts")

[P3] [UNFIXED] mule-detection/src/app/page.tsx:10 — dashboard metadata still claims a "risk distribution" breakdown while the dashboard renders only the 2-category split (SWARM36 Domain-1 reported-only #30) — `DashboardContent.tsx:127-133` renders exactly two `CategoryBar`s (Mule / High Risk) under "Account Categories" — say "flagged-account category split", or ship a real multi-band distribution chart

[P3] [NEW] mule-detection/src/app/layout.tsx:38 + src/app/globals.css:127-131 — activating the skip link with Enter leaves a 2px white outline ring around the entire `<main id="main-content">` viewport: programmatic focus inherits `:focus-visible` from the keyboard-activated link, and the global `:focus-visible { outline: … }` rule then draws a full-page border-offset rectangle — suppress on the landmark target only: `#main-content:focus { outline: none; }`

[P3] [NEW] mule-detection/src/app/layout.tsx:6 — self-hosted Inter weight 600 is loaded/preloaded but consumed nowhere — `grep font-(medium|semibold|bold|extrabold|light)` over src yields only 4 × `font-medium`, all paired with `font-mono` (JetBrains Mono 500); headings are `font-normal` (PageHeader.tsx:13) or UA-bold 700 (error.tsx:25, not-found.tsx:10, Card.tsx:16 h3) → change to `weight: ["400", "700"]` and drop the dead ~15KB woff2 + preload entry

[P3] [NEW] mule-detection/src/app/accounts/page.tsx:10 — same description promises "KYC and behavioral flags" but no KYC data is rendered anywhere in the UI — `grep kyc|KYC` in AccountsContent.tsx → zero hits (dataset ships `kyc_status`; feature vector hardwires it per SWARM36 D2#10) — remove "KYC and" or add a KYC column/filter

[P3] [UNFIXED] mule-detection/src/app/layout.tsx:38 — hardcoded `lg:ml-[200px]` still duplicates the sidebar width magic number in `Sidebar.tsx:61` `w-[200px]` (SWARM36 reported-only #29) — extract one shared constant (e.g. export `SIDEBAR_WIDTH_PX = 200` from Sidebar.tsx and use `lg:ml-[${…}]` via a style attr or CSS var `--sidebar-w`)

TOTALS sev=P0:0,P1:0,P2:0,P3:11 status=NEW:8,UNFIXED:3,REGRESSION:0

CLEAN: mule-detection/src/app/loading.tsx, mule-detection/src/app/not-found.tsx