# D3-6 [VIZ-STALE-HUNT] Dead-reference sweep across mule-detection/: grep for 'NetworkGraph', 'HierarchicalHypergraph', 'Bipartite', 'bipartite', 'network_graph', 'hierarchical_hypergraph' in src/, scripts/, public/ (filenames), package.json, next.config.ts, docs/, *.md. Any surviving importer/script/npm-ref/doc claim about deleted features = finding. Also inspect scripts/verify_constellation_tmp.py (tracked junk?) and whether package.json scripts reference nonexistent files.
(agent_id=agent-75)

 outcome="completed">Sweep complete. All verification passes done — compiling the report.

---

## D3-6 [VIZ-STALE-HUNT] — Dead-Reference Sweep Report

Baseline reconciliation before findings: of the known graphs-viz items, **M13** (vis-network/vis-data dead deps) is **fixed** — absent from both `package.json` and `package-lock.json`; **H1** fixed via `PlotlySankey.tsx:11-15` factory + `plotly.js/lib/sankey` partial bundle; **D3#3** fixed — `src/types/plotly-partial.d.ts` was narrowed to `plotly.js/lib/*` subpaths and no longer declares react-plotly.js; **03-report #22** (`verify_bipartite.py`) — resolved at HEAD, file deleted everywhere (`find -maxdepth 3 -name "verify_*.py"` returns only `verify_constellation_tmp.py`); **03-report #23** — fixed, `scripts/__pycache__/` now holds only existing sources' bytecode; **03-report #25** — moot for the inner repo (`mule-detection/graphify-out/` no longer contains `.graphify_detect.json`/`.graphify_extract.json`). Deleted-feature references in `src/`: **zero** (all six target symbols, plus bare `hypergraph`/`pairwise`/`crime network` variants). Live `graphify-out/manifest.json`: 103/103 entries exist on disk — current snapshot healthy.

Findings:

[P3|NEW] mule-detection/SIH_AUDIT_REPORT.md:207,309,337 — stale audit doc makes three false viz architecture claims: reviews deleted component ("**NetworkGraph**: ✅ Edge colors"), praises "**Good vis-network integration**" (vis-network was never imported anywhere per M13 and is now purged from package.json), and shows a git-status snapshot listing modified `src/components/NetworkGraph.tsx` (file deleted, replaced by MuleGalaxy.tsx) — evidence: `grep NetworkGraph src/` = 0 hits; `grep '"vis-' package.json package-lock.json` = 0; glob of src/components shows no NetworkGraph.tsx — fix: rewrite §4 Components-Reviewed / §6 UI tables against the current MuleGalaxy/Sankey/recharts stack or delete the superseded report (its `/api/detect`+seed staleness is separately known-reported in D6#29; these viz lines are not covered by it).

[P3|UNFIXED] mule-detection/{constellation_verify.png, scripts/verify_constellation_tmp.py, graphify-out/cache/**}:1 — register item S14 tracked junk still present at HEAD: a 441,476-byte generated screenshot committed at repo root, the temp-named smoke script, and ~50 `graphify-out/cache/` files tracked despite the `.gitignore` rule `graphify-out/cache/ast/` — evidence: `git ls-files` lists all three groups; extra detail beyond S14: the script now writes `scripts/constellation_verify.png` (line 23) while the tracked copy sits at repo root and `scripts/constellation_verify.png` doesn't exist, so the committed PNG is orphaned even relative to its own generator — fix: `git rm --cached` all three groups; widen ignore to `graphify-out/cache/` and `constellation_verify.png`.

[P3|NEW] mule-detection/scripts/verify_constellation_tmp.py:17 — tracked Python tool depends on unmanifested third-party package: `from playwright.sync_api import sync_playwright`, and no `requirements*.txt`/`pyproject.toml`/`setup.py` exists anywhere under mule-detection — evidence: `ls requirements*.txt pyproject.toml setup.py` → "(no python manifest)"; script otherwise healthy (all UI selectors verified current against MuleGalaxy.tsx:811-876: "Network Graph", ALL FLAGGED/MULE ACCOUNTS/WATCHLIST, "Search the network graph", RE-CENTER) — fix: fold into S14 deletion, or keep under scripts/ with a pinned dev-requirements note.

[P3|NEW] mule-detection/detect_step.py:1-6 — tracked temp helper with stale output contract: imports `graphify.detect` (unmanifested Python dep) and writes CWD-relative `graphify-out/.graphify_detect.json`, an artifact that no longer exists among current graphify-out outputs (verified listing: analysis/labels/root/semantic_marker/manifest/graph/GRAPH_REPORT only); root-level `_step` name marks it as one-off scaffolding — evidence: `git ls-files` tracks it; `ls graphify-out` shows no `.graphify_detect.json`; no Python manifest — fix: delete from git, or archive under scripts/ with dependencies documented.

[P3|NEW] mule-detection/scripts/visualize_sankey.py:8-46,82 — standalone demo renders a Sankey from entirely fabricated random flows (hardcoded fake ids `MULE_FANIN_01`/`VICTIM_*`, `random.seed(42)`), reads zero project datasets, and writes `scripts/sankey_money_flow.html` (absent on disk, untracked); shares nothing with the production PlotlySankey→/api/analytics pipeline — evidence: head of file shows pure `random.uniform` flow synthesis; no `open()` of any repo JSON — fix: delete, or rewire to `public/transactions_synthetic.json` if a static HTML export is actually wanted.

[P3|NEW] mule-detection/graphify-out/2026-08-24/:61,217-219 — archived dated snapshots accumulate in git (~700KB/day: 680K+749K+737K ≈ 2.2 MB) and the oldest documents since-deleted sources (`generate_hierarchical_hypergraph.py`, `NetworkGraph.tsx`), shipping stale code-structure claims in tracked markdown; not covered by S14, which names only `cache/**` — evidence: `du -sh` sizes; 08-24 GRAPH_REPORT.md lines vs deleted paths; live manifest verified current (103/103 entries on disk) so only archives are stale — fix: track only the newest dated snapshot (gitignore `graphify-out/20*`), prune older ones from the index.

[P3|NEW] mule-detection/package.json:1-34 — non-uniform line endings within one file: lines 1–5 and 22–34 are CRLF while the `scripts` block body (lines 6–21) is LF — artifact of a partial edit; no prettier/EOL lint configured to catch it — evidence: Read tool flags "Mixed or lone carriage-return line endings"; `.gitattributes` declares no text/EOL policy for it — fix: normalize whole file to LF and consider adding `*.json text eol=lf` to `.gitattributes`.

Explicit slice answers: **package.json scripts reference only existing targets** (`eslint src`, `tsc --noEmit`, standard next commands — no nonexistent-file refs). **public/ filenames**: no `network_graph.json`/`bipartite_network.json`/`hierarchical_hypergraph.json` remain. **next.config.ts / vercel.json / proxy.ts**: clean of dead viz refs.

TOTALS sev=P0:0,P1:0,P2:0,P3:7 status=NEW:6,UNFIXED:1,REGRESSION:0

CLEAN: src/** (all 51 TS/TSX files — zero surviving references to any deleted viz feature; imports/fetches/hrefs all resolve), public/* (filenames), package.json scripts targets, package-lock.json (vis-* fully purged), next.config.ts, vercel.json, src/proxy.ts, src/app/graph/page.tsx, src/components/MuleGalaxy.tsx (selector contract with verify script intact), src/components/plotly/PlotlySankey.tsx, src/types/plotly-partial.d.ts, README.md, CLAUDE.md, design-tokens.md, docs/ML_MODEL_REPORT.md, RESEARCH_PAPERS/*.md