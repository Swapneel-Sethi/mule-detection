# D4-4 [DATA-CONVERT] Files: mule-detection/scripts/convert_csv_transactions.py, generate_dataset_json.py. Focus: CSV column mapping vs actual header of 'transactions_1m (1) (1).csv' at outer root (peek header line only!), amount unit conversions (INR paise?), dedup logic, null/NaN passthrough, output field names vs /api/data-local filters (is_mule, risk_level enum casing), pagination assumptions.
(agent_id=agent-79)

 outcome="completed">Audit complete. All evidence gathered via read-only inspection and streaming Python samples; nothing was modified.

## D4-4 [DATA-CONVERT] — Findings (worst-first)

**[P1|NEW]** `mule-detection/scripts/generate_dataset_json.py:128-131` — emits `ml_score`/`calibrated_score` = `risk_pct` (0–100), contradicting the shipped artifact (0–1) and breaking the sanctioned post-processor — measured: artifact `calibrated_score` 0.053–0.856, `ml_score` 0.262–0.466, while `risk_score` 5.3–85.6; `risk==cal: 0`, `risk==ml: 0` across 105,501 rows. The documented chain (`convert_csv_transactions.py:20-22` → `recompute_transaction_scores.py`) consumes calibrated as 0–1: `base=(cal+cal)/2` (:77-79) then `risk=min(100,max(0,raw*100))` (:102) — with 0–100 inputs every base ≈ tens ⇒ ×100 clamps to 100 ⇒ all 99,952 txns flagged@100, silently. Fix: emit `'ml_score': score`/`'calibrated_score': score` (0–1) keeping only `risk_score`/`behavioral_score` at 0–100, or update recompute to `/100`, plus add a scale assert in both scripts.

**[P2|NEW]** `mule-detection/scripts/generate_dataset_json.py:58-74,132` — `compute_flags()` produces a flag vocabulary that matches neither the shipped data nor the analytics mapper — artifact vocab: `fan_out`×98,231, `fan_in`×32,930, `high_velocity`×11,679, `new_account`×8,923, `pass_through`×5,095, `fanout_source`×3,532, `fanin_receiver`×3,417, `circular_loop`×679, `high_value`×681, `dormant`×48, `alert_flagged`×40; script can only emit `pass_through/high_velocity/new_account/network_hub/high_out_degree/fan_out_suspect/high_value` (zero occurrences of `network_hub`/`high_out_degree`/`fan_out_suspect` in artifact). On regeneration, `canonicalFlag` (`src/app/api/analytics/route.ts:10-16`) maps neither `fan_out_suspect` nor `network_hub` nor `high_out_degree`, and no `fan_in` flag is emitted at all → FANIN/FANOUT pattern analytics silently collapse for ACC rows; UI renders raw unknown chips. Fix: align `compute_flags` to the canonical vocab (`fan_in`/`fan_out`/`pass_through`/`circular_loop`/`dormant`…) or extend `canonicalFlag`.

**[P2|UNFIXED]** `mule-detection/scripts/generate_dataset_json.py:7-10,77` — required input `ml_features_100k.csv` absent at both candidate paths → script unrunnable, artifact unreproducible — verified: `ls` shows no such file at repo root; `dataset_output/` directory does not exist (known master #31 / 04-report C3, still open at HEAD). Fix: commit a sample/pinned copy of the features CSV or delete/archive the script until it exists.

**[P2|UNFIXED]** `mule-detection/scripts/convert_csv_transactions.py:104-119` — naive source timestamps declared UTC (`.000Z` append) though they are IST local wall-clock times — verified 992,941/992,941 CSV timestamps naive; comment :108-111 codifies "treated as UTC" (known 04-report M2). Downstream, JS `new Date("…T20:18:05.000Z")` renders shifted hours/dates in local-tz UI cells and mislabels the hour feature semantics. Fix: append `+05:30` instead of `Z` (or document IST-as-label consistently).

**[P3|NEW]** `mule-detection/scripts/generate_dataset_json.py:52-56` — `risk_level()` bands diverge from shipped labels for 7,463/105,501 rows (7.1%) — measured applying script bands to artifact `risk_score`: `{match: 98038, MISMATCH: 7463}` (artifact was relabeled per swarm fix #6 calibration thresholds; script retains 75/50/25 cut-offs) → regeneration reshuffles the data-local flagged/category universes (`route.ts:71-87`). Fix: derive bands from the same calibration constants the relabel used.

**[P3|NEW]** `mule-detection/scripts/generate_dataset_json.py:129` — `graph_score = hub×1e6` but shipped artifact encodes `hub×~1e5` — measured median ratio `graph_score/hub_score` = 100,069 (sample: hub 1.075e-5 → 1.1); regeneration inflates graphScore 10×, surfaced raw via `normalizers.ts:222`. Fix: use `* 100000` to match artifact, or regenerate + bump consumers together.

**[P3|NEW]** `mule-detection/scripts/generate_dataset_json.py:133-136` — comment claims "firstSeen + account_age_days always stay mutually consistent", but shipped data violates the invariant — measured: 19,982/20,000 sampled ACC rows have `(2026-08-22 − firstSeen).days != account_age_days`; offsets median −129.5 d, range [−489, +235] (e.g. ACC00012E05 age=1086, firstSeen=2024-12-07, expected 2023-09-01). Data repair fixed month-13/inversions but never re-anchored dates; regeneration would silently shift ~all firstSeen dates. Fix: correct the comment and/or backfill firstSeen = anchor − age_days in the data.

**[P3|NEW]** `mule-detection/scripts/generate_dataset_json.py:140-141` — `json.dump(rows, f)` writes default spaced separators, undoing the compaction fix — artifact head bytes `[{"account_id":"ACC00012E05",…` are compact (swarm fix #21 compacted the artifact only, not this writer); rerun regresses ~89 MB → ~95+ MB. Fix: `json.dump(rows, f, separators=(",", ":"))` (as the sibling converter already does at `convert_csv_transactions.py:147`).

**[P3|NEW]** `mule-detection/scripts/convert_csv_transactions.py:117-118` — `endswith("Z")` passthrough accepts any malformed Z-suffixed string unvalidated — regex guards only the naive branch; e.g. `"garbageZ"` returns verbatim. Latent (verified 0 occurrences today: all 992,941 naive, all outputs end `Z`). Fix: require the full ISO regex for the Z branch too.

**[P3|NEW]** `mule-detection/scripts/generate_dataset_json.py:26,85` — brittle boolean parse `row.get('is_mule','') in ('True','true')` silently yields False for `'TRUE'`/`'1'` — input format unverifiable today (CSV absent), but a casing/encoding variant would zero out every mule label with no warning. Fix: normalize (`str(v).strip().lower() in {'true','1','yes'}`) and count/log unexpected truthy-looking values.

**[P3|UNFIXED]** `mule-detection/public/transactions_synthetic.json` (produced by the convert→recompute chain my scripts feed) — mixed riskScore scales persist — measured min 0.1 / max 100.0, 7,469 rows ≤1.0 vs 92,483 on 0–100; flagged 7,959 incl. 18 CSV-NONE rows (known master #29, still shipped). Fix: regenerate uniformly after fixing the P1 above.

**[P3|UNFIXED]** `transactions_1m (1) (1).csv` mode column (mapped through `convert_csv_transactions.py:141`) — 100% of RTGS rows violate RBI's ₹2L minimum — measured 49,496 RTGS txns, file max ₹196,720.91 < ₹200,000 (known 04-report M1, unchanged; converter passes mode through uncritically). Fix: resample/relabel at generation time, not in the converter.

**Verified clean** in this slice: CSV header matches docstring exactly; `mode` enum maps 1:1 to UI rails (`upi|imps|neft|rtgs` — artifact types 59,992/25,195/9,799/4,966 vs `TransactionsContent.tsx:192-196` options); amounts are rupees-with-paise kept as-is (8.49–196,720.91, zero unparseable, zero empty fields — no unit conversion needed, no NaN/null passthrough possible with current data); zero duplicate `txn_id`s (dedup unnecessary); output record shape exactly matches the app Txn shape (8 keys) consumed by `data-local` pagination/filters; `__file__`-anchored paths present in both scripts (fix #19/#29 intact); seeded (42); month-13/inversion formulas fixed and artifact confirms 0 inversions, 0 month-13; alerts hold 0 dangling refs so the converter's stale-ref guard is consistent with post-swarm state.

TOTALS sev=P0:0,P1:1,P2:3,P3:8 status=NEW:8,UNFIXED:4,REGRESSION:0