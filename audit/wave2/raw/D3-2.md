# D3-2 [VIZ-GALAXY-2H] File: mule-detection/src/components/MuleGalaxy.tsx LINES 550-1070. Focus: hover/pick hit-testing math, tooltip state races, animation frame leaks, zoom/pan clamping, detail-panel dialog semantics, keyboard access, memory growth (unbounded arrays), props/refs misuse.
(agent_id=agent-71)

 outcome="completed">Audit complete. All claims below verified against HEAD source, the installed library internals (`3d-force-graph`, `three-render-objects`), and the producer route. Known-item cross-check: D3 fixed items #1/#4/#14/#16 hold at HEAD (no regressions); register A22 (radiusCache staleness) is genuinely fixed at `MuleGalaxy.tsx:521`; nine reported-only items from master #19 remain present.

---

**[P2] [NEW]** mule-detection/src/components/MuleGalaxy.tsx:336 — trace-mode `zoomToFit` frames the whole visible galaxy, not the traced chain, so "Follow the money" dims but never focuses — library walks scene objects and hidden nodes have no objects, so bbox = all visible nodes (unchanged by trace mode, which only recolors via `nodeColor`); `zoomToFit(durationMs?, padding?, nodeFilter?)` exists precisely for this (`node_modules/3d-force-graph/dist/3d-force-graph.d.ts:70`; `getGraphBbox` default filter `() => true` at `3d-force-graph.js:63150-63153`) — pass the third arg: `graph.zoomToFit(700, 90, (n) => traceSet.has(n.id))` with the `traceSet` already built at line 330.

**[P2] [NEW]** mule-detection/src/components/MuleGalaxy.tsx:991-1057 — detail side-panel stays rendered and keyboard-focusable while closed; only `translate-x-full` moves it visually — `panelOpen` initializes `false` (line 180) yet the NETWORK SUMMARY branch mounts from first paint, putting the off-screen `Close` button (994) and, after any selection, trace-chain buttons (1024) and up-to-80 flow buttons (1040/1049) into tab order and the a11y tree; Escape-focus-loss compounds it (focus left on a now-invisible button) — add `inert={!panelOpen}` to the panel div (React 19 supports boolean `inert`) and restore focus to the stage on close.

**[P2] [NEW]** mule-detection/src/components/MuleGalaxy.tsx:723-788 — programmatic zoom/orbit math assumes `controls.target === origin` and bypasses the `minDistance`/`maxDistance` clamps — `zoomCamera` scales camera position about world origin and arrow-keys orbit about origin, but right-drag pan is enabled (`three-render-objects.js:104499` `this.enablePan = true`) moving `controls.target` off origin; per-frame `controls.update()` (`three-render-objects.js:112046`) then re-aims at the panned target and re-clamps radius into [0.5, 1e6], so post-pan keyboard zoom skews/lurches and overshoot snaps back each frame — compute offsets from `graph.controls().target` (or reset target in these handlers) instead of assuming `{0,0,0}`, and clamp the resulting radius before applying.

**[P3] [NEW]** mule-detection/src/components/MuleGalaxy.tsx:951-952 — plain wheel is consumed for zoom over a `min(78vh,880px)` canvas, trapping trackpad page-scroll (same class fixed as L1 for the other three canvases, never applied here) — OrbitControls dollies on bare wheel (`zoomSpeed 1.6`, line 539) and `touch-none` blocks touch scroll — require Ctrl/meta+wheel for zoom or leave `preventDefault` to a guarded handler.

**[P3] [NEW]** mule-detection/src/components/MuleGalaxy.tsx:845-851 — Enter resolves to bank-cluster isolation whenever any bank name contains the query substring, overriding the account suggestions shown to the user — `bankNames.some((b) => b.toLowerCase().includes(...))` (848) wins over `first` even when the dropdown lists an exact account-ID hit whose holder name merely contains the bank string; also a no-match Enter is a silent no-op — prefer `first` whenever suggestions are non-empty and fall back to `focusSearchResult()` only otherwise.

**[P3] [NEW]** mule-detection/src/components/MuleGalaxy.tsx:839-871 — search type-ahead has no combobox semantics: no `aria-expanded`/`role="listbox"`/`role="option"`, and no Arrow-Up/Down traversal (Enter is first-hit only) — screen readers announce nothing about the popup and keyboard users cannot pick suggestion 2..8 — wire `role="combobox"` + `aria-controls`/`aria-activedescendant` with arrow-key index state.

**[P3] [NEW]** mule-detection/src/components/MuleGalaxy.tsx:834-837, 931-935 — view-mode trio and pattern chips convey pressed state by color alone (`bg-frost text-void` / `border-risk-low…`), no `aria-pressed` (or `role="tablist"`/`aria-selected`) — identical to the gap fixed on Sankey toggles (master D3 #17) but never applied to this file — add `aria-pressed={viewMode === value}` / `aria-pressed={activePatterns.has(pattern)}`.

**[P3] [NEW]** mule-detection/src/components/MuleGalaxy.tsx:906-917 — timeline `<input type="range">` exposes bare 0-100 to assistive tech; the human-readable cutoff date sits in an unassociated sibling span — SR users hear "37" not a date — set `aria-valuetext={scrubCutoffDay ? \`through ${scrubCutoffDay}\` : "full history"}`.

**[P3] [NEW]** mule-detection/src/components/MuleGalaxy.tsx:34-35 — `lastDay` field actually stores the corridor's EARLIEST activity day — producer keeps the minimum (`route.ts:105` `if (day && (!existing.lastDay || day < existing.lastDay))`) and both doc comments say "Earliest"; only the identifier lies, inviting a future `>= lastDay` misuse — rename to `firstDay` across payload + consumer.

**[P3] [NEW]** mule-detection/src/components/MuleGalaxy.tsx:1039-1053 — outgoing/incoming flow lists are silently capped at 40 (`slice(0, 40)`, line 312) with no "top 40 of N" hint, unlike every other disclosed cap in this suite — append a muted `Showing top {n} of {total}` row when truncated.

**[P3] [UNFIXED]** mule-detection/src/components/MuleGalaxy.tsx:549-597 — listener/timer rollback window: `introTimer` + `pointerdown`/`wheel` listeners attach at 549-551 but `teardownRef.current` is only assigned at 592-597, so any throw between (ResizeObserver/rAF construction) orphans them (teardown stays noop) — known master #19 item, still present — assign a partial teardown immediately after 551 and overwrite it at 592.

**[P3] [UNFIXED]** mule-detection/src/components/MuleGalaxy.tsx:564-591 — FPS meter calls `setFps` every 500 ms for the component's lifetime, re-rendering toolbar/legend/panel tree forever — known #19, still present — gate updates behind a ref-driven stat cell or stop once engine idle.

**[P3] [UNFIXED]** mule-detection/src/components/MuleGalaxy.tsx:604-634 — hover rebuilds the neighbor highlight by scanning ALL `galaxySnapshot.links` per mousemove (O≈10k) — known #19, still present — precompute an id→neighbors Map alongside `adjacency`.

**[P3] [UNFIXED]** mule-detection/src/components/MuleGalaxy.tsx:655 (also 142, 380) — `bloomRef` is written and nulled but never read; quality tweak mutates the local `bloomPass.strength` (576) instead — known #19, still present — drop the ref or route the strength change through it.

**[P3] [UNFIXED]** mule-detection/src/components/MuleGalaxy.tsx:707-711 — bank-cluster zoom races a fixed 120 ms `setTimeout` against the visibility-effect commit + kapsule digest; on a slow frame it fits the pre-filter scene — known #19, still present — run the fit from the visibility effect (post-`refresh()`) rather than a timer.

**[P3] [UNFIXED]** mule-detection/src/components/MuleGalaxy.tsx:876 (also 559, 471-472) — every `zoomToFit` path centers on the full canvas box, ignoring the 400 px overlay panel, so the fitted cloud's right flank hides beneath the panel — known #19, still present — shrink the fit viewport (e.g., pad asymmetrically or fit into `width - 400` when `panelOpen`).

**[P3] [UNFIXED]** mule-detection/src/components/MuleGalaxy.tsx:931 — `patternCounts.slice(0, 12)` saturates with no "+N more" affordance when datasets carry >12 flags — known #19, still present.

**[P3] [UNFIXED]** mule-detection/src/components/MuleGalaxy.tsx:970-983 — legend swatches contradict rendered channels: "Flagged corridor" swatch `#f87171` vs actual ribbon `rgba(239,69,98,.26)` (line 398), and "High-value flow" slate swatch implies a distinct color though high-value only thickens the existing flagged/context colors (400) — known #19, still present.

**[P3] [UNFIXED]** mule-detection/src/components/MuleGalaxy.tsx:1018-1032 — BFS trace walks the full unfiltered `adjacency` (283-303), so the chain lists and click-flies-to (via `focusNode`, 676-694) nodes hidden by tier/pattern/bank filters; isolated degree-0 accounts additionally get a clickable no-op "Follow the money" button (1012) — known #19/#27, still present — intersect the walk with `visibleIds` and disable the toggle when `tracePath` is null.

TOTALS sev=P0:0,P1:0,P2:3,P3:16 status=NEW:10,UNFIXED:9,REGRESSION:0

CLEAN (verified-defect-free spans of the slice): mule-detection/src/components/MuleGalaxy.tsx lines 660-675 (searchSuggestions memo), 799-827 (stats/error/loading gates), 938-942 (help copy matches implemented keys), 1060-1067 (bottom stat grid); lifecycle/RAF/observer cleanup otherwise audited sound (single RAF loop, disposed-guarded callbacks, full `_destructor` + geometry/material dispose).