# D5-3 [PAGE-ANALYTICS] File: mule-detection/src/components/AnalyticsContent.tsx. Focus: fetch abort/unmount guards, spinner-forever on failure, Account Categories double-count (mule ∩ high-risk overlap), chart color collisions, Sankey data key derivation, recharts lazy vs eager imports, division-by-zero in derived percentages, axis labels claiming IST while data UTC.
(agent_id=agent-84)

 outcome="completed">Audit complete. All checks done (HEAD code read line-by-line, contracts proven against `/api/analytics`, `/api/data-local`, `SankeyChart`, tokens, utils, dataset sampling via Python replication, `tsc --noEmit` exit 0).

**Verified fixed at HEAD (not re-reported):** H4 (abort/error/retry now wired), M1-intra-page double-count (bars now disjoint within Analytics), M2 ₹Cr unit bug, A2 txnByPattern double-count (dedup confirmed), A12 mixed-TZ (all buckets IST now), L1/L2 dead code removed, M10 metadata title, L16 `accountsTotal` now consumed. No REGRESSIONs found.

[P1|NEW] src/app/api/analytics/route.ts:345-353 (+ consumers src/components/AnalyticsContent.tsx:200-224, src/components/SankeyChart.tsx:116) — `sankeyByPattern` sliced to top-20 corridors **before** shipping, so every Sankey pattern-node total and the filter chip's "{flows} · {accounts} · {₹total}" reflect ≤5% of true pattern volume, silently contradicting the "Transaction Amount by Pattern" bar chart on the same page — Python replication of the exact aggregation: corridors FANIN 3,235 / FANOUT 3,333 / PASSTHROUGH 697 / CIRCULAR 679; top-20 carries **1.12% / 1.96% / 5.10% / 5.38%** of volume (e.g. chip shows ~₹0.10 Cr for FANIN vs bar's ₹8.73 Cr, ~90× gap) with no "top 20" disclosure anywhere — aggregate non-top-20 corridors into explicit "Other" flows/nodes (or ship full aggregation, cap only at render) and qualify the chip copy.

[P2|NEW] src/components/AnalyticsContent.tsx:182-189,461-480 — "Account Categories"/Summary parity claim is false: Analytics shows Mule **1,962** / High Risk **6,616** (analytics `muleCount`=mule∧severity-tier) while Dashboard renders **8,578** / **0** (data-local `route.ts:173-178` counts ALL mules / non-mule-severe) — measured from dataset: severe-tier mules 1,962; non-mule severe 0; comment "identical numbers to the Dashboard's Account Categories card" (and analytics route.ts:69-75, which additionally mischaracterizes data-local's formula as "confirmed mules BELOW the severity tier") are both wrong — pick one canonical disjoint definition in both APIs and correct all three comments (DashboardContent.tsx:58-62 fallback encodes yet a third matching-analytics formula that API stats always override).

[P2|UNFIXED] src/components/AnalyticsContent.tsx:171,172-180 — FANOUT bar fill `var(--color-charcoal)`=#1a1a1a on `--color-void`=#000 (contrast ≈1.2:1, globals.css:4,6) — near-invisible primary series, AND identical to the dimmed fill applied to other bars when any filter is active so dimmed-vs-FANOUT states collide; residual half of L3 (only CIRCULAR got recolored, despite comment :31-33 claiming distinctness) — give FANOUT a literal hex like its siblings (e.g. reuse #f6ad55 family) and dim via opacity, not a shared token.

[P3|NEW] src/components/AnalyticsContent.tsx:154-159 — Retry button calls `loadAnalytics()` with no AbortSignal — unmount mid-retry leaves the fetch running and sets state post-unmount; rapid retries race last-response-wins with no supersede guard (effect path :137-144 has this protection, manual path doesn't) — keep a ref'd controller and abort it in cleanup/next load.

[P3|NEW] src/components/AnalyticsContent.tsx:167-171 vs 38-43,47-50 — same pattern wears unrelated hues across charts on one page (FANIN #7fd1f0 in line/Sankey/chip vs frost #e2e2e2 bar; FANOUT orange vs near-black; PASSTHROUGH #b8bab9 vs ash #888888; CIRCULAR #ef6c6c vs #e15759) — chip dot highlights a bar in a different color than the dot — derive bar fills from `PATTERN_LINES`/`PATTERN_DOT_COLORS` instead of CSS vars.

[P3|NEW] src/components/AnalyticsContent.tsx:52-75 — dead consumer contract: `muleAccounts`, `cleanAccounts`, `inOutData` (whole shipped top-20 array), `moneyFlowData.amount`, `volumeByDay.transactions` are typed and shipped by the API but rendered nowhere (orphaned "In vs Out" feature) — drop from payload+interface or render the card.

[P3|NEW] src/components/AnalyticsContent.tsx:438-444 — Money Flow category tick labels ("critical→critical" ≈17 glyphs × ~5.4px at 9px JetBrains Mono ≈ 92px) exceed `width={70}` — longest risk-pair ticks clip/overlap the plot area — raise width to ~96 or abbreviate levels (CRIT/HIGH/MED/LOW).

[P3|NEW] src/components/AnalyticsContent.tsx:121-122 — `json.error` branch is unreachable dead code: the route only returns `{error}` with HTTP 500 (api/analytics/route.ts:392-398), which already throws at `!r.ok` :118, so the server's reason string never reaches the UI — fold the message extraction into the `!r.ok` path.

[P3|NEW] src/components/AnalyticsContent.tsx:146-165 — loading and error branches render bare content with no `<PageHeader>` unlike Dashboard/Transactions' loading/error states (D5#12 convention); redundant `error ? … : ""` ternary inside a branch where `error` is guaranteed truthy — add PageHeader, drop ternary.

[P3|UNFIXED] src/components/AnalyticsContent.tsx:320-323,419-422,512-514 — `key={index}`/`key={i}` on Cells and circular-path cards (L4) — key by `entry.pattern` and `path.from+path.via+path.to`.

[P3|UNFIXED] src/components/AnalyticsContent.tsx:237 — hardcoded X-axis caption "Date [Aug 2026]" (L5, master D6#29) — derive month from data range.

[P3|UNFIXED] src/components/AnalyticsContent.tsx:197 — `<StatCard label="Alerts" value={data.totalAlerts}>` passes raw number → compact `formatNumber` path, inconsistent with sibling cards' `toLocaleString("en-IN")` (L15; impact nil until count ≥1000) — preformat the string.

[P3|UNFIXED] src/components/AnalyticsContent.tsx:4-18 — static eager recharts import bundle hazard (S17) while sibling Plotly is next/dynamic lazy — lazy-load chart blocks or accept documented cost.

[P3|UNFIXED] src/components/AnalyticsContent.tsx:200-224 + api/analytics/route.ts:328-330 — `account_id.slice(-6)` corridor keys merge distinct accounts and the chip's `{accts}` count undercounts (A11; measured at HEAD: 4 colliding suffix groups covering 8 mule accounts) — key by full id, slice only for display.

[P3|UNFIXED] src/app/api/analytics/route.ts:178-196 + AnalyticsContent.tsx:354-380 — "Transaction Volume Over Time" aggregates ALL mule-touching txns incl. unflagged while the adjacent "Flagged Turnover" stat and every sibling chart are flagged-only (A24) — align universes or label the chart.

[P3|UNFIXED] src/app/api/analytics/route.ts:168-176 + AnalyticsContent.tsx:359-363 — day keys `.slice(5)` MM-DD merge across years (A25; benign for single-month Aug-2026 data) — keep YYYY-MM-DD internally, format for display.

[P3|UNFIXED] src/app/api/analytics/route.ts:292,276,376 + AnalyticsContent.tsx:512-524 — cycle dedup key `[from,via,mid].sort()` destroys traversal orientation so displayed "A→B→C→A" order is arbitrary; result silently capped `slice(0,10)` of 41 detected cycles with no hint; self-loop/2-node degenerate cycles tolerated (latent — 0 self-loops, 0 reciprocal pairs, 41 well-formed cycles measured) — preserve direction in dedup key, surface the cap.

TOTALS sev=P0:0,P1:1,P2:2,P3:14 status=NEW:8,UNFIXED:9,REGRESSION:0