# V-D4-6 [SYNTHETIC-DATA VERIFY - READ-ONLY] Do NOT edit. Machine-verify current artifacts per audit/wave2/raw/D4-6.md methodology (python sampling; never cat &gt;10MB files): accounts_dataset.json/alerts_synthetic.json schema vs src/app/api/data-local/route.ts expectations, enum casing, score ranges, id uniqueness, alert-&gt;account referential integrity, kyc_status presence, date formats, counts vs UI-quoted numbers. Write measured results to audit/wave2/VERIFY-D4-6.md

outcome="completed">V-D4-6 complete. Report written to `C:/MISCELLANEOUS PROJECTS/SIH_2026/1/audit/wave2/VERIFY-D4-6.md`; no other file touched, no git, no builds.

## FIXED
None — read-only verify line; only `audit/wave2/VERIFY-D4-6.md` written.

## SKIPPED
None — all checks executed (full-population python census of both artifacts; 92MB file never printed, only parsed).

## HANDOFF
- **Artifact contract PASSES**: top-level arrays match `route.ts` cast; exact field names on 105,501/105,501 account rows and 155/155 alerts; `risk_level` lowercase with non-overlapping bands (low ≤28.3 / med 56.0–63.7 / high 64.8–66.1 / crit ≥67.2); `risk_score` 5.3–85.6 all numeric; `is_mule` strict bool 8,578/96,923; ids unique both files; alert→account refs 171/171 resolve; `kyc_status` present on all rows (`'1'`×83,542/`'0'`×21,959); dates ISO-only, no epoch mix, no month-13, no chronology inversions.
- **Still broken in artifact bytes** (owner = data pipeline): P2 `pass_through_ratio` max 1.173e10 with 21,218 rows >1e6 (epsilon-divide; `ACC00004BEC` tin=0 reproduced); P2 `account_age_days` on 0/7,001 ACM mules; P3 velocity dual-scale (ACC 0–1116 vs ACM 0–0.139), graph_score dual-scale (0–5 vs 0–2.5), ACM pagerank identically 0, hardcoded `lastActivity="2026-08-22"` ×98,500, 40 empty-`firstSeen` stubs, dual flag vocab.
- **Src-side still open** (owners): AlertsContent dead filter options (`low`/`info` severity — 0/155; `dismissed` status — 0/155); xgboostPredictor.ts ~210-213 stale "kyc_status/account_type/authority_score unavailable" comment contradicted by artifact.
- **Fixed at HEAD since snapshot** (no action): route `resolved===true` disjunct removed (`route.ts:203-211` now status-string only); Dashboard fallback now severe-only matching route's severe-only `muleCount` (`route.ts:176-178` ↔ `DashboardContent.tsx:54-63`).
- **Orchestrator note**: flag-vocabulary census differs from D4-6.md's numbers (new tokens `new_account` fleet-wide on ACM, `high_value`, `alert_flagged`) while every structural metric matches → dataset regenerated between snapshot and now; sha256 pins recorded in report.

## EXTERNAL
None surfaced.

## DEFERRED
None.

## NOTES
UI-quoted counts verified correct: `DashboardContent.tsx:53` fallback 105501 ✓; `AccountsContent.tsx:47` "8,578 All Flagged" ✓ (all 1,962 critical/high rows are mules, so flagged universe == mule count == 8,578). `explanation` key absent fleet-wide (benign, nullable in `MappedAccount`). eslint self-check skipped — non-src scope, zero source edits.