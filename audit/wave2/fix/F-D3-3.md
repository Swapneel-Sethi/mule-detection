# F-D3-3 [GRAPHS-VIZ] OWN: mule-detection/src/app/graph/page.tsx, mule-detection/src/app/api/graph/mule-galaxy/route.ts | FINDINGS: audit/wave2/raw/D3-3.md | P1: add TTL + stale-while-recompute to cachedPayload (copy pattern from api/analytics/route.ts) and render-or-delete dead generatedAt (coordinate-by-comment: MuleGalaxy.tsx is another agent's file — if the fix belongs there, put it under HANDOFF)

outcome="completed">## FIXED
- `src/app/api/graph/mule-galaxy/route.ts:60-79,249-289` — **P1**: replaced never-invalidated `cachedPayload` with `CacheEntry {payload, signature, builtAt}`; rebuilt when either JSON's mtime+size signature changes or `CACHE_TTL_MS=60_000` elapses; rebuild runs in background while stale payload serves (stale-while-recompute); concurrent requests share one `inflight` build (stampede dedupe); build failure logs server-side and keeps serving last-good payload instead of 500-ing/re-parsing ~110 MB per request.
- `route.ts:180` — **P2 (half)**: response `Cache-Control: no-store` → `public, s-maxage=60, stale-while-revalidate=300`, copied verbatim from `api/analytics/route.ts:390`.
- `route.ts:158-161` — **P3**: score ternary collapsed to `calibrated_score * 100` (dataset max 0.856, `risk_score ≡ ×100`; clamp retained); dead boundary branches + mixed-scale hazard removed.
- `route.ts:196-198,215` — **P3**: redundant `validLinks` filter and now-purposeless `nodeById` map dropped; endpoint↔node-id invariant kept documented at the `accountIds` comment.
- `route.ts:31-33,113-121` — **P3 [NEW]**: `lastDay` buckets via new `istDay()` (`Intl.DateTimeFormat("en-CA", {timeZone:"Asia/Kolkata"})`, same pattern as analytics) fixing UTC-slice off-by-one for 00:00–05:30 IST activity; unparsable stamps return `""` without dropping the txn's amount/count. Format stays `YYYY-MM-DD` so MuleGalaxy's lexicographic scrubber comparisons are unaffected.
- `route.ts:107-112,131-134,205-212` — **P3 [NEW]**: single `riskLevel()` normalization helper now feeds both the universe filter (`isHighRiskLevel`) and node tier mapping; casing drift can't split the paths again.
- `route.ts:41-53` — **P1b**: dead `generatedAt` **deleted** (grep: zero readers; only a declaration in MuleGalaxy.tsx:44). Dead `meta.totalVolume` also deleted (declared MuleGalaxy.tsx:50, never read).
- `src/app/graph/page.tsx:8-12` — **P3 [NEW]**: title `"Network Graph"` → `"Graph"` (matches Sidebar.tsx:12); description `"ML-flagged"` → `"flagged"` (universe is rule band; `ml_score` never consulted).

## SKIPPED
- none outright; two P3s moved to HANDOFF because the fix lands in a file I don't own.

## HANDOFF
- **MuleGalaxy.tsx:44,50** — remove now-dead `generatedAt` / `totalVolume` from the local payload interface (route no longer emits them; both were unread, so nothing breaks meanwhile).
- **A29 rename (coordinate, atomic)**: `meta.mules`→`severeMules`, `meta.highRisk`→`watchlist` in route.ts:224-225 + labels/readers at MuleGalaxy.tsx:802-803. Current semantics inverted vs sibling APIs (galaxy `mules`=severity-tier only ≈1,962; `highRisk`=watchlist ≈6,616).
- **MuleGalaxy.tsx** — preferred fix for the 53 isolated flagged nodes is a client-side degree-0 badge/filter; server-side filtering would desync `meta.nodes` from other pages' flagged counts (product call).

## EXTERNAL
- none.

## DEFERRED
- **P2 remainder**: precomputing a small static galaxy JSON at dataset-generation time (generator pipeline, outside my line) and `functions.memory`/`maxDuration` tuning (S13/S10 — deployment-target config, platform-specific API).

## NOTES
- Verification: `npx eslint src/app/api/graph/mule-galaxy/route.ts src/app/graph/page.tsx` → exit 0, zero warnings. Grep confirms no stale identifiers (`cachedPayload|loadJson|validLinks|fallbackScore|generatedAt|totalVolume|nodeById`) remain. No tsc/git/build per hard rules.
- Pattern note: analytics/route.ts has **no internal TTL anymore** — its current pattern is process-lifetime parse cache + CDN `s-maxage=60, swr=300`. I copied that layering and added mtime-signature invalidation on top because galaxy caches a *derived* payload where post-regeneration visibility was the actual P1 defect.
- `page.tsx` changed under me mid-edit (another agent had already removed `"live"`); I re-read and applied only the remaining title/"ML-" fix.
- AGENTS.md's release-automation block (graphify update → build → commit → push → deploy) conflicts with this wave's hard rules (NO git, NO builds) — not executed; orchestrator owns release steps.