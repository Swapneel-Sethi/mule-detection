# F-D5-5 [PAGES-API] OWN: mule-detection/src/lib/normalizers.ts, src/app/api/data-local/route.ts | FINDINGS: audit/wave2/raw/D5-5.md | Param whitelisting, cache TTL, resolved double-count, silent caps made visible; align flagged-filter predicate with analytics route's (that file is another agent's — align TO their documented definition per D5-6.md, note under HANDOFF if contract change needed)

outcome="completed">## FIXED

`src/app/api/data-local/route.ts`
- :7-40 — P2 REGRESSION: replaced swallow-catch loaders that poisoned cache with `cachedAccounts = []`. New shared `loadDataset()`: promise-deduped cold-start loads (stampede fix), failures logged + rethrown, never cached → retryable.
- :63-80 — same finding: load failure now returns HTTP 503 `{error:"Local dataset temporarily unavailable"}` instead of 200-with-zeros; client (`useLocalData`) already errors on `!res.ok`.
- :53-59, 91-104 — P2 param whitelisting: `risk` validated against {critical,high,medium,low} else no-op; `category` validated against {all,mule,high} else "all"; `q` capped at 100 chars.
- :207-218 — P2 turnover predicate aligned to `/api/analytics`: loop now requires `txn.flagged === true` ∧ endpoint-in-flagged-universe (verified identical to analytics/route.ts:116-122 and universe predicate at :57-61); false "so Dashboard, Analytics, and data-local agree" comment rewritten truthfully. Dashboard Turnover now shows the flagged-only ₹25.83 Cr figure.
- :128-143 — P3: O(100k)-row txn double scan now guarded behind `if (includeTransactions)`.
- :15, 145-158 — P3 silent `slice(0,500)` cap made visible: `MAX_ALERTS` const + payload keys `alertsTotal` (true dataset count) / `hasMoreAlerts`.
- :239-256 — P3 dead stats keys removed after grep-verifying zero consumers: `turnover`, `avgRisk`, misnomer `alertsTotal`, `alertsResolved`, `riskDistribution` (+ dead `riskCounts` accumulation).
- :145, :232, :251 — P3 CP1252 mojibake restored to real em-dashes (3 spots).

`src/lib/normalizers.ts`
- :252-258 — P3 false "<60 undercounts badly" comment rewritten to the honest rationale (disjoint tier rule mirroring both routes' flagged-universe definition).
- :160-173 — P3 `nonNeg` silent clamp: warn-once `console.warn` for present-but-invalid values (negative counts/junk); absent fields still default silently.
- :266-300 — P3 fallback `computeStats`: optional third param `transactions?: {flagged?: unknown}[]` (backward-compatible — existing call site untouched); when passed counts `flagged === true` rows matching data-local; false "there is no per-transaction flag here" comment replaced; page-scoped nature of `totalTransactions` documented honestly.
- :1-5 — P3 17 orphan fields: interface documented as deliberate API-shape preservation.

## SKIPPED
- **TTL on dataset cache** (P2#4): datasets are immutable artifacts; process-lifetime caching matches analytics' documented owner-intent policy. Implemented the failure-retry and stampede halves instead.
- **Pruning 17 unused MappedAccount fields**: constructor sites exist outside my line (mockData shape deps); audit's alternative option (document as API-shape preservation) taken.
- **Sort whitelisting** (part of P2#3): already fixed in current source — `SORTABLE_FIELDS` set with safe fallback at route.ts:116-117; finding was stale on that point.

## HANDOFF
- **useLocalData.ts:127 owner**: pass `json.transactions` as third arg to `computeStats(mappedAccounts, mappedAlerts, json.transactions)` to activate the new aligned flaggedTransactions path (one-line change); D5-6 P2 residual (page-scoped `totalVolume`/`totalTransactions` approximations in the fallback) also resolved there.
- **analytics/route.ts owner**: my turnover alignment assumes their flagged-universe stays `is_mule || severityTier` — if D5-6 P1 changes that universe, data-local's computeStats must follow.
- **Contract change notice**: data-local response lost 5 stats keys, gained top-level `alertsTotal`/`hasMoreAlerts`; load failure changed from 200-empty to 503-`{error}`. Grep-verified no consumer breaks; `riskDistribution` was optional in useLocalData's Stats type.

## EXTERNAL
None.

## DEFERRED
None beyond HANDOFF items.

## NOTES
- Verified: `npx eslint src/lib/normalizers.ts src/app/api/data-local/route.ts` exits clean; full post-edit re-read of route.ts confirms coherent final state; grep confirms zero mojibake bytes remain in either file. Project-wide tsc left to central verification per instructions.
- mule-detection/AGENTS.md release-automation block (graphify update / build / commit / push / Vercel deploy) conflicts with this wave's hard rules (no git, no builds) — not executed; orchestrator decision needed centrally.
- Pre-existing dead local `alertSeverityCounts` (computed, never returned) left intact inside computeStats per minimal-diff discipline — candidate for a future cleanup wave.