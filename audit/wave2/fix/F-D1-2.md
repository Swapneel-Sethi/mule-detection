# F-D1-2 [UI/UX] OWN: mule-detection/src/app/layout.tsx, error.tsx, global-error.tsx, loading.tsx, not-found.tsx, page.tsx, accounts/page.tsx, alerts/page.tsx, analytics/page.tsx, graph/page.tsx, transactions/page.tsx | FINDINGS: audit/wave2/raw/D1-2.md | ALSO scan audit/wave2/raw/D1-6.md for findings located in YOUR files

outcome="completed">## FIXED

- `src/app/layout.tsx:6` — Inter weights `["400","600","700"]` → `["400","700"]`; weight 600 had zero consumers (grep: only 4×`font-medium`, all paired with `font-mono` → JetBrains Mono 500). Drops dead ~15KB woff2 + preload.
- `src/app/layout.tsx:24` — `twitter.card: "summary_large_image"` → `"summary"`; no `og:image`/`metadataBase` anywhere in src (grep confirmed), so large-image card rendered broken.
- `src/app/layout.tsx:37-40` — added `focus:outline-none` to `<main id="main-content">` so keyboard-activated skip link no longer draws a `:focus-visible` ring around the whole viewport; comment updated to explain why. Done in-file (Tailwind variant) rather than the audit's globals.css rule, since globals.css is outside my ownership line. Safe: `main` has `tabIndex={-1}`, reachable only via programmatic focus.
- `src/app/error.tsx:21` — added `role="alert"` to route-error container (WCAG 4.1.3 announcement).
- `src/app/global-error.tsx:38` — added `<meta name="viewport" content="width=device-width, initial-scale=1"/>` beside `<title>`; standalone document previously laid out at ~980px virtual width on mobile. React 19 hoists it into head, same mechanism the bundled docs endorse for `<title>` here.
- `src/app/page.tsx:10` — dashboard metadata "risk distribution" → "flagged-account category split" (matches the two-band CategoryBar actually rendered).
- `src/app/accounts/page.tsx:10` — "Browse 100k+ analyzed accounts … KYC and behavioral flags" → "Browse 8.5k+ flagged accounts — mule classifications, risk scores and behavioral flags" (fixes both the ~12× universe overclaim and the nonexistent-KYC claim in one rewrite).
- `src/app/alerts/page.tsx:10` — "alerts raised by the detection engine" → "Precomputed fan-in, fan-out, rapid-movement and behavioral-change alerts from the trained-model pipeline" (engine is unwired; displayed alerts are precomputed records).
- `src/app/graph/page.tsx:11` — dropped "live" from description; API serves immutable JSON artifacts per request.

## SKIPPED

- None outright. One finding routed to HANDOFF instead (fix lives in a file I don't own).

## HANDOFF

- **Sidebar.tsx owner**: shared-constant fix for the duplicated 200px sidebar width (D1-2 #11, dup'd in D1-6). `Sidebar.tsx:61` holds `w-[200px]`; my `layout.tsx:39` still carries `lg:ml-[200px]`. Extracting `SIDEBAR_WIDTH_PX`/CSS var requires editing Sidebar.tsx — not mine to touch.
- **All other D1-6 findings** (AnalyticsContent FANOUT contrast P1, MuleGalaxy aria-pressed/contrast/combinator/perf, AlertsContent PageHeader parity, StatCard locale split, EmptyState role, Button/LoadingState spinner, TransactionsContent empty th + tz formatting, globals.css scrollbar, icon strategy, palette tokenization) target components/globals.css owned by other lines — unmodified, as reported in `audit/wave2/raw/D1-6.md`.

## EXTERNAL

- None arising from this scope.

## DEFERRED

- `mule-detection/AGENTS.md` release-automation block (graphify update, build verify, commit, push, Vercel deploy) — every step is forbidden by this wave's hard rules (no git, no builds); left to orchestrator.

## NOTES

- Verification: `npx eslint` on all 7 changed files → clean (exit 0, no warnings). Project-wide `tsc` deliberately not run per instructions. No builds/dev servers run.
- Consulted bundled Next 16 docs (`node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/error.md`) before touching boundary files: `retry` stable since v16.3.0 (already used correctly — no change needed there), global-error must render its own `<html>/<body>` and can't export metadata — my `<meta>` addition follows the documented `<title>`-component pattern.
- `loading.tsx` and `not-found.tsx`: CLEAN in both audits — untouched.
- D1-6 contributed only two duplicate findings already covered above (layout twitter card, accounts metadata); nothing new in my files from that sweep.
- Total diff: 7 files, ~12 changed lines plus a 2-line comment — minimal-diff discipline held.