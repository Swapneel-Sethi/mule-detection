# F-D1-4 [UI/UX] OWN: mule-detection/src/components/ui/DataTable.tsx, EmptyState.tsx, ErrorState.tsx, LoadingState.tsx | FINDINGS: audit/wave2/raw/D1-4.md | ALSO check D1-6.md for your files

outcome="completed">## FIXED
- `src/components/ui/DataTable.tsx:5-30,44-50,54-60,65-78,110` — D1-4 F1: deleted unreachable dead code (whole sort API `sortable/sortBy/sortOrder/onSort`, `defaultRender`, inline skeleton branch + `loading/loadingCount` + `Skeleton` import) per the audit's first-choice remedy; grep proved zero external references. `Column.render` is now required (safe: all 15 columns across the 3 call sites supply it — Alerts 7, Transactions 8, Accounts 3). Removal note left at :5-8 so the latent th-button/aria-sort gaps aren't reintroduced blind. File 194→120 lines.
- `src/components/ui/DataTable.tsx:54-60` — D1-4 F5: loaded wrapper no longer emits a generic `"Data table"` region; `role="region"`+`aria-label` only when a real `caption` exists (Alerts keeps its named landmark; Accounts/Transactions lose the duplicate). Empty branch (:46) drops its message-named region entirely. `tabIndex={0}` kept on the scroll container.
- `src/components/ui/EmptyState.tsx:41-46` — D1-4 F2 = D1-6 dup: root is now `role={variant === "error" ? "alert" : "status"}`, so ErrorState announces assertively.
- `src/components/ui/ErrorState.tsx:18-20` — comment updated to match new EmptyState role behavior (still no nested live region).
- `src/components/ui/LoadingState.tsx:12-20,35,43` — D1-4 F6: byte-identical spinner SVG ×2 extracted to file-local `SpinnerSvg({className})`; sized `h-5 w-5` / `h-8 w-8` as before.

D1-4 F3 (skeleton `<td>` dropping width/classname/align) and F4 (loading wrapper dropping `className`/`caption`) are moot — resolved by the F1 branch deletion.

## SKIPPED
- None. All 6 owned P3 findings fixed or mooted.

## HANDOFF
- **Button owner** (D1-6 P3): Button.tsx:50 spinner is the third copy with fixed `h-4 w-4`. I kept my extraction file-local because a shared `ui/Spinner` would require editing Button.tsx, which I don't own. If that owner creates one, LoadingState can adopt it in two lines.
- **TransactionsContent owner** (D1-6 P3): nameless empty `<th>` (`header: ""`) at TransactionsContent.tsx:99 is caller-side column data — not fixable from DataTable.
- D1-6 icon-strategy item cited DataTable's ▲▼ sort glyphs — moot for this file now; remaining clauses belong to other owners/orchestrator.
- Orchestrator: mule-detection/AGENTS.md mandates release sync (`graphify update .`, build, commit, push, Vercel deploy) — deliberately NOT run; conflicts with wave HARD RULES (no git/builds/generators). Run centrally after waves land.

## EXTERNAL
- None arising from my four files.

## DEFERRED
- Nothing for my line.

## NOTES
- Verification: `npx eslint <4 changed files>` from mule-detection/ → exit 0, zero warnings. Pre-edit greps confirmed sort/loading props and `defaultRender` appear only inside DataTable.tsx, and nothing imports `Column`/`DataTableProps` directly. Project-wide tsc intentionally not run (orchestrator verifies); residual type risk is nil-to-low given the render-supplied proof above. Row-key `""` guard, width/align/headerClassName interpolation, striped/hover logic all preserved verbatim. No git commands, no builds, no secret files touched.