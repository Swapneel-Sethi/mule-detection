# F-D3-6 [GRAPHS-VIZ] OWN: mule-detection/docs/** (stale references), mule-detection/README.md is ANOTHER AGENT'S — hands off. You MAY propose package.json script changes only under NOTES (package.json owned elsewhere). Delete nothing tracked. Fix surviving references to deleted NetworkGraph/HierarchicalHypergraph/Bipartite features wherever they fall in docs/** ; findings source: audit/wave2/raw/D3-6.md

outcome="completed">Sweep complete. Verification evidence: three case-insensitive Grep passes over `mule-detection/docs/**` (`NetworkGraph|HierarchicalHypergraph|Bipartite|bipartite|network_graph|hierarchical_hypergraph`, then `hypergraph|vis-network|vis-data`, then prose variants `pairwise|crime network|force.?directed|edge color`) returned **zero matches**; full read of the sole doc file confirms it makes no viz-architecture claims at all. This independently corroborates D3-6's CLEAN listing of `docs/ML_MODEL_REPORT.md`. No files changed, so no eslint pass required.

## FIXED
None — `mule-detection/docs/**` contains exactly one file (`ML_MODEL_REPORT.md`, 2.7 KB), and it has zero surviving references to deleted NetworkGraph/HierarchicalHypergraph/Bipartite features or any viz stack. Nothing to remediate in owned scope.

## SKIPPED
Nothing skipped within scope; all seven D3-6 findings target files outside my line (see HANDOFF).

## HANDOFF
All D3-6 findings are outside `docs/**` — for their owners/orchestrator:
- `mule-detection/SIH_AUDIT_REPORT.md:207,309,337` (P3) — stale viz claims: praises deleted NetworkGraph edge colors, nonexistent vis-network integration, git-status snapshot naming deleted `src/components/NetworkGraph.tsx`. Root-level md, not in docs/**; not edited.
- S14 tracked junk (P3): `constellation_verify.png` (441 KB, repo root), `scripts/verify_constellation_tmp.py`, ~50 tracked `graphify-out/cache/**` files despite `.gitignore` — needs `git rm --cached`, which is forbidden here.
- `scripts/verify_constellation_tmp.py:17` (P3) — unmanifested `playwright` dep; no requirements/pyproject exists in mule-detection/.
- `detect_step.py:1-6` (P3) — one-off scaffolding writing now-nonexistent `graphify-out/.graphify_detect.json`.
- `scripts/visualize_sankey.py:8-46,82` (P3) — demo rendering purely random fabricated flows; shares nothing with production PlotlySankey pipeline.
- `graphify-out/2026-08-24/` dated archives (~2.2 MB, oldest references deleted sources) — index-prune candidates.

## EXTERNAL
None arising from my scope.

## DEFERRED
None.

## NOTES
- package.json (owned elsewhere): finding #7 proposes normalizing mixed CRLF/LF line endings to LF across the whole file and optionally adding `*.json text eol=lf` to `.gitattributes`. No script-target problems exist — all scripts reference existing files.
- `mule-detection/AGENTS.md` carries a release-automation block (graphify update / build / commit / push / Vercel deploy); it directly conflicts with this wave's hard rules (no git, no builds), so it was disregarded for this task per instruction precedence. Flagging so the orchestrator can run its central release flow if desired.