# V-D1-6 [UI/UX VERIFY - READ-ONLY] Do NOT edit source. Task: after skimming audit/wave2/raw/D1-6.md, spot-verify its 33 cross-cutting findings against CURRENT HEAD (other fixers are editing concurrently — judge what you see, note 'fixed-by-another/still-present/unverifiable'). Write results to audit/wave2/VERIFY-D1-6.md. Also grep app-wide for any REMAINING phantom classes/tokens referenced in tsx but undefined in globals.css.

outcome="completed">Verification complete. Report written to `audit/wave2/VERIFY-D1-6.md` (no source files touched, no git, no builds).

## FIXED
- None — D1-6 is a read-only verify line; only `audit/wave2/VERIFY-D1-6.md` was written.

## VERIFICATION RESULT (33 findings)
- **32 STILL-PRESENT** at HEAD (refs re-anchored to current lines): P1 FANOUT invisible bar (`AnalyticsContent.tsx:171`, charcoal #1a1a1a on surface-1 #111111); P2 zero `aria-pressed` in MuleGalaxy (only hit repo-wide is `SankeyChart.tsx:237`), `/70` contrast pair (`MuleGalaxy.tsx:842,:938`; FilterBar twin confirmed fixed), 44× sub-11px text + Recharts `fontSize: 9` (`AnalyticsContent.tsx:389,:441`); all 29 P3s including Alerts parity regression, dead `.chart-accent-*`/`.risk-badge-*` CSS + SankeyChart `#444345` (:259), RiskBadge orphaned, v2.4 vs 0.1.0, twitter card overclaim, fabricated `safeStat(s.totalInDataset, 105501)` + route emitting `totalInDataset: 0` (:236), wrapper split, IST-vs-local tz mismatch, graph perf trio.
- **1 FIXED-BY-ANOTHER**: StatCard locale-split (#6) — file was concurrently rewritten with a pinned en-IN compact formatter; `formatNumber`/`getUserLocale` now have zero component callers.

## PHANTOM-CLASS SWEEP (extra task)
- Machine-scanned 343 className sites + all string literals vs globals.css definitions and Tailwind v4 palette: **zero phantoms remain**. All flagged candidates were valid core utilities on manual review (`border-t-frost`, `ring-1`, `outline-offset-2`, `-translate-x-full`, …). Scratch scanner deleted after use.

## HANDOFF
- Fix priority + current line refs are in the report's FINDING-BY-FINDING section; quick wins flagged (EmptyState role ternary, aria-hidden ✕ spans, "Turnover"→"Flagged Turnover", drop Info option, delete dead tokens).
- Orchestrator: include `ui/StatCard.tsx` (concurrent rewrite) and `AnalyticsContent.tsx` error-state wrapper change in the central eslint/tsc pass. Raw-digest lines drifted up to ~25 lines; VERIFY-D1-6 refs are authoritative.
- Broad churn (79-site token replace, icon standardization, combobox pattern, perf trio) recommended as post-wave batch.

## EXTERNAL
- Canonical version number (v2.4 vs package.json 0.1.0) needs a product-owner decision.

## DEFERRED / SKIPPED
- Nothing dropped or deferred beyond the batching note above; no credentials/IAM/git items arose from these findings.