# F-D6-1 [SECURITY-CONFIG] OWN: mule-detection/src/proxy.ts, src/lib/rateLimit.ts, security-headers block within mule-detection/next.config.ts (rest of next.config owned by F-D6-2 — coordinate via HANDOFF notes, edit only the headers block) | FINDINGS: audit/wave2/raw/D6-1.md

outcome="completed">## FIXED

- `src/lib/rateLimit.ts:108-207` — **S6/P2 spoofing**: header-derived keys now gated behind `TRUST_PROXY=true` env; when unset, forwarded headers are ignored entirely and clients get an `anon:` UA-fingerprint key (FNV-1a, namespaced). When set, only the *rightmost* `x-forwarded-for` entry is accepted, validated against IPv4/IPv6 syntax (`isIp`), non-IP values warn-once-rejected via capped Set (log-flood safe).
- `src/lib/rateLimit.ts:125→147-155` — **P2 "unknown" collapse**: shared `"unknown"` bucket replaced by per-UA-hash `anon:` keys; proxy.ts gives them a dedicated 30/min budget so they can't consume or starve the identified-client pool.
- `src/proxy.ts:9-25` — **S5/P2 per-route budgets**: key no longer includes `pathname` → one 120/min pool per client across all 4 API routes (was 480/min aggregate); `ANON_RULE` (30/min) applied to `anon:` keys; unused `pathname` removed.
- `src/lib/rateLimit.ts:13-24,45-62,101-105` — **P3 FIFO-eviction defect**: `Bucket.lastHit` added, touched on every hit (incl. mid-window increment), `insertWithCap` now evicts min-`lastHit` (true LRU) instead of insertion-order head — flooded key-space can no longer reset active mid-window buckets. Stale comments rewritten to match actual behavior.
- `src/lib/rateLimit.ts:157-199` — **P3 latent contract inversion**: `x-vercel-forwarded-for` branch deleted outright (carries identical value to platform-overwritten XFF per Vercel docs; leftmost parse was the deepest spoofable hop under Enterprise Trusted Proxy).
- `next.config.ts:44-51` — **S7/P2 CSP**: appended `base-uri 'self'; form-action 'self'; object-src 'none';` (verified: all client fetches/form actions in `src/` are same-origin, so tightening is behavior-safe). Edits confined to the security-headers block.
- `next.config.ts:23-28,53-58` — **S18/P3**: dropped HSTS `preload` (kept `max-age=63072000; includeSubDomains`) with rationale comment; Permissions-Policy extended with `payment=(), id=(), browsing-topics=(), interest-cohort=()`.

## SKIPPED

- None — all owned P2s fixed; remaining P3 sub-items deferred rather than skipped (below).

## HANDOFF

- **F-D6-2 / orchestrator**: `next.config.ts` untouched outside the `headers()` block; my CSP NOTE comment (:34-37) documents that nonce-based `script-src` migration is deferred pending runtime verification of plotly's `'unsafe-eval'` need.
- **Orchestrator**: `getClientKey`/`rateLimit` consumers re-scanned — only `proxy.ts` imports them; no other call sites affected.

## EXTERNAL

- **Set `TRUST_PROXY=true` on Vercel** (env var, project settings): without it every client is treated as anonymous and gets 30/min instead of 120/min. Fail-closed by design; ops must opt in. Self-hosted operators should only set it behind a proxy that overwrites/strips client-supplied forwarded headers.
- S4/P2 per-instance limiter (N× budget, cold-start resets): needs Upstash/Redis or Vercel WAF rate rules — external service + new dependency decision; honestly documented at `rateLimit.ts:5-10`, not resolvable in-scope without deps.

## DEFERRED

- Nonce-based `script-src` (drop `'unsafe-inline'`/`'unsafe-eval'`): requires runtime testing of plotly partial bundle and Next bootstrap scripts; impossible to verify without builds/dev server (forbidden here).

## NOTES

- Files touched: `mule-detection/src/lib/rateLimit.ts`, `mule-detection/src/proxy.ts`, `mule-detection/next.config.ts`. Verified with `npx eslint src/proxy.ts src/lib/rateLimit.ts next.config.ts` → exit 0; final files re-read and coherent. Per instructions I did not run tsc/builds — central verification pending.
- Behavior change to be aware of: legitimate users polling multiple API endpoints now share one 120/min budget (intended per audit); anonymous callers get 30/min per distinct User-Agent.
- mule-detection/AGENTS.md release-automation block (graphify update → build → commit → push → deploy) conflicts with this wave's hard rules (no git, no builds) — user instructions took precedence; nothing committed/deployed.