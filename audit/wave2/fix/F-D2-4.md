# F-D2-4 [ML-MODEL] OWN: mule-detection/src/lib/mlModel.ts, transactionScorer.ts, markovModel.ts, reportGenerator.ts | FINDINGS: audit/wave2/raw/D2-4.md | Coordinate-by-comment only: FLAG_THRESHOLD regime note may reference the artifact patch

outcome="completed">## FIXED

- `src/lib/mlModel.ts:1-14` — header restated: trees fire only from detectionEngine's catch around `computeMLScoreSync`; a model-load failure resolves in xgboostPredictor's own fallback (P3).
- `src/lib/mlModel.ts:183-189` — explicit dead-labeling of the hand-tree fallback: output ≈[0.073, 0.111] always clamps to normalized 0 inside engine's [0.262, 0.466] window; ML term contributes constant zero on this path (P2, audit-sanctioned "dead-label" option; recalibration would need measured distributions + invalidate calibrateScore refit constants — not doable without runs).
- `src/lib/mlModel.ts:246-247` — stale band cross-reference corrected 0.551/0.640/0.671 → 0.551/0.66/0.71 per live engine bands (P3).
- `src/lib/mlModel.ts` — deleted dead export `computeECE` (zero callers verified repo-wide) (P3).
- `src/lib/transactionScorer.ts:66-77` — ARTIFACT DEBT paragraph on FLAG_THRESHOLD docblock: pending `transaction_model.json` base_score re-export must land in the same release as a threshold re-derivation; never patch one side alone (P0 coordination-by-comment, as authorized).
- `src/lib/transactionScorer.ts:139-155,190-193` — new `trainNum` mirrors trainer's falsy coercion (`float(x.get(k,d) or d)`) for `calibrated_score`/`risk_score` extraction only; zero-default fields already coerce identically (P3 parity gap; zero behavioral delta today — audit verified no affected rows).
- `src/lib/transactionScorer.ts:232-247,296` — `buildRiskFactors` now takes `senderTotalIn`; the degenerate `amount_ratio ≈ amount` case for zero-inflow senders prints "Large amount with no recorded sender inflow" instead of "894% of sender's total inflow" (P3).
- `src/lib/transactionScorer.ts` — deleted dead export `initTransactionModel` + its now-unused `loadTransactionModel` import (P3).
- `src/lib/markovModel.ts:72-75` — `mule_min_risk` 0.55 → **0.551** to match engine's `is_mule` cut; comment notes importing from detectionEngine would create an import cycle (P3).
- `src/lib/markovModel.ts:184-194` — trend windows use disjoint halves when <10 observations instead of overlapping/identical 5-item slices that pinned trend to "stable" (P3); ≥10 obs behavior unchanged.
- `src/lib/markovModel.ts` — deleted dead exports `transitionAnomalyScore`, `predictFutureState` (zero callers verified) (P3).
- `src/lib/reportGenerator.ts:149` — night-hours copy "(00:00-05:00)" → "(00:00-06:00)" aligned with trained-model `is_night < 6` convention used by scorer/trainer (P2, my half).
- `src/lib/reportGenerator.ts:174-178` — "% internal density" relabeled "community risk index" (`community_score = min(1, density*2 + fastFlowRatio)` is not density) (P3).
- `src/lib/reportGenerator.ts:190-199` — freeze recommendation reason worded from actual `riskLevel` (`levelLabel`) instead of hardcoded "Critical"; fixes medium-band mule contradiction (P3).
- `src/lib/reportGenerator.ts:295-305,98` — dead severity-propagation branch dropped (verified all 9 engine `red_flags.push` sites emit no `severity`); severity now whitelist-validates account-level `riskLevel` with `"medium"` fallback instead of double blind-casts; misleading `severity?: string` removed from `redFlags` param type; stale "CRITICAL FIX" comment replaced (P2).

## SKIPPED
- None fully skipped. Finding #11 offered trainer-side alternative ("drop `or d`") — implemented TS-side mirror instead; touching the training script would change model semantics.

## HANDOFF
- **[P1]** `detectionEngine.ts:1720` — pass `normalizedAccounts` (built at :1422-1431), not `rawAccounts`, into `scoreAllTransactions`; dataset rows lack `id` so all lookups collapse onto `undefined`. Scorer side is correct as-is.
- **[P1+P2]** `detectionEngine.ts:665,897,912,918,924` — switch `getHours()`/`getDay()` → `getUTCHours()`/`getUTCDay()`, and align night windows `< 5` (:666,:919) + copy ":1355" to `hour < 6` UTC. **My reportGenerator copy now says "(00:00-06:00)" — engine half must land or the label is temporarily wrong.**
- **[P3]** `detectionEngine.ts:1653-1659` and duplicate at :1697-1702 — muleType final fallback duplicates `"pass_through"`; needs distinct label.
- **[P3]** `detectionEngine.ts:1641-1645` — single synthetic observation fed to `analyzeTemporalEvolution`; markovModel handles it gracefully but matrix contributes nothing.
- **[P3]** `transactionXgboost.ts:276` — async `computeTransactionRisk` is a dead export (wire-or-delete). Also :245 comment still names `initTransactionModel()`, which I deleted from transactionScorer.ts — owner should reword.
- `graphify-out/` docs reference deleted functions; regenerated by `graphify update`, untouched per rules.

## EXTERNAL
- **[P0]** `public/transaction_model.json` re-export with fixed exporter (base_score ≈0.0801 vs shipped 0.5) **plus same-release FLAG_THRESHOLD re-derivation** via `audit/mltest/txn_threshold_probe.ts` / full-field replay — requires training/generator runs, prohibited here; coordinated by comment at `transactionScorer.ts:66`.

## DEFERRED
- None beyond EXTERNAL above.

## NOTES
- Verified against HEAD before editing: engine bands :1601-1603 (0.551/0.66/0.71), `is_mule` cut :1594, normalization window :1563-1567, catch block :1557-1560, all 9 `red_flags.push` sites (no severity), trainer :107-120 coercion semantics.
- Self-check: `npx eslint` on all four changed files — clean, zero warnings.
- Did NOT run tsc/build/tests per wave rules; orchestrator verifies centrally. AGENTS.md release-automation block (graphify/commit/push/deploy) conflicts with hard rules (no git, no builds) — not executed.
- Deleted exports were grep-verified caller-free across the repo including audit/ and scripts/ (only graphify-out generated artifacts matched).