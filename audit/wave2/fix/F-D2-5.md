# F-D2-5 [ML-MODEL] OWN: mule-detection/scripts/train_transaction_model.py, train_meta_learner.py, auto_calibrate_thresholds.py | FINDINGS: audit/wave2/raw/D2-5.md | Python-only fixes; do NOT run training (no model regeneration)

outcome="completed">## FIXED

`mule-detection/scripts/train_transaction_model.py`
- :82-88, :153-187 — **[P1]** CV groups now = union-find connected components of the sender–receiver graph (was endpoint *pairs*, letting 23.6k senders straddle folds); stale "never share an account" pair comment replaced.
- :76-79, :119-132 — **[P3]** new `_num()` helper: defaults apply only to absent fields (`None`), real zeros no longer coerced (`calibrated_score 0→0.3`, `risk_score 0→10` bug).
- :93, :114-117, :159-161 — **[P3]** unparseable-timestamp imputations now counted and printed as WARNING (mirrors amount-skip reporting).
- :306-310 — **[P3]** `resolve_base_score` silent 0.5 fallback now prints a WARNING on the save_config-fail + attr-None path.
- :339-346 — **[P3]** exporter always emits the `missing` branch (even when it duplicates the yes branch); closes the TS `node.missing ?? null → leaf 0` divergence from XGBoost NaN semantics.
- :230-236, :254-271 — **[P3]** held-out classification now reported at both 0.5 and the deployed 0.30 operating point (`transactionScorer.ts FLAG_THRESHOLD`), with rationale comment re: `scale_pos_weight` miscalibration.
- :274-276, :441-443 — **[P3]** full-refit disclosure: importances labeled "full-data model"; SUMMARY states exported artifact is fit on ALL data and CV/held-out numbers are the out-of-sample estimates.

`mule-detection/scripts/auto_calibrate_thresholds.py`
- :123-150 — **[P2]** sanity checks before export: adjacent-band gaps must be ≥ EPS=0.01 and `flagged > non-mule max`, else prints diagnostics and `SystemExit(1)` instead of writing collapsed bands.
- :7-16 — **[P2 partial / P3]** docstring now documents the real HEAD state (detectionEngine/recompute hardcode 0.71/0.66/0.551; shipped ml_params.json holds 0.671/0.64/0.551/0.551 + stale Platt A/B — sinks disagree) with propagation instructions; stale "Run from the project root" CWD note removed.

`mule-detection/scripts/train_meta_learner.py`
- :39-49 — **[P3]** documented (not renamed) that the COMMUNITY slot carries raw label-derived `risk_score` — rename would break `combine_ml_params.py:19,61,70` which rebuilds under the same key; tautology caveat recorded at the source.

## SKIPPED
- F-meta-learner rename-slot option — breaks a consumer in a file I don't own; chose the finding's document-only alternative.

## HANDOFF
- detectionEngine.ts (:1594, :1601-1603) / recompute_ml_scores.py (:163-172) / public/ml_params.json: reconcile to ONE canonical band set + regenerate platt (stale A=-39.8078/B=12.6312, known #44). Spans TS+py+JSON outside my line; generated-constants module or CI drift check per finding.
- .gitignore:25 — add `scripts/_learned_thresholds.json` (sibling weights file is ignored; thresholds output is not). combine_ml_params.py:34 — guard/order pipeline against missing `_learned_thresholds.json` FileNotFoundError.
- transactionXgboost.ts:86 — stale citation "train_transaction_model.py:302-306" → reference `resolve_base_score` (now ~:294-322, fallback :306-310). No TS change needed for the missing-branch fix: exporter now always provides it, effective next training run.

## EXTERNAL
- Product-owner decision required: which threshold/platt set is canonical before reconciliation (above) can land.

## DEFERRED
- Artifact regeneration: all trainer changes take effect on the next training run (forbidden here). Shipped `public/transaction_model.json` (3.9 MB, base_score correctly 0.5) unchanged; expect modest size growth once missing branches are always exported (~40% of internal nodes previously relied on the null shortcut).

## NOTES
- Verification: `python -m py_compile` clean on all three; in-process functional test of `extract_features` (chain/isolated components `[0,1,2]`, zero preservation, ts counter), `resolve_base_score` warning path, and always-exported missing branch — all passed; degeneracy checks dry-runmed read-only against the real dataset and correctly refuse the collapsed 0.612/0.612/0.613/0.629 band set while passing the separation-gap condition (clean max 0.283 < flagged 0.612). Test-generated `scripts/__pycache__` removed; `_learned_weights.json`/`_learned_thresholds.json` left exactly as found.
- eslint self-check skipped — scope is Python scripts, non-src per procedure.
- No git commands, builds, dev servers, or training runs; no secret files touched; only my three owned files edited. AGENTS.md's release-automation block (build/commit/push/deploy) conflicts with this assignment's hard rules and was not executed.