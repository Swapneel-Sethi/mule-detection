# F-D2-12 [ML-MODEL] OWN: mule-detection/src/lib/detectionEngine.ts (entire file) | FINDINGS: read BOTH audit/wave2/raw/D2-1.md AND audit/wave2/raw/D2-2.md | Key P1s: scoreAllTransactions must receive normalizedAccounts not rawAccounts (~L1720); in/out_txn_count + avg amounts must prefer dataset columns over graph degree (~L1543)

outcome="completed">F-D2-12 complete. Single file touched: `mule-detection/src/lib/detectionEngine.ts` (1844 → 2000 lines). Verified with `npx eslint src/lib/detectionEngine.ts` (exit 0, zero findings) plus targeted seam re-reads; project-wide tsc left to orchestrator per instructions. Zero external importers of the module exist, so all signature/interface changes are containment-safe.

## FIXED
All in `mule-detection/src/lib/detectionEngine.ts`:
- :1853 — **[P1]** `scoreAllTransactions(validTransactions, normalizedAccounts)` — raw dataset rows key `account_id`, scorer indexes by `id`; was default-scoring all ~100k txns.
- :1676-85 — **[P1]** xgFeatures now prefer dataset `in_txn_count`/`out_txn_count`/`avg_in_amount`/`avg_out_amount` via `finite()`, graph degree fallback-only; comment at :1652-59 updated.
- :239-288 — **[P2]** rapid-movement: forward-only window (`outTime ≥ incTime && ≤ windowMs`, was `Math.abs`), per-node iteration (kills O(T·d_in·d_out) rescan), real evidence `{incoming_txn, outgoing_txn}` from new `EdgeData.txnId` (:119, wired :1547), severity-ranked cap.
- :504, :927 — **[P2]** net-flow balance defect: retained balance derived as `max(totalIn − totalOut, 0)` in pass-through detector + feature extractor (was reading generator's tin−tout column as retained funds).
- :1587-91, :941-48 — **[P2]** velocity windows anchored to max txn timestamp (`evaluationTime` param) instead of `Date.now()`; `/25`,`/6` magic numbers → proportional `(7/180)`,`(30/180)`.
- :1593-1602, :1642-45 — **[P2]** O(A×T) kill: per-account txn buckets built once; extractor takes the bucket (signature :893-902).
- :1768, :1610-14 — **[P2]** O(A×P) pattern filter + O(A×Σmembers) clusterSize scan replaced by prebuilt indexes (:1606-18).
- :1892 — **[P2]** alert-side `find()` O(H×T) → `txnById` Map.
- :1562-77 — **[P2]** cross-detector `.slice(0,100)` removed (fan-in/fan-out were evicting every other detector; summary counts now consistent); `generateMLAlerts` ranks by severity and caps its own input (:1897-1904).
- :1931-35 — **[P2]** pattern-alert ID collisions fixed with sequence index.
- :1340-41 — **[P2]** ghost explanation keys `"fan_in"/"fan_out"` → `"is_fan_in"/"is_fan_out"`; factor loop branches on `typeof boolean` → 1|0 instead of implicit coercion (:1369-74).
- :1917 — **[P3]** ML-HIGHRISK critical cut 70 → measured p99 12.6 (old cut above model ceiling ≈66.3, unreachable).
- :1497-1503, :1727, :1733-37, :1752-57 — **[P3]** shared `CALIBRATED_CUTS` table; `critical_risk`/`high_risk` flags now derive from band cuts (0.71/0.66) so flag taxonomy can't contradict risk_level.
- :1788, :1827 — **[P3]** non-fan/non-transit mules labeled `"other"`, not forced `"pass_through"` (both sites).
- :1440, :1454, :1468 — **[P3]** copy fixes: spike = "transaction count", concentration = "outgoing transfers", community = "cluster risk index" (not "% internal density").
- :1963-80 — **[P3]** centrality normalized by max observed degree (÷n·0.5 pinned all nodes ≈0 at n≈105k, deadening centrality signals).
- :122, :145 — **[P3]** `UpdatedAccount`/`Alert` exported for `DetectionResult` consumers.
- :695, :707, :1049-62 — **[P3]** shared inclusive `BURST_WINDOW_MINUTES=5` (extractor was strict `<5min`).
- :585-99 — **[P3]** community fast-flow: each out-edge fast at most once vs nearest preceding internal in-edge, forward-time only (ratio can no longer exceed 1).
- :849-886 — **[P3]** PageRank mass-conserving iteration (dangling redistribution, teleport folded once as `(1−d)·pref`); degenerate all-equal case → uniform prior.
- :350, :377-82, :392-436, :469 — **[P3]** circular-transfer cycle key preserves orientation (canonical rotation); layering aggregates parallel edges (mean, not `edges[0]`) and drops unused `start` param; truncation caps now severity/hub-ranked (rapid/circular/layering/structuring).
- :493-96 — **[P3]** pass-through accepts canonical 1-in/1-out relay; unknown accounts skipped, not credited balance 0.
- :53 — **[P3]** `Transaction.risk_score` optional (dataset ships `riskScore`; field unconsumed here).
- :71-73 — **[P3]** dead `PatternType` members `"community_cluster"`/`"bridge_account"` dropped (grep-verified unreferenced).
- :686, :966-997 — **[P3]** UTC parsing everywhere (`getUTCHours`/`getUTCDay`) matching tz-aware training pipeline.
- :930-32, :957-60, :1115 — **[P3]** sentinel `999` ratios → 0 with `is_one_sided` marker; ">3" gates, factors, and narratives no longer fire on one-sided accounts.
- :907-12, :1150 — **[P3]** `age_missing` marker emitted for the imputed-age rows.

## SKIPPED
- **D2-1 #18 communityId sparse keys** — not reproducible at HEAD: counter increments only inside the accept branch, so accepted keys are contiguous (`community_0..N`). No defect present.
- **D2-1 #19 betweenness n≤2 zeros** — zero *is* the correct betweenness for ≤2-node graphs; fabricating a constant would be wrong. PageRank half of the finding was fixed.
- **D2-1 #11 dropped-count reporting** — severity ranking implemented, but plumbing "suppressed count" fields into summary is invasive summary-schema churn; deferred with caps now ranked so suppression is least-harmful.
- **D2-2 #23 hub/authority proxy fallback removal** — auditor verified 105,501/105,501 dataset coverage, so skew is fallback-path-only; "fail loudly when columns missing" changes library error contract beyond minimal diff.
- **D2-2 #24 catch-path `mlScore()` provenance** — surfacing `{score, source}` reshapes `UpdatedAccount`/reportGenerator inputs; API decision for orchestrator.

## HANDOFF
- **mlModel.ts:231-232** — calibrateScore docstring still claims bands 0.551/0.640/0.671 untouched while engine uses ITER-2 cuts (now centralized as `CALIBRATED_CUTS` in detectionEngine.ts:1497); docstring owner should sync. (Not my file.)
- **reportGenerator.ts:128** — repeats the "% of outgoing funds" claim; engine copy now says "outgoing transfers"; align this file too.
- **transactionScorer.ts docstring L49-52** — its "detectionEngine passes raw dataset accounts" wording is now inaccurate (engine passes normalized accounts, which carry full trained-scale fields — the regime it describes still holds).
- **Orchestrator**: behavior deltas to expect in central verification — flags `high_risk` threshold moved 0.50→0.66, `critical_risk` 0.70→0.71; pass-through recall expands (1-in/1-out allowed); centrality/pagerank-derived scores become non-zero.

## EXTERNAL
- None newly surfaced. Pre-existing items (GCP IAM rotation, Netlify token revocation, D4 #24 dataset regeneration fixing the net-flow `balance` column at the generator) remain with the orchestrator; consumer-side mitigations for D4 #24 are in place here until regeneration.

## DEFERRED
- **D2-2 #22 ML_SCORE_MIN/MAX literals (:1717-19 area)** — proposed fix ships train-time min/max in model_weights.json; requires training-pipeline artifacts/knowledge (no training runs permitted). Literals documented as-is.
- **D2-2 #20 Markov single-observation trajectory** — honest fix needs multi-day observation history that doesn't exist in a static-snapshot run (fabricating series would be leakage); hiding the panel requires a reportGenerator contract change (not my file).
- **D2-1 #7 circular total_amount over parallel edges** — proper fix needs timestamp-consistent per-hop chain selection within a bounded window; orientation-key half was fixed, amount half needs design.

## NOTES
- AGENTS.md's release-automation block (build/commit/push/deploy) conflicts with this task's hard rules (no builds/git/deploy); task rules took precedence — release steps intentionally NOT run.
- Engine remains unwired from any route (header notice still accurate); all changes affect offline semantics only.
- Perf note for future wiring: removing the pattern cap means `allPatterns` can hold ~10⁵ entries on the full dataset; per-account/alert consumers are indexed/bounded, but memory now scales with true pattern count (arrays were already fully materialized pre-cap by the detectors themselves).