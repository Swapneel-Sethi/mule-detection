# F-D4-5 [SYNTHETIC-DATA] OWN: mule-detection/src/scripts/muleSeed.ts, seedData.ts | FINDINGS: audit/wave2/raw/D4-5.md | If verdict is dead-code, prefer clear deprecation comment over deletion (tracked files; no git ops)

outcome="completed">All fixes verified. Final report:

## FIXED
- `src/scripts/muleSeed.ts:305-345` — **[P2]** account `features` now emits the 4 model-consumed keys: `night_txn_ratio` (00:00–05:59 share from `TRANSACTIONS`, same window as detectionEngine), `txns_per_day` (txn count / `age_days`), `is_pass_through` (mule_type/flag), `bridge_score` (= stored `betweenness_centrality`, mirroring detectionEngine where both come from one value). Tree 3 and Trees 5/8 branches plus 3 `interactionScore` rules no longer silently zero out.
- `src/scripts/muleSeed.ts:155` + `:364` — **[P2]** txn `type` switched from legacy `"transfer"` to `TXN_RAILS = ["upi","imps","neft","rtgs"]` (deterministic rotation).
- `src/scripts/muleSeed.ts:1-17` — **[P3/dead-code]** clear `@deprecated` header: no importer/npm-script/API-route consumes `generateMuleSeed()`; kept as scenario-pattern reference per remediation policy.
- `src/scripts/muleSeed.ts:300` — **[P3]** risk bands moved to canonical edges ≥75/≥50/≥25.
- `src/scripts/muleSeed.ts:290-297,381-383` — **[P3]** added `addHours()` naive-timestamp helper; alert stamps are now offset-less `YYYY-MM-DDTHH:mm:ss` (+1h by same-format arithmetic, no `.toISOString()`), so lexical ordering is TZ-independent.
- `src/scripts/seedData.ts:1-11` — **[P3]** header rewritten: dropped false "API route handles database writes" claim, added matching `@deprecated` note pointing at `generate_dataset_json.py` / `generateMuleSeed()`.
- `src/scripts/seedData.ts:92` — **[P2]** `TXN_TYPES` → payment rails; removed now-unused `SEVERITIES`/`STATUSES`.
- `src/scripts/seedData.ts:104-108,163-166` — **[P2]** txn `flagged` is now `from.is_mule || to.is_mule` (computeStats disjoint-tier semantics); `risk_score` field retained.
- `src/scripts/seedData.ts:231-259` — **[P2]** alerts reference transactions whose endpoints actually include the narratively-named accounts (muleSeed-style), so each ref set has real multi-leg evidence instead of index-aligned `transactions[i]`.
- `src/scripts/seedData.ts:105-109,140-152` — **[P3]** risk bands canonicalized; `is_fan_in`/`is_fan_out` booleans derive from assigned flags and `reasons` mirror flags (`Pattern: …`, confirmed_mule filtered) exactly like muleSeed.
- `src/scripts/seedData.ts:261-303` — **[P3]** per-alert literal `severity`/`status` (concentration alert now critical/new, no born-dismissed alerts); anchor timestamp via shared `addHours` naive format.

## SKIPPED
- None — all 4 P2s and all actionable P3s in owned files were fixed.

## HANDOFF
- `mule-detection/SIH_AUDIT_REPORT.md:243-255` — **[P3]** still documents the deleted `/api/seed` route with wrong cardinalities ("30 accounts + 20 txns + 8 alerts" vs actual bundle 39/36/7). Not in my ownership line; doc owner should delete/update that section.
- `graphify-out/` artifacts referencing these two files are now stale vs edited source; `graphify update .` + central tsc verification belong to the orchestrator (generators forbidden for me).

## EXTERNAL
- None arising from this slice (audit itself confirmed: no env vars, secrets, or batch code present in either file).

## DEFERRED
- Product decision (existing register #30): wire up or delete both seed scripts — I applied the mandated deprecation-comment alternative.
- Residual imprecision accepted by design: seedData alert descriptions still quote specific ₹ amounts/time-windows that won't equal their (now endpoint-correct) ref txns' amounts; audit offered "fix refs **or** strip amounts" and I took the refs option to stay copy-aligned with `mockData.ts:132-139`.

## NOTES
- Verification: `npx eslint src/scripts/muleSeed.ts src/scripts/seedData.ts` → exit 0. Behavioral check by importing the real modules under Node v26 type-stripping: **ALL_CHECKS_PASSED** — flagged⟺mule-involved on all 80 txns; every alert has ≥1 endpoint-matched ref anchored after its latest evidence; all timestamps offset-less; canonical bands hold; reasons≡flags for all 20 accounts; all 4 previously-missing features present with correct semantics (CYCLE_C1 night ratio 1, MULE_FANIN 0, MULE_PASS `is_pass_through=true`).
- Removing two `rng()` draws/account shifts seed=42 output values vs the pre-fix generator (still fully deterministic; nothing downstream depends on them — zero importers).
- AGENTS.md conflict: its release-automation block (build, commit, push, deploy) contradicts this task's explicit NO-git/NO-builds mandate; direct task instructions took precedence — none of those steps were run.