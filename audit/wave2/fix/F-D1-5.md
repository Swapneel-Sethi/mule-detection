# F-D1-5 [UI/UX] OWN: mule-detection/src/components/Sidebar.tsx, SidebarOverlay.tsx, ui/FilterBar.tsx, ui/PageHeader.tsx, ui/RiskBadge.tsx | FINDINGS: audit/wave2/raw/D1-5.md | ALSO check D1-6.md for your files

outcome="completed">## FIXED
- `src/components/Sidebar.tsx:26-31` — [P2, D1-5] mq `change` handler now calls `setIsOpen(false)` when crossing to ≥lg, so rotating portrait→landscape disarms the scroll-lock + focus trap instead of leaving them armed into persistent-sidebar mode (also clears stale `aria-expanded`).
- `src/components/SidebarOverlay.tsx:49-52` — [P2, D1-5] focus trap filters focusables by `offsetParent !== null`, so the `lg:hidden` close button (display:none below lg) can no longer be a dead wrap target whose `.focus()` no-ops; container fallback preserved.
- `src/components/Sidebar.tsx:46-48,73-74` — [P3, D1-5] drawer gets `role="dialog" aria-modal="true"` (label "Main navigation") when `isOpen && isMobileViewport`; plain `role="navigation"` otherwise — restores the modal semantics the rewrite dropped.
- `src/components/Sidebar.tsx:70-72` — [P3, D1-5 pre-hydration tabbable #28] closed branch adds `invisible` + `lg:visible`, a CSS-only guarantee that the off-screen drawer is untabbable before hydration. Self-caught correction: switched `transition-transform` → `transition-[transform,visibility]` because visibility must participate in the transition for discrete interpolation (stays visible until slide-out ends) — without it the close animation would have been an abrupt vanish.
- `src/components/SidebarOverlay.tsx:24-28,70` — [P3, D1-5] scrollbar-gutter compensation: body `padding-right` set to `innerWidth - clientWidth` while locked, restored to prior inline value on unlock — kills the horizontal layout shift on classic-scrollbar platforms.
- `src/components/Sidebar.tsx:97-101` — [P3, D1-5] active-route check uses segment boundary `pathname.startsWith(item.href + "/")`; future `/alerts-archive` can't highlight two items.
- `src/components/ui/PageHeader.tsx:18` — [P3, D1-5 #11 remainder] subtitle `text-[11px]` → token `text-caption` (verified `--text-caption: 11px` at globals.css:48 and in use by ui/ primitives).

## SKIPPED
- RiskBadge orphan adoption/deletion (D1-5 + D1-6 P3) — adoption requires editing AlertsContent/DashboardContent (not my line); deletion would preempt the preferred fix and destroy the documented unknown-severity contract mid-wave. Routed to HANDOFF.
- 200px sidebar-width magic number (`Sidebar.tsx:70` ↔ `layout.tsx:38`, #29) — proper fix needs a shared token defined in `globals.css` `@theme` plus an edit to `src/app/layout.tsx`; both outside my ownership line.
- Icon standardization on lucide-react (D1-6 M13, names my hamburger/close SVGs) — app-wide convention change = drive-by refactor for this wave; lucide is currently used only in error/not-found pages.

## HANDOFF
- **AlertsContent/DashboardContent owner**: adopt `RiskBadge` in Alerts' Severity cell (`AlertsContent.tsx:90-98`) and Dashboard's category chip; unknown severity there currently falls through to neutral styling contradicting RiskBadge's "unknown ≠ safe" rule. If adopted, `globals.css` `.risk-badge-*` blocks stop being dead CSS.
- **globals.css + layout.tsx owner**: define `--sidebar-w: 200px` in `@theme`; consume as `w-(--sidebar-w)` in `Sidebar.tsx:70` and replace `lg:ml-[200px]` in `layout.tsx:38`.

## EXTERNAL
- Version chaos: footer `v2.4` (`Sidebar.tsx:123`) vs `package.json` `"0.1.0"` — canonical version is a product-owner decision (explicitly external per wave rules); left untouched.

## DEFERRED
- None beyond the above.

## NOTES
- Changed files: `Sidebar.tsx`, `SidebarOverlay.tsx`, `ui/PageHeader.tsx`. `FilterBar.tsx` untouched — both audits verdict it CLEAN (sentinel contract verified sound); `RiskBadge.tsx` untouched (see HANDOFF).
- Verified with `npx eslint` on all three changed files from `mule-detection/`: clean, twice (after each round). No tsc run per instructions. All edits re-read back from disk and confirmed.
- Conflict noted: `mule-detection/AGENTS.md` release-automation block demands build/commit/push/Vercel deploy after any tracked-file change; wave hard rules forbid git/builds/deploy, so orchestrator rules took precedence — nothing committed, built, or deployed.
- No Next.js-specific APIs touched (plain client components), so the Next 16 docs consultation wasn't applicable.