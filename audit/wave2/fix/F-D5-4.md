# F-D5-4 [PAGES-API] OWN: mule-detection/src/lib/useLocalData.ts, mockData.ts, utils.ts | FINDINGS: audit/wave2/raw/D5-4.md | THE headline fix: silent mock fallback masking outages must go — honest error+retry; fix timeout-leaves-loading, loadMore replace-vs-append, currentPageRef premature advance, formatCurrencyINR crore/lakh math

outcome="completed">## FIXED (F-D5-4)

**useLocalData.ts**
- `src/lib/useLocalData.ts:133-139` — **P2**: mixed-scale `riskScore` corruption no longer passes through; rows ≤1 rescaled ×100 onto the 0–100 axis at the hook boundary (7.5% of flagged txns previously rendered as empty/clamped risk bars and sorted on the wrong scale).
- `src/lib/useLocalData.ts:123-130` — **P3**: dropped `json.stats || computeStats(...)` silent-metric-swap fallback; now hard-throws `"Malformed response from data API"` (verified route always includes `stats` in success payloads, route.ts:123-127). `computeStats` import removed.
- `src/lib/useLocalData.ts:12,7-11` — **P3**: stale `ApiTransaction` docstring rewritten (demo-type-union justification no longer exists); interface aliased to mockData's rails-aligned `Transaction` (safe: only consumer compares `.type`, never assigns).
- `src/lib/useLocalData.ts:39,93,101-104,184-187,221` — **P3**: exposed `isLoadingMore` (set on append start, cleared by still-current request in `finally`) so consumers can disable/feedback the loadMore control.

**mockData.ts**
- `src/lib/mockData.ts` (whole file) — **P3 ×3**: reduced to a types-only module (5 interfaces + new exported `StatsShape`). Kills the all-dead runtime exports + module-scope `Math.random()` hydration hazard, the patternTypes/alert-count inconsistency, and the alert-predates-txn timestamp defect (generators gone → all three moot). `StatsShape` replaces the former `typeof stats` contract for useLocalData.

**utils.ts**
- `src/lib/utils.ts:106` — **P3**: `formatCurrencyINR(NaN/±Infinity)` now returns `"—"` instead of falsifying corrupt amounts as `"₹0"` (`value === 0` split into its own branch).
- `src/lib/utils.ts:34-41` — **P3**: `getUserLocale()` pinned to `"en-US"`, navigator read removed — eliminates the SSR-vs-hydration compact-format mismatch ("150K" vs "1.5L"); docstring updated.
- `src/lib/utils.ts:17-32` + all call sites — **P3**: added `memoIntl()` cache keyed by class+locale+serialized options; applied to every `NumberFormat`/`DateTimeFormat`/`RelativeTimeFormat` construction; fixed-arg INR formatter hoisted to one module-level instance.

## SKIPPED
- **utils.ts dead exports (P3)** — audit lists 11 unused symbols (`cn`, `truncate`, etc.) as delete-or-wire. Deletion is a broad surface change in a shared util mid-swarm; a concurrent wave could be wiring these per its own findings. Needs a coordinated sweep once all waves land (→ HANDOFF).
- **Headline items** (mock fallback, timeout-leaves-loading, loadMore replace-vs-append, currentPageRef advance, INR crore/lakh math) — confirmed already fixed at HEAD by prior waves; audit digest agrees (A18/A19/A31 verified fixed). No action taken, none needed.

## HANDOFF
- **AccountsContent owner**: hook now exports `loadMore`/`isLoadingMore`; wire them to lift the 1000-row flagged-universe cap with progress feedback (audit P3, useLocalData limit question below is yours/product's).
- **route.ts owner**: stale comment at route.ts:224 — "(useLocalData derives it from mockData's stats)" → now derives from mockData's `StatsShape` interface.
- **Orchestrator**: coordinate the utils.ts dead-export deletion after all waves merge; central tsc will catch any collision.

## EXTERNAL
- None arising from D5-4.

## DEFERRED
- Raising request `limit` 1000→5000 (route permits): ~5× initial payload tradeoff — product/perf call, not a pure fix.

## NOTES
- Verified pre-edit: mockData had zero runtime importers (sole importer was `import type`); `txn.type` never assigned anywhere; route always supplies `stats`.
- `npx eslint src/lib/useLocalData.ts src/lib/mockData.ts src/lib/utils.ts` — clean exit 0. No tsc run per instructions; orchestrator verifies centrally.
- Behavior changes worth knowing downstream: missing/malformed stats now surfaces as error banner + retry instead of silently recomputed numbers; ~7% of txn rows render at their true 0–100 score (sort order in TransactionsContent shifts accordingly); non-finite amounts display "—".
- mule-detection/AGENTS.md release-automation block (graphify update / build / commit / push / deploy) conflicts with this task's hard rules (no git, no builds) — task instructions followed; release steps belong to the orchestrator.