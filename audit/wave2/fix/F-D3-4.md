# F-D3-4 [GRAPHS-VIZ] OWN: mule-detection/src/components/SankeyChart.tsx, plotly/PlotlySankey.tsx, types/plotly-partial.d.ts, scripts/visualize_sankey.py | FINDINGS: audit/wave2/raw/D3-4.md

outcome="completed">All changes verified. Remediation complete — final report:

## FIXED

- `src/components/SankeyChart.tsx:74-77` — link-aggregation keys switched from `|` to `\u0000` separator (with explanatory comment); parse sites at :141, :146 updated to match, so an account id containing `|` can no longer swap key fields.
- `src/components/SankeyChart.tsx:116-121` — pattern-node label no longer embeds the total (`P:${p}` → label is just the pattern name); the amount renders once via `customdata`/hovertemplate, killing the duplicated hover text.
- `src/components/SankeyChart.tsx:230-240` — legend chips get `disabled={!onPatternSelect}` + `cursor-not-allowed`, so a consumer without `onPatternSelect` sees inert chips instead of clickable-looking no-ops.
- `src/components/plotly/PlotlySankey.tsx:3-10` — docstring's unverifiable "~3.5 MB / 4.65 MB / ~4 MB saved" figures replaced with a qualitative claim ("bundles every trace type — several MB… fraction of that").
- `scripts/visualize_sankey.py:31-38` — CIRCULAR demo now closes the cycle (`loop[(k+1) % len(loop)]`, A→B→C→A) matching production semantics; `LOOP_EXIT` removed, so the `"LOOP" in n` red-color rule no longer paints the cash-out beneficiary as a mule.
- `scripts/visualize_sankey.py:74-76` — title changed to "Synthetic Demo — Mule Money Flow by Fraud Pattern" so fabricated data no longer carries the production chart title verbatim.

Verification: `npx eslint src/components/SankeyChart.tsx src/components/plotly/PlotlySankey.tsx` from `mule-detection/` → clean; `ast.parse` on `visualize_sankey.py` → OK (no execution, no artifacts written). Did NOT run project-wide tsc per instructions.

## SKIPPED

None — all findings targeting my files were fixable cheaply.

## HANDOFF

For owner of `src/app/api/analytics/route.ts` (+ `AnalyticsContent.tsx`) — all three P2s land there, none in my files:
- **[P2] route.ts:323-326** — Sankey-only heuristic rebrands pattern-less flows from critical/high sources as PASSTHROUGH; contradicts comments at :8-9/:317 claiming shared attribution with `txnByPattern` (:124-140). Delete the promotion branch or replicate it in `txnByPattern`.
- **[P2] route.ts:292, 295-300** — circular-path dedup key `[from,via,mid].sort().join("->")` destroys orientation; amount sums lifetime volumes of all three edges instead of the cycling transactions'. Use directional key `${from}->${via}->${mid}` and track only cycling txns' amounts.
- **[P2] route.ts:328-330, 341** — sankey endpoints keyed on `account_id.slice(-6)` merge distinct accounts sharing a 6-char suffix (and note the `|` join at :330 has the same separator fragility I fixed in SankeyChart). Use full ids in keys/labels; SankeyChart already truncates at render (`…${id}`), so no downstream change needed beyond labels.
- **[P3] route.ts:347 + AnalyticsContent.tsx:200-214** — top-20 pre-sampling presented as authoritative in chip/node totals ("N flows · ₹total", "Other · N accts"). Label as top-20 samples or ship unsampled aggregates. Not fixable from SankeyChart: the sampling happens upstream before flows reach it.

## EXTERNAL

None arising from this wave.

## DEFERRED

AGENTS.md release-automation steps (graphify update, build verify, commit/push/deploy) — barred here by wave hard rules (no git/builds); orchestrator should apply its own release flow post-verification.

## NOTES

- All four P2/P2-consumer findings were re-located and confirmed verbatim at current HEAD line numbers before handoff; no drift.
- `types/plotly-partial.d.ts`: CLEAN per audit, untouched, confirmed nothing needed.
- Left alone deliberately (not flagged): `linkAgg` key uses `>` separator in SankeyChart (:135) — merging there only sums amounts, no field swap possible; minimal-diff rule kept it out of scope.
- No new deps, no framework APIs touched, so node_modules/next docs weren't needed.