# F-D4-2 [SYNTHETIC-DATA] OWN: mule-detection/scripts/generate-synthetic-data.ts | FINDINGS: audit/wave2/raw/D4-2.md | Drift-vs-python findings must keep BOTH generators semantically identical — python side is another agent's file; if you must change shared constants, mirror exact values and note under HANDOFF

outcome="completed">## FIXED

All in `mule-detection/scripts/generate-synthetic-data.ts` (only file touched). Every finding's evidence was re-verified against the live artifacts before editing (105,501 accounts; `lastActivity` max `2026-08-22`; 7,001 missing-`account_age_days` = exactly the mule population; fan-in "0 distinct" 38/40; unflagged risk max 38.3; alert histogram has zero circular/dormant_activation).

- **[P1] Unseeded generator** — mulberry32 PRNG seeded 42 (`scripts/generate-synthetic-data.ts:92-99`) threaded through all 14 randomness sites (`rand()`); window anchor now deterministic: `--as-of=<ISO>` arg, defaulting to and clamped by the dataset horizon (max account `lastActivity`) at :221-228. Zero `Math.random`/`Date.now` remain. Verified: identical 100k-draw sequences across reseeds.
- **[P2] Fan-in/out copy mixing scopes** — descriptions quote lifetime dataset fields `acc.unique_senders` / `acc.unique_receivers`; run txns are evidence-only (:356, :375, with explanatory comment).
- **[P3] Flag rule >70** — `FLAG_RISK_CUTOFF = 40` (:106, used :257) preserves the shipped invariant "unflagged ⟹ risk ≤ ~38–40".
- **[P3] Dormancy gates exclude all mules** — `account_age_days` made optional; new `effectiveAgeDays()` falls back to `firstSeen`-derived age (:137-141), used in both gates and both descriptions.
- **[P3] Dead circular branch + contradicting dormant copy** — seeded 30 deliberate reciprocal a→b/b→a pairs among the first-30 high-risk candidates the detector actually scans (:237, :281-292), via a shared `buildTxn()` closure so totals stay exactly 8,000; `dormant_activation` copy rewritten to state dormancy facts instead of "suddenly active" (:437). Chose seeding over "mine lifetime graph" because the dataset carries no edge list.
- **[P3] Window postdates lastActivity** — `endDateMs = min(--as-of, horizon)` (:226-227); transactions can no longer postdate the snapshot.
- **[P3] Alert timestamps decoupled** — `recentTxns()` selects most-recent-first (tie-broken by id) for all six families; `alertTimestamp()` anchors at newest referenced txn +1h (mirrors D6 #8), falling back to the account's own `lastActivity`.
- **[P3] Locale-dependent currency** — pinned `Intl.NumberFormat("en-IN", INR)` (:109); verified output `₹2,04,678.93`.
- **[P3] cwd-relative paths** — resolved from `import.meta.url` → `<repo>/public` (:22); applied to all three paths.
- **[P3] Pretty-printed output** — compact `JSON.stringify(x)` matching the Python siblings' convention.
- **[P3] Robustness bundle** — `validateAccounts()` asserts non-empty array + 17 dereferenced fields + parsable dates (:120-135); empty-input guard in `weightedRandomChoice` (:148); atomic temp-file+rename writes (:177-181).

## SKIPPED

None outright. The npm-script half of finding #9 is split off to HANDOFF (outside my file ownership).

## HANDOFF

- **package.json** (not my line): add `"generate:synthetic"` script — but note there is **no `tsx`/`ts-node` devDependency**, so pick a runner first (e.g. Node ≥22.6 `node --experimental-strip-types scripts/generate-synthetic-data.ts`, or add tsx as a devDep — adding deps needs owner sign-off).
- **generate_synthetic_data.py** (other agent's line): drift finding confirmed live — py still emits legacy vocab `routine|normal|high_value|suspicious_transfer` (:59-65), 50k txns vs TS 8k, 30d window vs TS 180d, different flag rules/id formats. I changed **no shared constants** on the TS side (rails vocabulary, id format, field shapes untouched), so nothing needed mirroring. Recommend the finding's own resolution: one shared spec or archiving the py generator.
- **Orchestrator**: (a) central tsc will include this file via `**/*.ts` — standalone strict check passed, but verify project-wide as planned; (b) shipped artifacts are now intentionally stale relative to the fixed generator (old alerts still say "0 distinct accounts") — a paired regeneration run should be scheduled when builds are permitted; (c) AGENTS.md release steps (graphify update/build/commit/push/deploy) deliberately not done per task hard rules.

## EXTERNAL

None arising from this slice.

## DEFERRED

- Per-account chronology precision: global horizon clamp prevents any txn postdating the snapshot max, but same-window staleness vs an individual account's older `lastActivity` remains possible; full fix would require refreshing `lastActivity` during regeneration (a 92 MB write) — left for a regeneration-policy decision.

## NOTES

Verification performed without running the generator (prohibited): `npx eslint` on the file → exit 0; single-file strict `tsc --noEmit` (ES2017/esnext/bundler, @types/node) → exit 0; PRNG reproducibility/distribution and en-IN Intl checked in isolated node snippets. Diff is confined to the owned script; comments/docstrings updated to match new behavior (header documents determinism contract).