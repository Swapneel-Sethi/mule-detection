# F-D2-3 [ML-MODEL] OWN: mule-detection/src/lib/xgboostPredictor.ts, transactionXgboost.ts | FINDINGS: audit/wave2/raw/D2-3.md | NOTE: a parallel agent is patching public/transaction_model.json base_score to 0.0800667 — make sure YOUR consumer-side math stays consistent with a probability-space base_score converted via logit; do not hardcode the number in TS

outcome="completed">## FIXED
- `src/lib/xgboostPredictor.ts:282` + `src/lib/transactionXgboost.ts:249` — [P3] cache-TTL dead path: sync wrappers now fire `void loadModel()/loadTransactionModel()` whenever cache is absent **or** past `MODEL_CACHE_TTL_MS`, so the TTL can actually expire and refresh (dedup unchanged; current call uses cached model during refresh). Comments updated to match.
- `xgboostPredictor.ts:358` + `transactionXgboost.ts:303` — [P3] `countSplitFeatures` no longer recurses into `missing`; verified via python sampling that both shipped artifacts serialize `missing` as value-identical sibling copies (tree0: 2/2 and 183/183), so split counts were double-counted. Comment added.
- `xgboostPredictor.ts:141` + `transactionXgboost.ts:135` — [P3] NaN traversal now follows `node.missing ?? node.left ?? node.right ?? null` (real XGBoost default-direction semantics) instead of terminating mid-tree when the exporter omits `missing`.
- `xgboostPredictor.ts:83-88` + `transactionXgboost.ts:76-81` — [P3] explicit `Number.isNaN(x) ? 0` branch in `sigmoid`; behavior identical (NaN previously fell to 0 by accident of `NaN > 0`), now documented.
- `xgboostPredictor.ts:318` — [P3] hoisted duplicated 7-element default-importances array into module const `DEFAULT_IMPORTANCES` (mirrors `transactionXgboost.ts:231`), replaced both inline copies.
- `xgboostPredictor.ts:210-214` — [P3] stale `buildFeatureVector` docstring ("kyc_status defaults to 1…") rewritten as pure ordering doc; notes `detectionEngine.ts` supplies every value.
- P0 consumer-side consistency (verified, **no TS change needed**): both files read `model.base_score` from the artifact at runtime and add `baseScoreLogOdds()` = logit(p) with (0,1) guards — nothing hardcoded. Confirmed `transaction_model.json` still ships 0.5 pre-patch; once the parallel agent's 0.0800667 lands, serving automatically contributes logit(0.0800667) ≈ −2.441, matching `predict_proba`. Account artifact stays 0.0 → contribution 0 on both TS and python paths.

## SKIPPED
- Delete orphaned exports `getFeatureImportances` / `getTransactionFeatureImportances` / `computeMLScore` / `computeTransactionRisk` [P3] — zero callers re-verified by grep, but removal is API-surface churn on a master reported-only item (#46); kept them and fixed their counting bug so they're correct if wired later.
- Server-side relative `fetch("/…json")` [P3, known #43] — proper fix (fs.readFile of `public/` or env base URL) has deploy-target-dependent path resolution (standalone/Vercel) and can't be verified without builds, which are forbidden this wave; left as reported-only.
- `val <= thresh` vs XGBoost strict `<` [P3, known #42] — must change identically in `scripts/recompute_ml_scores.py` (not my file) to avoid divergence; benign boundary-row-only.

## HANDOFF
- `transactionScorer.ts:67` — **P0 follow-up**: `FLAG_THRESHOLD = 0.3` was calibrated against the biased (base_score=0.5) distribution; must be re-derived once base_score=0.0800667 lands (per D2-3 P0).
- `transactionScorer.ts:128-131` — [P3] `safeNum` returns 0 for null/false/"" (train/serve mismatch vs trainer's `or`-coercion); fix: return fallback for null/"" keeping numeric 0.
- `transactionScorer.ts:334` — [P3] dead export `initTransactionModel` (#46) — remove or call at an API-route startup boundary.
- `scripts/recompute_ml_scores.py:85` — **[P2]**: adds `base_score` raw into log-odds instead of `logit(b)`; mirror the TS `baseScoreLogOdds` guard/conversion or the refit script diverges from serving on any future real-prior re-export (would poison `ML_SCORE_MIN/MAX`).
- `scripts/recompute_ml_scores.py:78-84` — [P3]: NOTE claiming leaf×lr "intentionally DIVERGES" from TS is false post-D2-#6 (TS multiplies lr too); rewrite to state parity.

## EXTERNAL
None in my line.

## DEFERRED
- Silent-fallback observability [P3, known MEDIUM] — `{score, source}` return-shape change ripples into `detectionEngine.ts`/`transactionScorer.ts` (not my files); additive `lastScoreSource` flag would itself be an orphaned export. Needs an orchestrator/cross-owner decision.
- Wire feature-importance exports into a UI surface or delete them — product decision.

## NOTES
- Verification: python sampling of both artifacts (sizes 64 KB / 3.98 MB, under limit) confirmed base_score values, learning_rate 0.1, 200 trees × 16 features, exact `feature_names` order match with both `buildFeatureVector`s, and `missing`=sibling duplication; `npx eslint src/lib/xgboostPredictor.ts src/lib/transactionXgboost.ts` → clean. Did not run tsc/builds/git per hard rules.
- `mule-detection/AGENTS.md` mandates graphify-update/build/commit/push/deploy after any tracked-file change; those steps conflict with this wave's explicit NO-git/NO-build rules and were therefore **not** executed — orchestrator owns release sync.