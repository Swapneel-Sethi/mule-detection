# F-D4-1 [SYNTHETIC-DATA] OWN: mule-detection/scripts/generate_synthetic_data.py | FINDINGS: audit/wave2/raw/D4-1.md | ALSO check D3-5.md for generator-side mismatches | Do NOT execute generators (no artifact regeneration)

outcome="completed">Remediation Wave B complete. Single file touched: `mule-detection/scripts/generate_synthetic_data.py` (342 → 406 lines). All 9 owned findings (2×P2, 7×P3) fixed; generators never executed; no artifacts touched.

## FIXED (all in `mule-detection/scripts/generate_synthetic_data.py`)

- `:10-11` — [D4-1 P2] Pinned UTC anchor `NOW` (default `2026-08-22T00:00:00+00:00`, env-overridable via `SYNTHETIC_NOW`); all 7 former `datetime.now()` sites (`:111`, `:166`, `:186`, `:228`, `:254`, `:309`, `:379`) now derive from it → seeded runs are byte-reproducible.
- `:15-23` — [D4-1 P2] New `iso_utc()` helper: tz-aware/naive→UTC, emits `...Z` per the pipeline convention in `convert_csv_transactions.normalize_timestamp()`. All timestamps/stats now Z-suffixed.
- `:25-38, :95, :304` — [D4-1 P3] Dead txn vocab (`suspicious_transfer|high_value|normal|routine`) replaced by weighted rails `upi|imps|neft|rtgs` via `random_rail()`, mirroring `generate-synthetic-data.ts:123-124` weights and the `mockData.ts:23` union.
- `:159-257` — [D4-1 P3] Alert types remapped onto the `mockData.ts:30` union with honest semantics: `mule_activity`→`behavioral_change`; `high_value_transaction`→`rapid_movement`; `suspicious_pattern` (whose type-grouping died with the vocab change) replaced by real topology-derived `fan_in`/`fan_out` alerts (distinct counterparties over flagged txns, top-3/direction, severity from endpoint risk tier); dead type-grouping block deleted.
- `:239-249` — [D4-1 P3] `high_risk_accounts` (severity `low`, ad-hoc ≥75 cutoff) redesigned as `dormant_activation`: critical-tier ∧ age>730 ∧ totalTransactions≤20, `severity:'critical'`. Measured on live data: 1,055 accounts match.
- `:320-331` — [D4-1 P3] Transfer records now carry `'flagged'` (same rule as txns), fixing schema asymmetry / undefined→false reads.
- `:97-101` — [D4-1 P3] Removed 5% pure-random flag term from both families; flags are behavior-derived only.
- `:257-261` — [D4-1 P3] Amount-distribution parity: transfers mirror txn multipliers (`U(5000,50000)*U(0.5,2.5)` mule; `U(100,5000)*U(0.2,3.0)` normal).
- `:262-265` — [D4-1 P3] Dead default `max_transfers_per_account=10`→`5`; docstring documents ~max/2 expected volume per account.

## SKIPPED

- None among owned findings — all P2/P3 were cheap and safe.

## HANDOFF (not my files)

- **D3-5 P2 REGRESSION**: `src/app/api/data-local/route.ts:174-178` highRisk inverted vs Dashboard comment/fallback (`DashboardContent.tsx:58-63`) — serves 0 forever on current artifact.
- **D3-5 P1**: `mule-galaxy/route.ts` module-level cache, frozen `generatedAt`, no stampede guard; `MuleGalaxy.tsx:87-96` radiusCache staleness.
- **D3-5 P3**: galaxy route dead branches/`totalVolume`/53 degree-0 nodes; `lastDay`→`firstDay` rename; UTC-vs-IST day bucketing.
- **D3-5 generator-side**: `public/transactions_synthetic.json` mixed riskScore scale (7,469 rows ≤1.0) — artifact of `convert_csv_transactions.py`/`recompute_transaction_scores.py`, not mine; fix at its two consumer sites or regenerate. `public/accounts_dataset.json` negative HITS epsilon rows — clamp in `generate_dataset_json.py` export.

## EXTERNAL

- None new. (Prior waves' GCP IAM/Netlify/git-history items remain with orchestrator.)

## DEFERRED

- Shipped `public/synthetic_dataset.json` (280.5 MB) still bakes in pre-fix riskScore units, naive stamps, and dead vocab — regeneration forbidden this wave; regen is a product-owner decision (D4#22 reported-only).
- Summary print `:398` still uses an ad-hoc ≥75 cutoff for a stdout-only stat — left as display-only, no schema impact.

## NOTES

- Verification (no generator execution, no artifacts written): `python -m py_compile` OK; AST-isolated dry-run of extracted helpers/alerts on stubs — `iso_utc` emits `Z` (incl. naive input), rail distribution measured upi .602/imps .245/neft .101/rtgs .051 over 10k draws, `generate_alerts` emitted exactly `[behavioral_change/high, rapid_movement/medium, fan_in/critical, fan_out/medium, dormant_activation/critical]` with flagged-only fan counting. Grep confirms zero remaining `datetime.now()` and zero retired vocab strings.
- eslint self-check skipped — scope is non-src (`scripts/*.py`) per instructions; no project-wide tsc run per instructions.
- The repo's `AGENTS.md` release-automation block (graphify update/build/commit/push/deploy) was intentionally **not** performed — it directly conflicts with this wave's hard rules (no git, no builds). Orchestrator should decide whether to run that flow after central verification.