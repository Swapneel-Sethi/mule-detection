# F-D2-6 [ML-MODEL] OWN: mule-detection/scripts/export_xgboost.py, convert_model.py, combine_ml_params.py, recompute_ml_scores.py, recompute_transaction_scores.py | FINDINGS: audit/wave2/raw/D2-6.md (script-side findings only) | The shipped JSON artifact is being patched by another agent — you own the PRODUCER side so future exports are correct

outcome="completed">## FIXED

- `scripts/export_xgboost.py:142-152` — P3 unrunnable: missing-CSV guard now raises a clear `SystemExit` pointing at `dataset_output/ml_features_100k.csv` (no in-repo producer of that table exists — verified).
- `scripts/export_xgboost.py:127-133` + docstring `:8-13` — P3: importance output renamed `feature_importances.json` → `model_feature_importances.json` (nothing consumed the old name; grep-verified across `src/`, file absent from `public/`), comment updated, docstring lists both outputs.
- `scripts/convert_model.py:7-14` — P3 unrunnable: documented legacy status + clear `SystemExit` when `mule_xgboost_model.pkl` is absent, redirecting to `export_xgboost.py`.
- `scripts/convert_model.py:98-101` — P3: `build_tree` raises `ValueError('tree dump references missing node id N')` on dangling yes/no/missing refs instead of silently fabricating `{'leaf': 0.0}`.
- `scripts/recompute_ml_scores.py:9-20` — P3: paths anchored via `ROOT = Path(__file__).resolve().parent.parent`; docstring CWD note replaced with in-place-rewrite note; `OUTPUT_PATH = ACCOUNTS_PATH`.
- `scripts/recompute_ml_scores.py:80-88` — P3: stale eta NOTE rewritten — this script and `xgboostPredictor.ts` BOTH multiply dump leaves by `learning_rate` (verified at `xgboostPredictor.ts:162-181`); `transactionXgboost.ts` noted as sole non-multiplier.
- `scripts/recompute_ml_scores.py:150-157` — P3: tree-validity check now requires a real leaf or ≥1 truthy child; degenerate `{feature, threshold, left:null, right:null}` stubs no longer count as valid (mirrors `isValidTree` in the TS consumer).
- `scripts/recompute_ml_scores.py:168-182` — P3: band/`is_mule` comments reworded to state they gate `ml_normalized` while `detectionEngine.ts:1594,1601-1603` applies identical constants to `calibratedScore` — shared cutoffs by historical accident, not parity.
- `scripts/recompute_transaction_scores.py:9,86-89` — P3: night window `< 5` → `< 6` (docstring `[0,6)`), with parity refs to `train_transaction_model.py:136` / `transactionScorer.ts:158`.

Verification: `ast.parse` OK on all four edited scripts. ESLint skipped per SELF-CHECK rule (scope is non-src Python). No scripts executed.

## SKIPPED

- export_xgboost "switch training source to accounts_dataset.json" alternative — rejected as silently changing the training distribution; kept the loud CSV guard instead.
- combine_ml_params.py code change for graceful missing-input handling — script matches its contract; `FileNotFoundError` on absent `_learned_thresholds.json` is already clear; adding messaging = non-mandatory churn.

## HANDOFF

- [P0] `public/transaction_model.json` `base_score: 0.5` vs trained intercept ≈0.0800667 (+2.44 logit bias live) → artifact owner: re-export via fixed `train_transaction_model.py`, then re-derive `FLAG_THRESHOLD` (`transactionScorer.ts:28-66`). Producer already fixed upstream; artifact mtime predates it.
- [P2] `public/ml_params.json` contradicts runtime constants AND is irreproducible at HEAD (missing `scripts/_learned_thresholds.json`; `metrics_note` key never emitted by `combine_ml_params.py`; `_meta` fields disagree with `_learned_weights.json`) → retire or regenerate + wire one canonical constants module (`detectionEngine.ts`/`mlModel.ts` owners). Producer script itself is correct against its contract.
- [P3] `public/transaction_feature_importances.json` consumed by nothing (runtime recomputes importances) → consume or delete.
- [P3] `public/model_weights.json` header (`"1.0.0"`, lr 0.1) matches no committed producer → re-export through pipeline so header matches.

## EXTERNAL

None in my findings (no IAM/token/git-history items).

## DEFERRED

- Re-export `transaction_model.json` / `model_weights.json` through committed producers — requires training runs, forbidden this wave.
- Regenerate `_learned_thresholds.json` + rerun `combine_ml_params.py`, or retire `ml_params.json` — requires calibration run + product-owner decision (retire vs regenerate).

## NOTES

- AGENTS.md in mule-detection mandates release automation (`graphify update .`, commit, push, Vercel deploy) after tracked-file changes — conflicts with this wave's HARD RULES (no git/builds); per instruction precedence I did neither; orchestrator should apply its usual central flow.
- Pre-existing asymmetry left untouched: `convert_model.py` always emits a `missing` child node, `export_xgboost.py` maps `missing==yes` → `null`; consumer handles both shapes.
- The TodoList tool kept being overwritten by concurrent agents' lists, so I abandoned it mid-run rather than clobber their tracking; all planned fixes above are complete and verified.