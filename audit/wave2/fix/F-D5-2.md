# F-D5-2 [PAGES-API] OWN: mule-detection/src/components/AccountsContent.tsx, AlertsContent.tsx | FINDINGS: audit/wave2/raw/D5-2.md | ALSO check D1-6.md for your files | Truncation must become visible to user; reset pagination on filter change; no dead search boxes

outcome="completed">## FIXED

**`src/components/AccountsContent.tsx`**
- AccountsContent.tsx:20-77 — [P2] truncation now visible & navigable: adopted the hook's dormant `loadMore` + `pagination`; footer shows `N loaded` vs server `total`, "Load more" reveals 500 more and pulls the next server page when the fetched window is exhausted (browsing reaches full 8,578, not just first 1,000)
- AccountsContent.tsx:60-63 — search/risk-filter changes reset the reveal window (`visibleCount → 500`) via handlers, single render pass
- AccountsContent.tsx:50,55 — honest counts while searching: match count labelled partial (`· searched N` suffix); false-empty mitigated via `emptyDescription` ("Searched X of Y — load more to scan further"); removed misleading "(refine search to see more)" suffix
- AccountsContent.tsx:11-15 — dropped dead "High Risk" option (`category=high` = non-mule critical/high ⇒ 0 rows on shipped data), with explanatory comment
- AccountsContent.tsx:53-54 — whitespace-only query no longer flushes the table: `search.trim()` guard + trimmed `q`
- AccountsContent.tsx:117-126 — `name` now rendered in Account cell so name-search hits are visible (bank stays search-only)
- AccountsContent.tsx:145 — risk shown as `toFixed(0)%` matching Dashboard presentation
- AccountsContent.tsx:160-161 — flag chips keyed `` `${flag}-${i}` `` (duplicate-flag collision fix)
- AccountsContent.tsx:186 — DataTable `caption="Flagged accounts"` (was generic aria-label region)

**`src/components/AlertsContent.tsx`**
- AlertsContent.tsx:52-70 — initial-load gate (`loading && alerts.length === 0`) stops whole-page spinner swap on refetch; loading/error/main states all get PageHeader + `p-8 max-w-[1200px] mx-auto` wrapper (fixes D1-6 REGRESSION)
- AlertsContent.tsx:118-127 — added `Refreshing…` (role=status) and `Refresh failed — showing previously loaded alerts` (role=alert) banners, mirroring AccountsContent parity
- AlertsContent.tsx:17-18,44 — sort precomputes `Date.parse(ts)||0` epoch deltas (no per-comparison Date objects, no NaN poisoning); search filters trimmed value `"ACC-123 "` now matches
- AlertsContent.tsx:88-99,110-119 — severity/status colors on semantic tokens: `low→text-risk-low`, unknown→dim `text-ash` (was bright `text-bone`), `investigating→text-risk-medium` (was off-token `text-amber-400`)
- AlertsContent.tsx:143 — Time column formats with `timeZone: "Asia/Kolkata"` (matches Analytics IST bucketing)
- AlertsContent.tsx:170-172,180-185 — removed dead filter options: severity Low/Info, status Dismissed (zero shipped rows each), with comments
- AlertsContent.tsx:47,69 — columns typed `Column<MappedAlert>[]`; all seven inline `(typeof alerts)[0]` signatures removed

## SKIPPED
- Shared `formatRisk()` helper (D5-2 P3) — helper would live in utils/Dashboard territory; applied local `%` suffix instead to reconcile presentation.
- RiskBadge adoption in Alerts (D1-6 P3) — component has different API/visuals; adopt-or-delete is an owner-level design call, not a minimal diff.

## HANDOFF
- **useLocalData.ts** (P2): Alerts mounts full-data hook (~2.7 MB: 1,000 accounts + txns it never renders). Fix: add fetch flags so AlertsContent can request `transactions=false&accounts=false`. I could not do this in-scope — hook is shared by Dashboard/Accounts/Transactions and outside my ownership line.
- **route.ts:125-126** (P3): silent `alerts.slice(0,500)` cap — return true total alongside cap; AlertsContent footer will surface it once provided.
- **route.ts:90-98 vs AccountsContent predicate** (P3): API `q` matches id/name/bank, client matches id/name/city/bank — align before wiring `q` up. Full server-side search would also close the residual search-covers-loaded-rows-only gap in Accounts.
- **RiskBadge zero importers / dead `.risk-badge-*` CSS** (D1-6 P3) — owner decision.

## EXTERNAL
- None new. (Dataset gaps themselves — zero non-mule high-risk rows, no low/info severities, no dismissals — are data-generation matters.)

## DEFERRED
- Repointing `category=high` semantics (vs. dropping the option, which I did): breaks category disjointness — product-owner decision required.

## NOTES
- Verification: `npx eslint src/components/AccountsContent.tsx src/components/AlertsContent.tsx` from `mule-detection/` — clean exit, zero warnings. Project-wide tsc intentionally not run (orchestrator verifies centrally). No builds/dev servers run, so runtime behavior of the load-more flow is unverified beyond lint/types-by-inspection.
- `mule-detection/AGENTS.md` mandates graphify update/build/commit/push/Vercel deploy after tracked-file changes; my orders forbid git/builds/deploys, so none were performed — release steps remain for the orchestrator.
- D1-6 items touching other files (StatCard locale split, Transactions empty `<th>`, galaxy toggles, etc.) left untouched — not mine.
- Line numbers above refer to the post-edit files.