# F-D5-6 [PAGES-API] OWN: mule-detection/src/app/api/analytics/route.ts, api/alerts/count/route.ts | FINDINGS: audit/wave2/raw/D5-6.md | P1: unify mule/highRisk KPI partition to match data-local (all is_mule vs non-mule-and-tier); delete false coincidence comments; fix txnByPattern double-count, IST/UTC mixing, dayKey year-merge, sankey suffix collisions; alerts/count honest failure codes

outcome="completed">## FIXED

All in `mule-detection/src/app/api/analytics/route.ts` unless noted. Ground truth verified against the datasets with an independent python port before/after editing.

- `route.ts:84-93` — [P1] KPI partition unified to the mandated definition: `muleCount` = **all** `is_mule` in flagged universe; `highRiskCount` = non-mule ∧ severity tier (no longer `total − muleCount`). False "numbers coincide…" comment deleted. Verified: 8,578 / 0, matching data-local/Dashboard rendering.
- `route.ts:28-45, 138-150` — [P2] txnByPattern double-count fixed via shared `patternForTxn()` single-bucket helper (PATTERN_PRIORITY + risk-tier fallback), used by bars and Sankey alike. Measured: FANOUT 8.90 → **4.64 Cr**; bucket sum now equals Flagged Turnover exactly (**25.8349 Cr**); 1,580 multi-flag txns single-bucketed.
- `route.ts:177-211, 270-276` — [P3] dayKey year-merge: internal keys are full `YYYY-MM-DD` (chronological sort, no cross-year merge); stripped to MM-DD only at payload emission for both `volumeByDay` and `patternTimeData`.
- `route.ts:191-204` — [P3/A24] volumeByDay now iterates flagged txns only: 7,944 rows / 25.83 Cr, same population as sibling turnover/pattern charts.
- `route.ts:316-365` — [P3] Sankey suffix collisions: aggregation keyed by **full ids**; display labels = shortest distinguishing suffix, grown only inside colliding groups. Verified: 8,531 endpoints → 8,531 unique labels; only the 8 known colliding accounts got longer labels.
- `route.ts:367-381` — [P2] per-pattern `slice(0, 20)` cap removed; all aggregated flows shipped heaviest-first (~500 KB) so the filter chip's "{n} flows · ₹total" reconciles with the bar chart instead of a censored top-20.
- `route.ts:299-305` — [P3] circular-loop comment corrected: amount is **corridor volume** (lifetime sums of all three directed edges incl. clean txns), not cycled amount; sorted-triple dedupe/orientation caveat documented. Behavior unchanged.
- `src/app/api/alerts/count/route.ts:13-58` — [P2] honest failure codes: on refresh failure serves last-good `{count, stale:true}`, else **503** `{error:"Alert dataset unavailable"}`; failures negatively cached 60 s so outages don't trigger a re-parse per request; stale flag computed post-refresh so a successful refresh never reports itself stale.
- `route.ts:241-248` — [P3] alertPatternMap comment fixed: shipped vocab has zero `circular` alerts (verified: rapid_movement×50, fan_in×40, fan_out×40, behavioral_change×25); CIRCULAR line is fed by behavioral_change as a documented stand-in.
- `route.ts:383-405` — [P3] dead payload fields removed (`muleAccounts`, `cleanAccounts`, `accountsTotal`, `inOutData`) including the dead 8,578-element sort feeding them.

## SKIPPED

- **Patterns card raw-flag vocab** (P3, `patternData`): mapping through `canonicalFlag` would silently drop unmapped flags like `new_account` (7,042 — the largest row); honest fix is retitling the card "Account Flags", which lives in AnalyticsContent.tsx → HANDOFF instead.
- **behavioral_change→CIRCULAR re-mapping**: chart-semantics/product decision; kept mapping, corrected the comment to describe it truthfully.

## HANDOFF

- **data-local/route.ts computeStats** (P2 UNFIXED, ~183-191 + 174-179): turnover still lacks `t.flagged === true` — ₹26.20 Cr vs analytics' ₹25.83 Cr despite its "agree" comment. Its mule/highRisk formulas are also still old (`is_mule∧tier` / `total−mules`); data-local must adopt the same new partition as analytics — today both render 8,578/0 only because every flagged account happens to be a mule.
- **normalizers.ts:264-273** (P2): fallback `computeStats` fabricates incompatible stats under identical keys — return nulls or txn-based values.
- **AnalyticsContent.tsx**: prune interface fields I removed (`muleAccounts`, `cleanAccounts`, line 56-57; `inOutData`, line 71) — nothing reads them and `json` is `any`, so tsc stays green until pruned. Optionally render/color an `OTHER` txnByPattern bucket (now present in payload; `PATTERN_DOT_COLORS` already has OTHER). P3 bar colors (FANIN frost vs #7fd1f0, FANOUT charcoal near-invisible). Card title "Transaction Volume Over Time" could read "Flagged …" after my filter change. "+N more" client cap now optional since flows are uncapped.
- **DashboardContent.tsx:123** (P3): identical "Alerts" labels show different metrics (107 activeAlerts vs 155 totalAlerts) — relabel one.
- **SankeyChart.tsx**: `accountsTotal` prop is unused — component owner may drop it.

## EXTERNAL

None arising from this line.

## DEFERRED

- Deleting the orphaned `/api/alerts/count` route (A27/P3): public URL may have external consumers; product-owner call.
- Re-mapping behavioral_change→CIRCULAR (above).

## NOTES

- IST/UTC mixing item from my brief was already fixed at HEAD (all buckets `Asia/Kolkata`) — verified, no change needed.
- Verification: `npx eslint` on both files clean (run twice, incl. after final edit). No build/dev-server/tsc per hard rules — central tsc owned by orchestrator. Numeric claims reproduced exactly against datasets: turnover 25.83 vs 26.20 Cr, FANOUT 8.90→4.64 Cr, 1,580 multi-flag txns, 4 suffix-collision groups/8 accounts, volumeByDay 7,944/25.83 Cr.
- `mule-detection/AGENTS.md` release-automation block (graphify/build/commit/push/deploy) conflicts with this wave's NO-git/NO-build rules; user instructions take precedence — release steps left to the orchestrator.