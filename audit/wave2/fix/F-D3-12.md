# F-D3-12 [GRAPHS-VIZ] OWN: mule-detection/src/components/MuleGalaxy.tsx (entire file) | FINDINGS: read BOTH audit/wave2/raw/D3-1.md AND audit/wave2/raw/D3-2.md

outcome="completed">Lint clean, `inert` typing confirmed against installed `@types/react` 19.2.18. Remediation complete.

## FIXED

All in `mule-detection/src/components/MuleGalaxy.tsx` (only file touched):

- **[D3-1 P2]** FPS latch after tab-hide — `visibilitychange` listener resets `fpsRef` on resume; unregistered in teardown (`MuleGalaxy.tsx:656-662`, `698`)
- **[D3-1 P3]** Filters/timeline/trace set during async-import window ignored — build applies `nodeVisibility`/`linkVisibility`/`nodeColor` from mirror refs at end of setup (`705-721`)
- **[D3-1 P3]** NaN date crash in `scrubCutoffDay` — strict `YYYY-MM-DD` regex in `dayRange`, finite guard before `toISOString()` (`240`, `250`)
- **[D3-1 P3]** `qualityRef` not reset in cleanup — reset beside `engineReadyRef` so rebuilds don't inherit latched degraded mode (`782-783`)
- **[D3-1 P3]** Degenerate zero-link snapshot renders void — `<EmptyState>` branch + "High-value flow" legend row hidden when cutoff infinite (`990-999`, `1192`)
- **[D3-1/D3-2 P3]** Silent 40-item flow cap — pre-slice totals + "Showing top N of M" disclosure rows (`336-346`, `1260-1262`, `1272-1274`)
- **[D3-1 P3]** ResizeObserver camera-yank — no-op-resize skip + pointer-interaction guard; refits only on real geometry change (`615-654`)
- **[D3-1 P3]** Blind `response.json()` cast — payload shape validated before `setSnapshot` (`208-211`)
- **[D3-1/D3-2 P3 UNFIXED]** FPS state re-rendering tree at 500 ms forever — state removed; written straight to a DOM ref stat cell (`668-672`, `1288-1291`)
- **[D3-1/D3-2 P3 UNFIXED]** All `zoomToFit` paths ignoring the 400 px panel — systemic fix: canvas geometry = mount minus panel via new `applyCanvasSize()` + panel-toggle refit effect; initial binary-search fit, RO refit, RE-CENTER, bank-cluster and trace fits all inherit it (`385-399`, `420-425`, `787-792`)
- **[D3-1/D3-2 P3 UNFIXED]** Hover O(E) scan per mousemove — adjacency-map lookup, O(degree) (`490-493`, `728-742`)
- **[D3-1/D3-2 P3 UNFIXED]** Legend swatch ≠ rendered ribbon — flagged-corridor swatch now exact `rgba(239,69,98,.26)`; "High-value flow" restyled as thickness cue, not a colour (`1176-1197`)
- **[D3-1/D3-2 P3 UNFIXED]** Trace BFS walking hidden corridors — filtered by `visibleIds` + scrub cutoff; trace toggle disabled when chain is null (`305-327`, `1226`)
- **[D3-1/D3-2 P3 UNFIXED]** Teardown rollback window — partial teardown assigned immediately after listener attach, overwritten with full version (`630-638`, `695-703`)
- **[D3-2 P2]** Trace `zoomToFit` framing whole galaxy — third-arg `nodeFilter` frames just the traced chain (`372-374`; signature verified at `3d-force-graph.d.ts:70`)
- **[D3-2 P2]** Off-screen panel keyboard-focusable — `inert={!panelOpen}` + focus restored to stage via shared `closePanel()` (both Close buttons, background click, Escape) (`378-383`, `1205-1217`, `936-941`)
- **[D3-2 P2]** Zoom/orbit assuming origin target, bypassing clamps — `zoomCamera` scales about `controls.target` clamped into `[minDistance, maxDistance]`; arrows orbit the offset about the live target (`855-877`, `894-935`)
- **[D3-2 P3]** Enter preferring bank-substring over listed suggestions — suggestions win, `focusSearchResult()` only on empty list (`1036-1043`)
- **[D3-2 P3]** Combobox semantics — `role="combobox"`/`listbox`/`option`, `aria-expanded`/`aria-controls`/`aria-activedescendant`/`aria-selected`, ArrowUp/Down cycling (`1011-1072`)
- **[D3-2 P3]** `aria-pressed` on view-mode trio + pattern chips (`1008`, `1134`)
- **[D3-2 P3]** Range slider `aria-valuetext="through <date>"` (`1109`)
- **[D3-2 P3]** Pattern cap saturation — "+N more patterns" (`1138-1140`)
- **[D3-2 P3 UNFIXED]** Bank-cluster zoom racing fixed 120 ms timer — fit now owned by the visibility effect post-refresh via `clusterFitPendingRef` + guarded rAF (`367-374`, `838-843`)

## SKIPPED

- **[D3-2 P3]** Plain wheel trapped for zoom over the canvas — Ctrl+wheel gating would break the documented primary navigation ("wheel zooms" in help copy and stage aria-label), and patching OrbitControls' internal wheel handling is invasive with no way to visually verify here. Bare-wheel dolly is conventional for 3D orbit views; `touch-none` blocking touch scroll on the canvas itself is intended (orbit gesture).

## HANDOFF

- **`lastDay` → `firstDay` rename** (flagged by both digests): the field stores earliest activity day but the identifier invites `>= lastDay` misuse. The rename must land simultaneously in producer `src/app/api/graph/mule-galaxy/route.ts` (interface + payload builder) and this consumer — route.ts is outside my ownership, so left to the orchestrator/route owner. Current runtime behaviour is correct; consumer comments already document earliest-day semantics.

## EXTERNAL

- Nothing arising from these two digests (no IAM/token/git-history items in scope).

## DEFERRED

- `mule-detection/AGENTS.md` release automation (graphify update → build → commit → push → Vercel deploy): conflicts with this wave's hard rules (no git, no builds/servers); release is the orchestrator's central step.
- Visual confirmation of camera behaviour (narrowed-canvas framing, target-relative orbits): needs a browser session; all library calls match verified type signatures.

## NOTES

- One file modified: `src/components/MuleGalaxy.tsx` (now 1295 lines). Verified with `npx eslint src/components/MuleGalaxy.tsx` from `mule-detection/` — zero errors/warnings. Project-wide tsc intentionally not run per instructions; boolean `inert` typing confirmed against installed `@types/react@19.2.18`.
- Panel-awareness is implemented by narrowing the WebGL canvas itself (`width − min(400, mountWidth)`), not by padding hacks — this fixes every fit path at once and keeps content centred in the visible region. `bloomRef` was repurposed as the live bloom-resize handle, resolving the write-only-ref finding by use.
- Behaviour-preserving details: stats array is 5 entries with a separate FPS `<Card>` (grid stays 6 columns; panel summary `slice(0, 5)` output unchanged); search Enter tradeoff (bank-cluster isolation now only when no account suggestions are listed) follows the audit's explicit prescription — bank chips still provide cluster isolation.