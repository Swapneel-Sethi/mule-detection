# F-D4-4 [SYNTHETIC-DATA] OWN: mule-detection/scripts/convert_csv_transactions.py, generate_dataset_json.py | FINDINGS: audit/wave2/raw/D4-4.md | CSV header ground truth: read ONLY header line of 'transactions_1m (1) (1).csv' at C:/MISCELLANEOUS PROJECTS/SIH_2026/1/ (quote path, head -1)

outcome="completed">## FIXED
- `scripts/convert_csv_transactions.py:10-12` — docstring: timestamp convention now documents naive-IST → `+05:30` tagging (was "treated as UTC").
- `scripts/convert_csv_transactions.py:106-131` — [P2] `normalize_timestamp` tags naive source times `+05:30` so encoded instants are correct for JS Date and the Asia/Kolkata-bucketed charts (`route.ts` buckets in IST). Also fixes [P3] beyond ask: full shape regex now covers the Z/offset branch (`garbageZ` rejected) plus `strptime` calendar validation (month-13/hour-99/Feb-30 hard-error instead of silent Invalid Date).
- `scripts/convert_csv_transactions.py:158-161` — scale assert: riskScore must stay 0–100 before write.
- `scripts/generate_dataset_json.py:16-19` — header comment no longer claims the shipped artifact satisfies the firstSeen/age invariant.
- `scripts/generate_dataset_json.py:27-42,106,188-189` — [P3] lenient `parse_bool` (`TRUE`/`1`/`yes` accepted); unrecognized truthy-looking values counted and warned in summary (was silent False).
- `scripts/generate_dataset_json.py:68-75` — [P3] `risk_level` bands aligned to calibration constants 0.71/0.66/0.551 on the calibrated score, matching `recompute_ml_scores.py`/`detectionEngine.ts` ITER-2 (was 0.75/0.50/0.25 on raw score).
- `scripts/generate_dataset_json.py:77-95` — [P2] `compute_flags` emits canonical vocab: `fan_out` (was `fan_out_suspect`), new `fan_in` via `unique_senders > 30`; dropped `network_hub`/`high_out_degree` (zero consumers; unmapped by `canonicalFlag`, so FANIN/FANOUT analytics collapsed).
- `scripts/generate_dataset_json.py:153-158` — [P1] `ml_score`/`calibrated_score` emit the heuristic score on the 0–1 scale (was `risk_pct` 0–100 → downstream ×100 clamp would flag all txns @100).
- `scripts/generate_dataset_json.py:167-177` — [P3] compact separators `(',', ':')` + dual-scale asserts (0–1 ml/calibrated; 0–100 risk/behavioral) before dump.

## SKIPPED
- **[P3] graph_score "10× inflation"** — REJECTED with evidence: `round(hub*100000*10)/10 ≡ hub×10⁵` to one decimal; numerically reproduces the artifact sample (hub 1.075e-5 → 1.1). The audit misread the `* 10 … / 10` rounding idiom as a multiplier. No code change; added a clarifying comment at `generate_dataset_json.py:150-151`.

## HANDOFF
- `scripts/recompute_transaction_scores.py` (not owned): its `(cal+cal)/2 → ×100` chain is what my P1 fix realigns to; no edit required — but it still clamps to 100 if ever run against a stale 0–100-scale artifact. Orchestrator may want a scale guard there.
- `src/app/api/analytics/route.ts` `canonicalFlag`: untouched per ownership; script now emits only names the mapper already resolves.

## EXTERNAL
- None new.

## DEFERRED
- **[P2]** `ml_features_100k.csv` absent at both candidate paths → generator unrunnable until the input is committed or script archived; product-owner decision + data we cannot fabricate (generators prohibited).
- Regeneration of `public/accounts_dataset.json` / `transactions_synthetic.json` (+ paired alerts) after these fixes — prohibited here (no generators/builds); mixed riskScore scales and firstSeen drift persist in shipped artifacts until then.
- **[P3]** RTGS ₹2L-minimum violations (49,496 rows): resample/relabel belongs at generation time; product decision.

## NOTES
- CSV ground truth read via `head -1`: header matches the converter docstring exactly; file lives at `mule-detection/` root, not the outer root the briefing named.
- Verification: `python -m py_compile` both scripts OK; AST-extracted changed functions behavior-tested — 11 timestamp cases (naive/fractional/Z/offset accepted with correct tagging; `garbageZ`, month-13, Feb-30, hour-99, empty rejected), bool parsing incl. `TRUE`/`1`/`None`, band boundaries 0.71/0.66/0.551, flag vocab ⊆ canonical set, clean row → no flags. All pass. Full-script runs impossible by design (input CSV for accounts absent; regeneration prohibited).
- eslint skipped — scope is non-src (`.py` scripts only).
- `mule-detection/AGENTS.md` release-automation block (build/commit/push/deploy) not executed — conflicts with wave hard rules; left for the orchestrator post-verification.