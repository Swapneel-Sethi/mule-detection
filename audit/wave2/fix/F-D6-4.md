# F-D6-4 [SECURITY-CONFIG] OWN: mule-detection/backend/main.py, backend/requirements.txt | FINDINGS: audit/wave2/raw/D6-4.md | CORS tighten, input validation, pin requirements loosely (&gt;=,&lt;), async-blocking fixes; note service is unwired from Next app

outcome="completed">All fixes verified. Final report:

## FIXED

- `mule-detection/backend/main.py:25` — added `random.seed(42)` at module top; mock data now identical across restarts (verified by regenerating twice)
- `mule-detection/backend/main.py:33-45` — CORS tightened: `MULEGUARD_CORS_ORIGINS` env parsing strips a literal `"*"` (fail-closed → empty list denies all cross-origin), and `allow_credentials=False` since the API has no cookies/auth (verified wildcard-stripping via subprocess)
- `mule-detection/backend/main.py:196-199,246-256` — `_parse_ts` now normalizes aware stamps to UTC via `astimezone(timezone.utc)` before dropping tzinfo (`+05:30` no longer misorders vs `Z`; docstring updated to match)
- `mule-detection/backend/main.py:396-440` — replaced exhaust-`nx.simple_cycles` with depth-limited iterative DFS rooted at each node's smallest member; bounded work on dense graphs, output semantics preserved
- `mule-detection/backend/main.py:109-165` — added typed response models (`RootResponse`, `CentralityResponse`, `CommunitiesResponse`, `RiskScoresResponse`, `PatternsResponse`, `GraphNode`, `GraphEdge`, `GraphResponse`, `StatsResponse`) and applied them to all 7 previously unvalidated routes; `GraphEdge.from_` uses `Field(alias="from")` so the JSON wire shape is unchanged (FastAPI serializes by alias)
- `mule-detection/backend/main.py:546-586,630-653,683-701` — analysis siblings de-duplicated into one cached pipeline: `_get_analysis()` memoizes `full_analysis()`; `/centrality`, `/communities`, `/risk-scores`, `/patterns`, and `/stats` all derive from it (`_compute_patterns` now regroups cached patterns by family) — no more per-request O(VE) recomputation
- `mule-detection/backend/main.py:193-196,205-208` — removed false-parity comments in mock generator: risk_level/status bands explicitly documented as simplified mocks, not dataset-calibrated cut-offs; flagged-threshold note added at the `"flagged": risk > 70` line
- `mule-detection/backend/main.py:489-492` — comment distinguishing static account `risk_score` (/api/graph) from computed graph-heuristic score (/api/analysis nodes) that share a field name
- `backend/requirements.txt:1-7` — exact pins loosened to ranges per assignment: `fastapi>=0.115.4,<1.0` (floor = first release resolving starlette ≥0.40 with the CVE-2024-47874 fix), `uvicorn/networkx/pydantic` as `>=current,<next-major`

## SKIPPED

- **[P3] auth + rate limiting on all 12 routes** — finding's own fix is conditional ("if the service is kept at all"); keep-vs-delete is pending owner decision, `slowapi` would be a new dependency (banned), and an auth scheme needs product decisions. Adding a hand-rolled limiter to an orphaned service would be speculative complexity.

## HANDOFF

- **[P2] service is orphaned** — confirmed: nothing in `src/`, `package.json`, docs, or `.env.example` references this FastAPI service; the Next app serves precomputed JSON from `public/`. Owner must decide delete-the-directory vs wire-it-up-deliberately. All my hardening above assumes it's kept.
- Orchestrator: `AGENTS.md` in mule-detection mandates release sync (graphify update/build/commit/push/deploy) after tracked-file changes — left entirely to central release process per wave rules.

## EXTERNAL

(none in scope)

## DEFERRED

- Full threshold recalibration of mock data to dataset-calibrated cut-offs (67.1/64.0/55.1 / ≈55.1 under_review / ≈40.8 flagged) — I chose the finding's sanctioned alternative (delete false-parity comments); real alignment changes mock distributions and belongs with the keep/delete decision.
- Hash-locked freeze (`pip freeze`/`uv lock`) — superseded by the assignment's explicit loose-range pinning instruction.

## NOTES

- Verification (all passed): `python -m py_compile main.py`; module import + functional suite — new DFS returns exactly the same cycle set as `nx.simple_cycles` filtered to length ≤5 (194/194 cycles, zero missing/extra after rotation normalization; `simple_cycles` doesn't root at min node, mine does — tuples differ only by rotation); `_parse_ts("…T09:00:00+05:30") < _parse_ts("…T05:00:00Z")` now correct; seeded regeneration byte-equal; TestClient exercised all 13 route probes (12×200, 404 for unknown account), graph edges still serialize `"from"` key, `stats.active_alerts == patterns.total`, `/risk-scores` matches `/analysis` node scores.
- Async-blocking item: nothing to fix — zero `async def` handlers exist (grep count 0); the file's header comment correctly documents the sync-on-threadpool design, which is the prior fix holding.
- Input validation: already covered by prior fixes (`Query(ge=0, le=10000)` on limit); no negative-amount surface exists — amounts come only from the internal mock generator.
- RCE surface re-confirmed clean: no pickle/joblib/sklearn/UploadFile/POST anywhere in `backend/`.
- eslint self-check skipped — scope is non-src Python, per procedure.
- No git commands run; only `backend/main.py` and `backend/requirements.txt` touched.