# F-D1-3 [UI/UX] OWN: mule-detection/src/components/ui/Button.tsx, Card.tsx, Skeleton.tsx, StatCard.tsx | FINDINGS: audit/wave2/raw/D1-3.md | ALSO check D1-6.md for your files

outcome="completed">## FIXED
- `mule-detection/src/components/ui/StatCard.tsx:8-12,25` — numeric `value` now formats via module-level `new Intl.NumberFormat("en-IN", {notation:"compact", compactDisplay:"short", maximumFractionDigits:1})` instead of `formatNumber`→`navigator.language` (D1-3 P3 / D1-6 locale-split). Kills the SSR/client hydration-divergence mechanism and row inconsistency; output shape identical for current values.
- `mule-detection/src/components/ui/StatCard.tsx:14-23,75-77` — `forwardRef<HTMLDivElement>` + `extends HTMLAttributes<HTMLDivElement>` + `displayName`, matching Button's contract (D1-3 P3 #8). All 8 call sites unaffected.
- `mule-detection/src/components/ui/Card.tsx:5-16` — `Card` now `forwardRef<HTMLDivElement, HTMLAttributes<HTMLDivElement>>` + `displayName`; still default export (D1-3 P3 #8).
- `mule-detection/src/components/ui/Card.tsx:21-27` — `CardTitle` accepts `as?: HeadingLevel`, default `"h2"`, fixing h1→h3 heading-order skips at all ~20 existing call sites with zero consumer edits (D1-3 P3 #7).
- `mule-detection/src/components/ui/Button.tsx:27-31,56` — spinner size derives from `size` (`sm: h-3 w-3`, `md/lg: h-4 w-4`) via a `spinnerSizeClasses` record mirroring the file's idiom (D1-3 P3 #31 / D1-6).

## SKIPPED
- D1-3 P3 "StatCard dead `trend`/`icon`/`variant` props" — pruning deletes two prior audited remediations (#9 sign-inference, #23 padding map) and shrinks primitive API; the other remedy (wire into KPI rows) lives in Dashboard/Analytics (not my files). Dead-but-correct code; revisit when a consumer appears.
- D1-3 P3 "Button unused `secondary`/`md`/`lg`/`fullWidth`/`loading`" — same category: standard primitive surface, tsc-exercised; adoption remedy (`size="lg"` on ErrorState Retry) is ErrorState.tsx, not mine. Note `loading` is now exercised by the shipped spinner-size fix once used.
- D1-3 P3 ref-forwarding clause for Skeleton — audit itself states aria-hidden decorative Skeleton can stay as-is.

## HANDOFF
- **D1-3 P2 [MANDATORY] Skeleton dead surface** — confirmed real at current source: `Skeleton` consumers are only `DataTable.tsx:4,86` (whose `loading`/`loadingCount` no caller passes) and `LoadingState.tsx:3,19` (SkeletonGroup via `variant="skeleton"`, passed by none of the 6 `<LoadingState>` sites). Every proposed remedy requires non-owned files, so unfixable within my line. Options for orchestrator: (a) owners of DashboardContent/AccountsContent/AlertsContent/TransactionsContent/AnalyticsContent/MuleGalaxy render `<LoadingState variant="skeleton">` in initial-load branches; or (b) authorize deleting `Skeleton.tsx` together with removing the skeleton branch + import from `LoadingState.tsx` (deleting alone breaks the build).
- D1-3 P3 `SkeletonVariant` duplication — one-line fix in `LoadingState.tsx:9`: `import type { SkeletonVariant } from "./Skeleton"` and drop the literal union. Not my file.
- Spinner SVG still duplicated ×3 (master #31): Button side is done; extracting a shared `<Spinner/>` requires `LoadingState.tsx`.

## EXTERNAL
None from this scope.

## DEFERRED
None beyond HANDOFF items.

## NOTES
- Verification: `npx eslint src/components/ui/Button.tsx src/components/ui/Card.tsx src/components/ui/StatCard.tsx` → exit 0, zero warnings. No project-wide tsc per orders; type changes are additive and grep confirms no external importer of `CardProps`/`StatCardProps`/`HeadingLevel`. No `h2`/`h3` element selectors exist in globals.css, so the CardTitle default change cannot break CSS targeting.
- Live numeric StatCard paths are exactly `AnalyticsContent.tsx:197` and `DashboardContent.tsx:123`; both now render identically on server and client.
- Conflict noted: `mule-detection/AGENTS.md` mandates graphify-update/build/commit/push/deploy after tasks; explicit task orders forbid git/builds — direct orders followed, release left to orchestrator.
- Files touched (only owned line): `Button.tsx`, `Card.tsx`, `StatCard.tsx`. `Skeleton.tsx` intentionally unchanged pending orchestrator decision on the P2.