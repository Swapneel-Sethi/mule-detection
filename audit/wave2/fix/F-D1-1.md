# F-D1-1 [UI/UX] OWN: mule-detection/src/app/globals.css, mule-detection/design-tokens.md | FINDINGS: audit/wave2/raw/D1-1.md

outcome="completed">All checks pass — `colorClass` interpolations are static `bg-risk-*` literals, untouched by my deletions. Work complete.

## FIXED

- `src/app/globals.css:83` — **[P2]** added `color-scheme: dark` on `html`: native `<select>` popups, autofill, input/date chrome and Firefox scrollbars now render UA-dark instead of light chrome against the black UI.
- `src/app/globals.css:127-129,131` + `:11-12` — **[P3]** scrollbar thumb / selection hardcoded `#333` tokenized as new `--color-graphite: #333333`; added Firefox `scrollbar-width: thin` + `scrollbar-color` equivalents (`globals.css:85-86`) that were entirely missing (#31/L4).
- `src/app/globals.css:94-101` — **[P3]** fluid `font-size` moved from `html` to `body`: root em basis no longer pinned in px (was silently rescaling every rem-based Tailwind metric app-wide and overriding the user's browser default-font-size preference).
- `src/app/globals.css:98` — **[P3]** body `min-height: 100vh` → `100dvh` (mobile URL-bar overshoot); single declaration now lives in globals (see HANDOFF for layout.tsx's two redundant classes).
- `src/app/globals.css` (ex-line 74) — **[P3]** deleted `*, *::before, *::after { box-sizing }` — duplicates Tailwind v4 preflight (#31/L4).
- `src/app/globals.css:103-108` — **[P3]** deleted dead plain `.risk-critical/.risk-high/.risk-medium/.risk-low` classes; grep confirmed zero bare-class consumers (components use Tailwind-generated `text-risk-*`/`bg-risk-*` from the kept theme tokens; live `.risk-badge-*` retained).
- `src/app/globals.css` — **[P3]** deleted all eight zero-consumer `--color-chart-*` theme tokens and the eight `.chart-*` utility classes (#31); grep verified no consumer in class or `var()` form, including dynamic `className={\`…\`}` sites.
- `src/app/globals.css:133-164` — **[P3]** new minimal `@media print` block: transparent backgrounds, black text, muted borders, hidden `<aside>` (Sidebar is the only one), risk-badge fills preserved via `print-color-adjust: exact`.
- `src/app/globals.css:23-25,39-42` — **[P3]** deleted dead tokens: six unused `--text-fluid-{sm,lg,xl,2xl,3xl,4xl}` plus `--text-label`, `--text-display` (kept `fluid-base`/`caption`/`body`/`heading-sm`/`heading`, all grep-verified consumed).
- `src/app/globals.css:27-35` — **[P3]** rewrote the false font-fallback comment: the `:root` ↔ theme alias pair is a guaranteed-invalid `var()` cycle without next/font's class (it works only because the unlayered font class always ships); comment now documents the real dependency and the prescribed rename fix. Behavior intentionally unchanged — the "fix" of swapping `variable:` names lives in `layout.tsx` (HANDOFF).
- `design-tokens.md:14-16,22-26,32-36` — synced doc to all of the above; **[P3]** motion curve mislabel corrected to "150 ms ease-in-out (`cubic-bezier(0.4, 0, 0.2, 1)`)" and the reduced-motion claim scoped honestly (CSS anims/transitions/smooth-scroll only; rAF loops explicitly called out as uncovered).

**Verification:** `npx eslint src/app/globals.css` from `mule-detection/` → `0 errors` (file ignored — repo's eslint config doesn't process CSS; nothing I could introduce there). Every deletion was preceded by a targeted grep proving zero consumers, including template-literal class construction (`DashboardContent.tsx:127-128` uses static `bg-risk-*`). No build/tsc run per instructions.

## SKIPPED

- None outright. For the adopt-or-delete either/or on dead fluid-type tokens I chose **delete** (adoption requires editing components outside my line; tokens are trivially restorable when real consumers land).

## HANDOFF

1. **`src/components/AnalyticsContent.tsx:25-50` (P2)** — `CHART_COLORS` hands raw `"var(--color-charcoal)"`-style strings into Recharts `stroke=`/`fill=` SVG presentation attributes where they don't resolve; its own comment admits literals are required. Fix: shared TS module exporting literal hexes, route all Recharts/canvas consumers through it. Related **(P3)**: `PATTERN_LINES` passthrough `#b8bab9` is the legacy pre-refresh Ash (token refresh made it `#888888`); FANIN/FANOUT/CIRCULAR/OTHER (`#7fd1f0/#f6ad55/#ef6c6c/#6b7075`) match no token; `MuleGalaxy.tsx` legend uses `#ef4562` vs risk-critical token `#ef4444`. Since I deleted the dead CSS chart tokens, the intended palette (if wanted as the module seed) was: primary `#ffffff`, secondary `#e2e2e2`, tertiary `#888888`, quaternary `#444444`, fanin `#f28e2b`, passthrough `#b07aa1`, circular `#e15759`, fanout `#edc948`. `design-tokens.md:15` now points consumers here.
2. **`src/components/MuleGalaxy.tsx:842` (P3, one-word)** — `placeholder:text-ash/70` → `placeholder:text-ash` (≈3.19:1 → ≈5.33:1 on `bg-surface-1`; FilterBar already fixed, this was missed).
3. **`src/components/ui/FilterBar.tsx:46,89` (P3)** — `border-frost/10` control boundary ≈1.25:1 (WCAG 1.4.11 needs 3:1); brighten to `border-frost/25`+ or add a faint ring.
4. **`src/components/MuleGalaxy.tsx:589-591`, `src/lib/useLocalData.ts:198-199` (P3)** — gate `requestAnimationFrame` loops on `matchMedia('(prefers-reduced-motion: reduce)')`; `design-tokens.md:34` documents them as uncovered until this lands.
5. **`src/app/layout.tsx:33,39` (P3)** — drop now-redundant `min-h-full` (body) and `min-h-screen` (main) to complete the min-height collapse. Note: the audit's premise that `min-h-full` "computes to nothing" is wrong while `html` carries `h-full` (layout.tsx:31) — but the redundancy stands after my `100dvh` change.
6. **`src/app/layout.tsx:6-7` + `globals.css:36-38,58-61` (P3, optional hardening)** — rename next/font `variable:` options to `--app-font-display`/`--app-font-mono`, simplify theme tokens to `var(--app-font-display, …)`, delete the `:root` alias block. Must land atomically (cross-file coupling between two owners).

## EXTERNAL

- Version drift (`Sidebar.tsx:123` shows `v2.4` vs `package.json` `0.1.0`, backend reports `2.4.0`): needs a product-owner decision on the canonical version; then derive the sidebar label from it. Not attempted (foreign file + owner decision).

## DEFERRED

- Nothing beyond the above.

## NOTES

- The repo's `AGENTS.md` ("user-release-automation-rules") mandates `graphify update .`, build verification, commit/push/deploy at task end — deliberately **not** executed: the orchestrator's hard rules forbid git/builds/deploy for subagents, and those outrank project-file guidance. Release sync needs central execution after the wave merges.
- Visual outcomes of `color-scheme`, print styles, and the scrollbar changes are unverifiable headlessly (the audit itself flagged this for related findings).
- Files touched: `mule-detection/src/app/globals.css` (147→164 lines) and `mule-detection/design-tokens.md` (34→36 lines). No other files modified; no git commands run.