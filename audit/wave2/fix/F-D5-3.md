# F-D5-3 [PAGES-API] OWN: mule-detection/src/components/AnalyticsContent.tsx | FINDINGS: audit/wave2/raw/D5-3.md | ALSO check D1-6.md for your files | P1: FANOUT bar invisible charcoal fill -&gt; distinct visible color; abort/unmount guards; honest failure states

outcome="completed">## FIXED

All in `mule-detection/src/components/AnalyticsContent.tsx` (only file touched; verified against current HEAD, line numbers below are post-edit):

- **:204-212, :26-30** [D5-3 P2|UNFIXED / D1-6 P1] FANOUT bar no longer `var(--color-charcoal)` (#1a1a1a on #000, ~1.1:1). All four bars now derive fills from `PATTERN_DOT_COLORS` (the PATTERN_LINES palette the trends chart + chip dots already use), so FANOUT renders visible orange and one pattern keeps one color across every chart on the page. Orphaned `bone`/`ash`/`accentCircular` entries + their stale "distinct from FANOUT" comment removed.
- **:353-361** Dimming on filter now via `fillOpacity={0.15}` instead of swapping fill to charcoal — kills the dimmed-vs-FANOUT collision (old dim fill was byte-identical to FANOUT's base).
- **:108-161, :172-173** Abort/unmount guards unified: `inFlight` ref'd controller shared by initial effect and manual retry (`retry` aborts any earlier attempt before refetching; unmount cleanup aborts whatever is in flight). Previously the retry button fetched signal-less → post-unmount setState + last-response-wins races.
- **:116-132** Honest failure state: unreachable `json.error` branch folded into the `!r.ok` path — server's `{ error }` reason is now surfaced to the UI (route only ever sends it with HTTP 5xx).
- **:163-188** Loading and error branches now render `<PageHeader>` inside the standard `p-8 max-w-[1200px] mx-auto` wrapper (Dashboard convention); redundant `error ? … : ""` ternary dropped.
- **:190-198, :269** X-axis caption no longer hardcodes "Date [Aug 2026]" — month span derived from data's MM-DD day keys ("Date [Aug]"); year intentionally not claimed since the payload carries none.
- **:229** Alerts StatCard preformats `toLocaleString("en-IN")` like siblings — also removes the sole live trigger of D1-6's StatCard locale-split finding.
- **:355, :459, :552** `key={index}`/`key={i}` → stable keys: `entry.pattern`, `entry.name`, `${path.from}-${path.via}-${path.to}`.
- **:427, :479-481** Recharts ticks raised from `fontSize: 9` → 10 (both charts); Money Flow category axis `width` 70 → 96 so ~17-glyph risk-pair labels stop clipping.
- **:252** Decorative ✕ wrapped in `<span aria-hidden="true">` so SRs don't announce "ballot x".
- **:48-70** Interface trimmed to rendered fields (`muleAccounts`, `cleanAccounts`, `inOutData` removed; `moneyFlowData.amount`, `volumeByDay.transactions` narrowed out) with a comment documenting the unconsumed API fields.
- **:224** Main content wrapper aligned to `max-w-[1200px] mx-auto` (D1-6 page-wrapper convention split).

## SKIPPED

- **Recharts eager-import bundle hazard (D5-3 P3/S17)** — lazy-loading chart blocks is a structural refactor (next/dynamic + Suspense boundaries across 6 chart sections) with real SSR/regression risk for a perf-only win; "accept documented cost" branch taken.
- **Mass `text-[11px]→text-caption` / `text-[13px]→text-body` token replacement (D1-6 app-wide)** — a repo-wide mechanical pass (~100+ hits); doing it file-by-file mid-wave creates mixed conventions. Better as one coordinated sweep.

## HANDOFF

- **[P1] sankeyByPattern top-20 slice — `src/app/api/analytics/route.ts:345-348` (+ SankeyChart.tsx consumer)**: corridors sliced before shipping, so Sankey totals and my filter chip show 1–5% of true pattern volume (~90× gap vs bar chart). Fix belongs to route owner: aggregate non-top-20 into explicit "Other"/ship full aggregation, cap at render; then qualify chip copy. Consumer-side change alone would be misleading, so I left it.
- **[P2→stale] Account Categories parity**: D5-3's evidence no longer holds at HEAD — `data-local/route.ts:166-179` now computes the identical disjoint formula (mule∧severity-tier / remainder) over the identical flagged universe as analytics. My file's parity comment (:214-215) is accurate again; no edit made. Remaining stale artifacts for other owners: analytics `route.ts:69-75` NOTE mischaracterizing data-local's formula, and `DashboardContent.tsx:58-62` fallback encoding a third formula (latent only — API stats always override).
- **route.ts items (not my file)**: A11 `slice(-6)` corridor keys merging accounts (:328-330); A24 volume-chart universe incl. unflagged (:178-196); A25 `.slice(5)` day keys dropping year (:168-176); cycle dedup `[from,via,mid].sort()` orientation loss + silent 10-of-41 cap (:292, :376); unused payload fields still shipped (`muleAccounts`, `cleanAccounts`, `inOutData`) if you'd rather drop than render.
- **Cross-file palette/token consolidation**: `--color-chart-accent-*` tokens in globals.css have zero consumers while literals live in AnalyticsContent/SankeyChart/MuleGalaxy; `SankeyChart.tsx:257` legacy `#444345`; shared `MONO_FONT` constant — needs one owner across those files.

## EXTERNAL

None arising from this line.

## DEFERRED

None beyond SKIPPED items above.

## NOTES

- Verification: `npx eslint src/components/AnalyticsContent.tsx` from `mule-detection/` — clean exit, zero findings. Project-wide tsc intentionally not run per instructions. No build/dev-server/git commands used; no secrets touched.
- The month-derived caption drops the year ("Date [Aug]" not "[Aug 2026]") because the shipped day keys are MM-DD only — the route owner could restore year display by shipping YYYY-MM-DD internally (also fixes A25).
- `mule-detection/AGENTS.md` carries a release-automation block (graphify update → build → commit → push → Vercel deploy). Every step conflicts with this wave's hard rules (no git, no builds); not executed — flagging for the orchestrator to run centrally at release time.